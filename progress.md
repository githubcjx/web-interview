# 复习进度总览
> 最后更新：2026-08-17 ｜ 当前周次：**第 5 周** ｜ 目标面试时间：未定（暂无邀约，碎片化备战）

## 模块掌握度
| 模块 | 状态 | 备注 |
|---|---|---|
| 1. JS 核心 | ⚠️ | Promise 四大静态方法 ✅；rAF 归属与自调度 ✅；副作用概念已讲；闭包/原型链/this/宏微任务 未测 |
| 2. Vue | **✅ 掌握** 🎉 | **08-07 结业测验 4.5/5 通过，13 个模块中第二个 ✅**（07-15 开荒 → 08-07 结业，跨 8 次会话）。⭐ 测验含**两道从未讲过的新场景**（组合式函数传值丢响应性、多语言前缀 vs 密码过期的死循环判断），**全部靠规律推对**——"复述通过 ≠ 能迁移"的缺口在此翻过。残留精度（不影响结业，滚动抽查）：`inject` 必须同步调用的**原因**、组件库 key 用 `Symbol`+`InjectionKey<T>`、`computed` 三级判断的中间一级。**后续只做长期滚动抽查**。以下为学习过程记录： | 响应式主线：ref/.value + reactive 局限 ✅、computed（惰性+缓存+版本号计数+双向链表）✅、computed vs methods 四判据 ✅、props 解构（3.5+）✅；`watch` 的 `flush` 三档 + 为什么默认 `pre` + `sync` 的 glitch ✅、`nextTick` 原理（异步批量+调度队列+微任务+Vue2 四级降级 vs Vue3 纯 Promise）✅；**08-05 第 2 段新增**：`key` 与 diff（就地复用 / index 做 key 两层后果 / 同层比较 O(n³)→O(n)）✅、`keep-alive`（cache+keys / `onActivated` / `max` 用 LRU / 内存权衡）✅、组件通信分类框架 + **`defineModel()` 3.4 转正** + 为什么移除 EventBus ✅、`provide/inject` 深入（传容器不传值 / 同步调用 / `readonly`+修改方法 / `Symbol`+`InjectionKey`）✅、Vuex vs Pinia 三条设计动机 ✅、⭐ **统一规律「响应性属于容器不属于值」**（四处同根因收拢）✅；**08-06 新增**：Vue Router 导航守卫全覆盖（三层作用域框架 / 为什么分层是"能拿到什么"而非"写在哪" / Pinia 单例反问 / v4 返回值风格取代 `next` / 登录拦截死循环 + **通用判据「重定向后条件还成不成立」** / `meta.requiresAuth` vs 白名单 / `redirect` 开放重定向 / 动态权限 `addRoute` + **「前端权限是体验不是安全」** / 执行顺序三段 / `beforeResolve`/`afterEach` / 异步守卫必配进度条+try-catch）✅；**🎯 Vue 内容已全覆盖，下次直接做结业测验，过了即第二个 ✅** |
| 3. React | ❌ | 未开始 |
| 4. TypeScript | ❌ | 未开始 |
| 5. 小程序与跨端 | **✅ 掌握** 🎉 | **08-05 结业测验 5/5 全过，13 个模块中第一个 ✅**（07-29 开荒 → 08-05 结业，共 5 次会话）。覆盖：双线程架构、setData 成本模型与三方向、WXS、性能定位（真机/回调打点/stringify）、登录鉴权六步 + 三铁律 + OAuth 本质、手机号新方案、分包三规则 + `preloadRule` + 独立分包（含 `getApp()` 坑）、长列表（流量 vs 存量 / 数据路径追加 / 虚拟列表 / 前缀表+二分 / recycle-view）、跨端选型、移动端适配（rpx≡vw / 1px 根因 / 安全区 `wx.getWindowInfo`）。话术库已收录 7 条。**后续只做长期滚动抽查** |
| 6. CSS 与浏览器 | ⚠️ | **08-17 新增：🔴 输入 URL 全流程开荒 + 渲染流水线全覆盖**——七站骨架 ✅（含答题策略"地图题，先给骨架、把深挖权交给面试官"）、解析阻塞三层链 ✅（**`<script>` 阻塞 HTML 解析 → CSS 阻塞 JS 执行 → 故 CSS 间接阻塞 DOM 构建**，第三层无提示答对）、主线程四步（样式计算/布局/分层/**绘制产出的是指令列表不是像素**）✅、合成线程三步（分块/光栅化/合成）✅、回流 vs 重绘的机制定义 ⚠️（**漏"样式计算跑不掉"**）、`transform` 只走合成 + **合成线程 ≠ 主线程** ⚠️（说小了）、**强制同步布局 + 读写分离** ✅（🏠 已串到小程序 `setData` 批量）。话术库 +2（共 7 条）。**七站里仍空白：TLS 握手（🔴，挂一个月）、TCP 握手挥手细节、DNS 递归查询**。 ｜ **08-12 新增：CSRF / `SameSite` 全覆盖**——CSRF 本质定义「根源不是攻击者能发请求，而是浏览器自动替他鉴权」✅、判据「凭证是浏览器自动附加还是 JS 主动附加」✅、`SameSite` 三档（`Strict`/**`Lax` 默认**/`None`+`Secure`）与 Lax 为何能挡 CSRF（攻击用的表单 POST、`<img>` 都不是顶级导航）✅、**同站(eTLD+1) vs 同源(协议+域名+端口)**⚠️（表述会滑）。话术库 +1（共 5 条）。 ｜ **08-11 新增：Storage 三兄弟全覆盖**——总根因「cookie 自动携带 → 4KB 限制 + CSRF 同源」✅、cookie vs storage 的物种分野（协议 vs 存储 API）✅、localStorage(origin) vs sessionStorage(origin+标签页)✅、多标签页多账号登录场景 ✅（自发讲权衡）、**sessionStorage 跨标签页复制规则**（只有 opener 关系才复制；`<a target="_blank">` 自 Chrome 88 隐式 noopener → **不复制**，Chrome 89 起对齐规范）✅、**token 存储权衡**（内存/localStorage/cookie/HttpOnly 各自怎么死）⚠️、静默刷新 + 与路由守卫的 `authReadyPromise` 冲突 ✅。**缺口：`HttpOnly` 与 `SameSite` 分工混淆**（见薄弱点）。话术库 +2 条（共 4 条）。 ｜ **08-07 开荒：强缓存 vs 协商缓存全覆盖**——两级分工（强缓存管"发不发请求"/协商管"传不传数据"）✅、`Cache-Control` vs `Expires`（客户端时钟）✅、`no-cache` vs `no-store`（**理解 ✅ 但表述会混，头号翻车点**）⚠️、`ETag`/`Last-Modified` 两对 header + 三个缺陷 + 为什么不全用 ETag ✅、memory/disk cache、F5 vs Ctrl+F5 行为差异、**hash 文件名 + `immutable` 与 HTML `no-cache` 的标准搭配**（缓存 key 是 URL）✅、发版 404 排障三层防线（第二层"保留旧资源"漏答 ⚠️）。话术库已收录 2 条。**待开**：storage 三兄弟、URL 全流程、BFC/居中/Flex、XSS/CSRF |
| 7. 性能优化 | ⚠️ | **08-17 拿到地基**：回流/重绘的**机制级**理解（不是背名词，是"从流水线哪一步开始重跑"）、`transform`/`opacity` 只走合成且**不在主线程**（JS 卡死时动画照常）、`will-change` 的代价、**强制同步布局与读写分离**（含触发属性清单）。08-03 另有：不定高虚拟列表（前缀高度表 + 二分）。**待开：Web 指标 LCP/INP/CLS、首屏优化、内存泄漏排查、Web Worker、监控 SDK** |
| 8. 网络与通信 | ❌ | HTTPS、SSE 均不会 |
| 9. AI 应用前端 | ⚠️ | **SSE 全链路结业**：选型✅、fetch+ReadableStream+AbortController✅、六连接坑✅、断点续传(Last-Event-ID+服务器缓存)✅、指数退避✅、流式渲染(chunk边界/TextDecoder/Markdown截断/rAF批量/自动滚动)✅、DOMPurify防注入✅；TTFT⚠️(量法对、指标设计意义答浅)；**RAG ⚠️ 已下调为"前端最小可用版"**(链路+前端三角色+权限隔离动机，已入话术库；切块/Hybrid/评估仅"听过一遍"不作记忆要求)；**待开**：Function Calling、MCP、Agent loop、Prompt 工程 |
| 10. 工程化 | ❌ | 未开始 |
| 11. 场景题 | ⚠️ | **08-12 开荒：🔴 双 Token + 无感刷新 + 并发请求队列，四段全打通**——① 存储方案（内存+`HttpOnly` cookie 的两个约束打架）✅ ② 静默恢复（启动调 `/api/refresh` + `authReadyPromise` 守卫冲突）✅ ③ **并发刷新**（`refreshPromise` 单例 / `_retry` 限次 / 刷新接口走裸实例 / `finally` 置空 / 重发换 header）✅ ④ **分页错乱 = 响应竞态**（序号比对 / AbortController / takeLatest）✅。⭐ 杀手理由靠 rotation 自己推出：**重复刷新不是"浪费请求"，是把用户踢下线**。话术库已收录 1 条（含完整代码）。⚠️ **拦截器代码是"带写"的不是闭卷，需安排一次闭卷默写**。其余场景题（大文件分片、10 万条列表、组件设计、i18n/权限）未动 |
| 12. 手写与算法 | ⚠️ | **08-06：防抖、LRU 双双闭卷一次成型 → 均升「面试可用」**（清单里前两个可用题）；节流仍欠（时间戳版对，缺 trailing）；⭐ 工具箱已有两件：**「时序表」验证法**（08-05 首次自主生效）+ **「删掉这句，哪条用例会红」**（08-06 新增，变异测试直觉版）；其余 7 题未动 |
| 13. 非技术面 | ❌ | 未开始 |

## 薄弱点清单（待复查）
> 07-24 已清理：`props 解构归因`、`副作用`、`嵌套 computed 拉模型`、`AI 断点续传服务器端`、`代码块流式难点`、`SSE 实战细节`。
> **07-31 已清理**：`setData 性能定位的具体工具与打点`（连续两次未过，本次通过）、`"降低凭证价值"的安全设计通则`、`TTFT 的指标设计意义`、`写完代码必须自跑时序表`（已教方法并首次跑对 2.5/3）—— 全部移出。

> **08-06 已清理**：`防抖 immediate 的执行时序`（闭卷一次成型，六版长跑收官）、`undefined vs null 与 === 的配套`（本次主动改用 `!timer`）、`跑时序表从代码字面读值`（本次未复发）—— 全部移出。

> **08-11 已清理**：`no-cache vs no-store 的表述精度`（08-07 挂上，本次一次通过）、`发版 404 的第二层防线`（链条+归因全对）、`inject 必须同步调用的原因`（机制满分）—— 全部移出。

> **08-12 已清理**：`HttpOnly 与 SameSite 的分工混淆`（08-11 挂上，本次一次答对）、`多问的题只答前半段`（08-11 挂上，本次三题均完整作答，转为观察）—— 全部移出。

**手写题（最高优先级）**
- [ ] **节流 trailing 分支 + 时间戳版/定时器版的行为差异** —— 只写了时间戳版且 `timer` 是死代码；未答"首次必执行、末次必丢失 vs 反之"（首次暴露：07-24，状态：**欠账最久的一题**，补写时必须附时序表 + 先列用例清单）

**AI 模块**
- [ ] **向量检索对专有名词/型号的天然局限** —— 07-24 Q2 答错（错归因为"知识库没数据"）。要点：embedding 编码语义，型号几乎无语义，`XR-2000` 与 `XR-3000` 向量几乎重合 → 解法 **Hybrid Search（BM25+向量+RRF）** + 元数据过滤（首次暴露：07-24，状态：待复查）
- [ ] **RAG 效果优化的第一步是建评估集** —— 07-24 有分层意识但缺"量化基线"概念，也漏了数据层（解析就错了）（首次暴露：07-24，状态：待复查）

**跨模块的稳定缺口（最高优先级）**
- [ ] **⚠️ 「照建议改，但改过头 / 改错位置」——已 3 次复发** ① 07-24 防抖第 2 版（在 `callNow` 判断前调 cancel）；② 07-31 防抖第 1 版（同一 bug 换变量名）；③ 07-31 教练说"`if (!timer)` 冗余、直接写赋值"，他把 `if` 和 `setTimeout` **一起删了**，冷却期消失。**根因：执行建议而不追问建议保护的是什么。对策（双向）——他：每接受一条建议先问"这条保护的是哪条不变量"、删任何一行前先问"这行在维持什么"；教练：给建议时必须说清边界（改哪行、保留什么）。**（首次暴露：07-24，状态：高频待观察）
- [ ] **讲权衡不跟缓解手段** —— 07-31 话术训练说"进二级页面会慢，这是权衡后的决定"，听起来像接受问题而非解决问题。**通用话术规则：讲权衡永远要跟一句"我怎么补的"**（此处即 `preloadRule` / 独立分包）（首次暴露：07-31，状态：待复查）
- [ ] **跑时序表：值必须从代码字面读，不能从"我以为"读** —— 07-31 填出自相矛盾的行未察觉；**08-05 升级版**：把 `let timer;`（`undefined`）的初值填成了 `null`，掩盖了 `timer === null` 恒假这个 bug。铁律：**从预期读值，表就只是想法的复印件，不是行为的显微镜**（首次暴露：07-31，08-05 复发升级，状态：待复查）
- [ ] **⚠️ 用例设计：会造陷阱用例，但不做分支盘点（08-06 更新）** —— 08-06 首次执行"先列用例再写代码"：**进步**是 LRU 里自己想到了"`get` 影响淘汰顺序"这条经典陷阱；**缺口**是只按直觉造两条，不按分支清点 —— 防抖缺"定时器到期行"和"冷却期后再调用"（**导致把 `timer = null` 删掉他的用例全绿**），LRU 4 个分支只覆盖 2 个。方法已给两条：**① 每个 `if` 分支至少一条 + 边界各一条；② 对每句"状态重置"问"删了它哪条用例会红"**（首次暴露：08-05，状态：训练中，下题继续要求）
- [ ] **⚠️ 理由要区分"做不到"和"不好做"—— 已 2 次复发** —— ① 08-05 表单组件库题（**杀手理由是"中间层是用户写的组件，组件库无权要求它们透传"**，不是"传不下去"）；② 08-06 三层守卫题答"代码好维护"（**"不好做"级，挡不住"beforeEach 里按 to.name 分发不也一样"；正解是全局守卫只有 to/from，拿不到组件实例**）。通用自查：**说完一条理由，问自己这是"做不到"还是"只是麻烦"**（首次暴露：08-05，08-06 复发，状态：下次主动设陷阱测）
- [x] ~~**抽象规律的提取（08-06）**~~ —— **08-07 结业测验 Q3/Q4 两道全新场景全部推对，已修复移出**。但**教学手法保留为常规动作**：讲完一个机制必追一道"用它判断没讲过的新场景"，这是检验抽象能力的唯一方式。
- [x] ~~**`no-cache` vs `no-store` 的表述精度**~~ —— **08-11 一次通过，移出**。他的定版表述："no-cache 是缓存但每次都要问服务器，跳过强缓存直接到协商；no-store 是一个字节都不存"。**顺带补的精度点（当时答错已纠正）：`no-cache` 也每次都发请求，省的是响应体**；三档成本 `no-store` > `no-cache` > `max-age` 命中（唯一不发请求的）。
- [x] ~~**发版 404 的第二层防线**~~ —— **08-11 链条 + "与 HTML 缓存无关"全对，移出**。补入的分水岭句：**HTML 缓存策略只对"新进入的用户"生效，对"已在页面里的用户"完全无效**（他不刷新就不会再请求 HTML）。
- [x] ~~**`HttpOnly` 与 `SameSite` 的分工混淆**~~ —— **08-12 一次答对，移出**。定版表述：**`HttpOnly` 管"谁能读它"（防 XSS）／`SameSite` 管"什么时候发它"（防 CSRF）**。
- [x] ~~**多问的题只答前半段**~~ —— **08-12 三题均完整作答，未复发，转为长期观察**。
- [ ] **同站(same-site) vs 同源(same-origin)** —— 08-12 把 `SameSite` 解释成"同源请求才带"。**同源看三件（协议+域名+端口），同站只看可注册域名 eTLD+1**（端口不算、协议算 = Schemeful Same-Site）。关键推论：`a.example.com` 与 `b.example.com` **跨域但同站，cookie 照带**（首次暴露：08-12，状态：下次开场必测）
- [ ] **概念联想断层** —— 08-12 问"表示一个现在没有、未来会有的值"，答 `null`（正解 `Promise`）。**基础概念在"被反过来问"时调不出来**——正着问"Promise 是什么"他答得出。属于知识组织方式问题，非知识缺失（首次暴露：08-12，状态：观察，后续多用反向提问）
- [ ] **描述 bug 时只想良性情形** —— 08-12 分页错乱只说出"第 2 页先回、第 3 页后回"（**最终结果正确，只是闪一下**），没想到真正的 bug 是**第 3 页先回、第 2 页后覆盖 → 稳定错误状态且不会自恢复**。对策：**描述并发/时序 bug 时，主动去找最坏的那个顺序**（首次暴露：08-12，状态：待复查）
- [ ] **渲染流水线：判断"哪几步重跑"时漏掉「样式计算」** —— 08-17 答"改 `background-color` 只重新绘制"。**样式计算跑不掉（改的就是样式）**，真正跳过的是**布局**——这才是"重绘比回流便宜"的原因（首次暴露：08-17，状态：下次开场必测）
- [ ] **`transform` 的省法说小了** —— 08-17 答"从光栅化开始"。正解：**只需要重新「合成」，连光栅化都跳过**（位图已存在，只换变换矩阵）；且**合成在合成线程**，所以 JS 卡死主线程时 `transform` 动画照常、`left` 动画冻结（首次暴露：08-17，状态：下次开场必测）
- [ ] **以为"写样式会立刻触发回流"** —— 08-17 强制同步布局题。实际：**写是批量的**（连改 100 次也只回流 1 次），**读写交替**才是元凶——读几何值时浏览器被迫同步 flush。解法：**读写分离**（首次暴露：08-17，状态：待复查）
- [ ] **⚠️ 似是而非的论断要先验证成不成立 —— 已 2 次复发** —— ① 08-06 答"不做动态路由，用户改 URL 就能进无权限页"（**不成立**，守卫拦得住）；② **08-12 用"内存里有没有 access token"作为区分"token 过期 / 权限不足"的判据**——**反例就在题目自己的场景里**：用户登录期间内存里永远有 token（只是过期了），这个条件在两种情况下都成立。正解是**不猜原因、用 `config._retry` 限次数**（结构封死），并推动后端用 `401`/`403` 区分语义。**自查：这句话我能不能举出反例？**（首次暴露：08-06，08-12 复发，状态：高频待观察）

**Vue（模块已 ✅，以下转为长期滚动抽查）**
- [x] ~~版本号计数 / 双向链表机制~~ —— **08-07 Q5 答出方向，补齐三级判断后收口**（挂 22 天）。抽查话术：**先问"世界变了吗"（`globalVersion`）→ 再问"我的依赖变了吗"→ 最后问"我的结果真变了吗"**，三道闸一道比一道贵。
- [x] ~~ref 自动解包只在**顶层**生效~~ —— **08-07 Q5 第 2 问满分**（`{ count: ref(0) }` 在模板里原样渲染 ref 对象）。补：`<script setup>` 是编译时 `_unref`，选项式 `setup()` 返回值是运行时 `proxyRefs`，**两条路径都只作用于顶层**。
- [x] ~~`inject` 必须同步调用的**原因**~~ —— **08-11 机制满分移出**（`currentInstance` 是模块级变量，setup 前 set、返回后立刻 unset；`inject` 靠它沿 parent 链找 `provides`）。已补 3.x 精度：**`<script setup>` 顶层 `await` 之后 `inject` 仍可用**（编译器包成 `withAsyncContext` 保存/恢复实例），手写 `async setup()` 则不行 —— 规则本质是"必须在 `currentInstance` 有效窗口内"，不是"不许异步"。
- [ ] 组件库 provide key 用 `Symbol` + `InjectionKey<T>` —— **08-07、08-11 连续两次漏答**（08-11 是漏答不是答错）。两个理由分层：**Symbol 从机制上杜绝冲突**（字符串加前缀只是"降低概率"，属于"只是麻烦"级）／**`InjectionKey<T>` 让 `inject` 自动推导类型**，字符串 key 永远推不出（首次暴露：08-07，状态：滚动抽查）

**未学（0 分区，需排进日程而非"复查"）**
- [x] ~~强缓存 vs 协商缓存的区别与响应头~~ —— **08-07 已讲透**（残留两点见上）
- [ ] **HTTPS / TLS 握手：非对称+对称加密各用在哪步、为什么这么设计** —— **挂了一个月（07-15 首次暴露），且是"URL 全流程"七站里唯一的完整空白**（08-17 摸底时他只讲 TCP 三次握手，没提 TLS）。**已升为下次主线首选。**
- [x] ~~不定高虚拟列表：前缀高度表 + 二分查找~~ —— **08-03 已讲透**（"为什么能二分"待复查，见上）
- [ ] Web 性能指标 LCP/INP/CLS 的定义与优化（首次暴露：07-15）

## 手写题清单
| 题目 | 状态 | 缺陷备注 |
|---|---|---|
| 防抖（含 immediate） | **✅ 面试可用**（08-06 达成） | 08-06 闭卷**一次成型**，六版长跑收官。守住两条：`setTimeout` 无条件执行（不进 `if/else`）、`!timer` 与 `let timer`(undefined) 配套；`callNow` 在 `clearTimeout` 之前读；主动加 `cancel` 且内部置 `timer = null`（cancel 后 immediate 能立刻再响应，加分项） |
| 节流 | **有缺陷（待补完）** | 07-24：时间戳版核心公式 ✅、this/args ✅、复用同一 now ✅。缺陷：`timer` 声明后从未赋值（死代码）、**缺 trailing → 末次触发永久丢失**；未写定时器版、未答两版行为差异、未跑时序表 |
| 深拷贝 | 未写 | |
| Promise.all | 未写 | 原理已讲清（含非 Promise 值自动包装），可优先手写巩固 |
| LRU 缓存 | **✅ 面试可用**（08-06 达成） | 08-06 闭卷**一次成型**，08-05 的三个 bug 全部未复发（尤其**容量检查放在 `has→delete→set` 之后**）。以下为 08-05 原始记录：⭐ **两个最难点自己想到**：选 `Map`（唯一同时具备"记插入顺序 + O(1) 增删查"）、`delete` 后 `set` 回去实现"移到末尾"。三个 bug：`this.map.keys[0]`（keys 是方法且返回迭代器 → 应为 `keys().next().value`，原写法致淘汰完全失效）、`get` 未命中没 `return -1`、**容量检查放在 `has→delete` 之前致"更新已存在 key"时误淘汰**。串联：`keep-alive` 的 `max` 内部就是 LRU |
| 超时重试 | 未写 | |
| 数组转树 | 未写 | |
| 并发数限制的异步调度器 | 未写 | |
| Promise 红绿灯 | 未写 | |

## 模拟面试记录
| 日期 | 轮次 | 总分 | 最大短板 |
|---|---|---|---|
| —— | 尚未开始 | —— | —— |

## 路线调整记录
- 2026-07-15：碎片化时间备战、暂无面试邀约 → 六周路线改为「**每次一个点讲透**」的弹性节奏；优先级：先补 JS/Vue 精度 → 尽早前置 AI 模块（差异化）。
- 2026-07-15：新增 `answers.md` 面试话术库；CLAUDE.md 第五节加第 9 条版本时效性原则、第六节改为多文件自主维护。
- 2026-07-16：会话日志改为 `sessions/` 目录、按日期一天一文件；取消汇总索引，全局状态只由 progress.md 负责。
- 2026-07-17：新增 README；约定每次会话结束自动 git commit+push 到 web-interview 仓库（已写入 CLAUDE.md 第六节）。〔07-29 依据 commit de9319a @ 07-17 18:00 更正原记录的 07-21〕
- 2026-07-22：SSE 全链路结业（对应 `sessions/2026-07-21.md`，commit 0852ce0 @ 07-22 11:35），进入第 2 周。
- 2026-07-24：**AI 模块深度下调**。RAG 讲超纲（BM25/RRF/HNSW/cross-encoder 属后端与算法词汇，前端岗不考此深度），学生反馈"吸收有限、名词听不懂"。根因判断：**AI 后端知识缺乏"挂钩点"**——Vue 学得快因为天天写、SSE 学得下去因为落在前端能摸到的地方（fetch/流/渲染），而向量/切块/召回全是悬空名词。→ RAG 降为"前端最小可用版"，Function Calling/MCP 后续同样按"前端视角"讲，不深入后端实现。
- 2026-07-24：**下阶段主线切到 🏠 小程序模块**（原计划的 AI 后续内容让位）。理由：他有真实项目经验（挂钩点最多、最省力）、是简历亮点却从未打磨、能反向带出通用原理（双线程≈进程隔离、setData≈跨线程通信成本）。
- 2026-07-24：**教学纪律调整**——改为"每次只讲 1 个点，讲完立刻让他复述"，不再一次性倾倒大信息量（他的强项是靠复述钉死表述，弱项是听完即滑走）。
- 2026-07-29：**"挂钩点"假设被数据证实**——同期对比：**RAG 复述通过率 1/3，小程序 8/9**。唯一差别是有没有真实经验做附着点。→ 确认模块优先级：**有经验挂钩的优先**（小程序 → Vue → 浏览器/网络八股 → 场景题），AI 只保留前端视角层，不碰后端实现细节。
- 2026-07-31：**提问纪律（他本人提议，采纳）**——① **一次只问 1 题**，答完追问、追问完再下一题；不再一次抛 3 题（一次一题才能形成追问链，也才贴近真实面试节奏）。② **同一天刚讲的内容不做滚动复习**（测的是短期记忆，无留存价值），滚动复习只针对**上次及更早**的内容；当天未答出的题按"欠账"留在薄弱点清单，下次开场补。
- 2026-07-29：**日志日期纪律**——发现 `sessions/2026-07-22.md` 实为 07-24 的会话（依 commit cae98e9 @ 07-24 11:54），已 `git mv` 更正为 `sessions/2026-07-24.md` 并同步文内日期；progress.md 中相关 07-22 引用一并改为 07-24，README 条目由 07-21 更正为 07-17。**今后写日志前必须先核对系统日期，不靠推测。**
- 2026-08-05：🎉 **模块 5「小程序与跨端」结业测验 5/5 全过，正式升 ✅——13 个模块中的第一个 ✅**（07-29 开荒 → 08-05 结业，5 次会话）。后续只做长期滚动抽查。**下阶段主线转 Vue**（他的主场、挂钩点最多、响应式主线已闭环，补完 watch/nextTick/diff/keep-alive 可冲第二个 ✅）。
- 2026-08-05（第 2 段）：**⭐ 知识串联出现最强一次表现**——Pinia 解构题他**没学过 `storeToRefs` 却自行推出"会有语法糖"**，归因为"解构切断响应链，类似 reactive 解构"。据此把四处同根因收拢成一条可朗读的规律：**「响应性属于容器，不属于值」**（reactive 解构 / watch 传值 / provide 传 `.value` / store 解构，解药统一为"包回容器"或"延迟取值"）。**这条已入话术库，是 Vue 模块的最高层抽象。**
- 2026-08-05：**教练手法调整（效果显著，固化）**——他说"××你帮我做"时，改为**"我做第一步，其余交还给他"**。本次防抖时序表用此法：教练只填 t=0 一行，t=50/t=80 交他填，**结果 bug 是他自己抓到的**（"执行了 3 次，正确是 1 次，我又写错了"）。比直接代劳有效得多，也比硬性拒绝更不伤积极性。
- 2026-08-03：**🏠 小程序模块内容全覆盖（5/5）**，用时 4 次会话。下次开场做 5 题结业测验，通过则成为 13 个模块中的**第一个 ✅**。之后建议顺序：Vue 继续（挂钩点最多）> 浏览器强/协商缓存（0 分区、20 分钟可拿一个 ✅）。
- 2026-08-03：**⭐ 他首次对教练的判断提出反驳且论证有价值**（uni-app 提前上车题：主张"迁移成本高，提前付值得"）。**这是好现象——面试中面试官会 challenge，能守住或调整立场是加分项。** 教练侧记录：**他反驳时先认真评估论证是否成立，不要一律当错处理**；本次他的判断本身正确，真正的缺口是"漏读题干里的『可能』二字，把不确定当确定"。
- 2026-07-31：**新增教学工具「时序表」**——他主动发问"时序表怎么写"（此前我要求过两次却从未讲过方法）。已给出方法论：状态变量占列 / 每行是一个时间点事件 / **定时器到期必须单独占一行** / 最后一列直接回答"我要验证什么" / **表要自洽，两列矛盾就是信号**。这是他第一个能自己验证代码的工具，后续节流、并发调度器、Promise 红绿灯全靠它。**教练教训：凡提出方法论要求，必须同时给方法，否则是要求不是教学。**
- 2026-07-31：**会话节奏（他本人要求）**——**下次起开场直接进主线，不做开场铺垫**；滚动复习与手写题穿插到主线告一段落之后，不占开场时间。
- 2026-07-31：**日志日期纪律（同型错误第二次）**——分包那次会话被误记为「07-29 第 2 次会话（同日续接）」，实际发生于 07-31（依 commit fa8b1ad @ 07-31 15:48），已拆分至 `sessions/2026-07-31.md`，progress.md 内相关 07-29 归因一并更正。**根因：用户说"继续"就默认同一天，但两次"收工→继续"之间实际隔了 2 天。硬性规则：每次写日志或追加会话前，先跑 `Get-Date`；"续接"≠ 同一天。**

- 2026-08-06：**Vue 模块内容全覆盖**（补完 Vue Router 导航守卫），下次直接做 5 题结业测验，过了即第二个 ✅。手写题**防抖 + LRU 双双闭卷一次成型升「面试可用」**，是清单里前两个可用题。
- 2026-08-06：**新增自查工具「删掉这句，哪条用例会红」**（变异测试的直觉版）——起因是他的防抖用例对 `timer = null` 完全瞎（删掉照样全绿）。此工具与「时序表」互补：**时序表验证"代码跑成什么样"，变异思路验证"用例够不够格"**。也可直接用于面试题"你怎么保证测试质量"。
- 2026-08-06：**⭐ 关键教学发现——"复述通过" ≠ "能迁移"**。`addRoute` 死循环那题，他此前对白名单、判据都能复述，但换成"资料没填完"的新场景立刻失效，把两个实例当成了规律。**今后每讲完一个机制，必须追加一道"用它判断一个没讲过的新场景"，这是检验抽象能力的唯一方式。**（本次追加后他独立推对了"没绑手机号"场景。）
- 2026-08-06：**教练纪律新增（他当场提出）——举例前必须自查例子是否真实成立**。本次我用"同时开两个浏览器标签导致 Pinia 状态污染"举例，**该例不成立**（不同 tab 是独立 JS 运行时、独立 store），已更正为"后台系统页内多页签 + `keep-alive` 缓存多实例"。**错误的例子比没有例子更糟——他会把它带进面试。**

- 2026-08-07：🎉 **模块 2「Vue」结业测验 4.5/5 通过，升 ✅——13 个模块中的第二个**（07-15 开荒 → 08-07 结业，跨 8 次会话）。**两个主场（Vue + 小程序）全部拿下，简历上最能打的两块已成形。**
- 2026-08-07：**⭐ "复述通过 ≠ 能迁移"的缺口正式修复**——结业测验 Q3（组合式函数传值）、Q4（多语言前缀 vs 密码过期，**两题答案相反**）都是临时编的新场景，他**全部靠规律推对**。**教学手法「讲完机制必追一道新场景迁移题」固化为常规动作**，它是目前唯一能区分"背下来了"和"想明白了"的工具。
- 2026-08-07：**路线转向 0 分区**。判断依据：深度已够（两个 ✅ 都是源码级），**当前最大风险是面太窄**——浏览器/网络/性能三块此前一题不会，任何一场面试都躲不开。本次已开荒浏览器缓存，后续按"边界清晰、单次可结"的顺序推：storage 三兄弟 → HTTPS/URL 全流程 → 性能指标。
- 2026-08-07：**新增训练项（针对表述精度）**——答完一个概念，追问"**用一句话把它和它最容易混的那个词区分开**"。起因是 `no-cache`/`no-store` 讲完十分钟即混用。他的老问题是"理解了但表述滑"，这个动作专治这一类。

- 2026-08-11：**✅ 上条训练项首战见效**——`no-cache`/`no-store` 隔 4 天闭卷一次答对，表述干净互斥。**该动作固化为常规**，下一个目标是 `HttpOnly`/`SameSite`（本次新暴露的同型混淆）。
- 2026-08-11：**⭐ 抽象迁移能力二次验证（跨模块）**——问"自动 reload 有什么风险"，他**自己搬来路由守卫那节的通用判据**「重定向后条件还成不成立」，判出无限刷新。判据建立于 Vue 守卫场景，用在了浏览器错误兜底场景，两者毫无表面关联。**继 08-07 结业测验后，抽象能力已可视为稳定资产。**
- 2026-08-11：**⚠️ 教练侧错误 + 已当场纠正**——我举例说 `<a target="_blank">` 会复制 sessionStorage，**该例在现代浏览器不成立**。查证：只有存在 **opener 关系**才复制；`<a target="_blank">` 自 **Chrome 88** 起隐式带 `noopener`（防 tabnabbing），**Chrome 89** 起 noopener 窗口不再克隆 sessionStorage。会复制的是 `window.open(url)` / `rel="opener"` / 浏览器"复制标签页"。**这是 08-06 定的「举例前自查」纪律第二次触发；版本相关例子今后一律先查官方源再讲。**
- 2026-08-11：**下阶段主线锁定 🔴 双 Token + 无感刷新 + 请求队列**（模块 11「场景题」开荒，目前 ❌）。本次讲 token 存储时已自然铺完前半段（静默刷新 + 守卫 `authReadyPromise` 冲突），下次直接进核心：**并发 401 只刷新一次 + 请求重放 + 分页错乱**。这条线同时打通模块 6（存储/安全）、模块 2（守卫）、模块 11（场景题），性价比最高。

- 2026-08-12：**进入第 5 周。🔴 双 Token 全链路四段打通，模块 11「场景题」从 ❌ 升 ⚠️**（该模块第一块）。**⚠️ 注意：拦截器代码是"带写"的不是闭卷，未来须安排一次闭卷默写才算数。**
- 2026-08-12：**⭐ 跨会话知识串联（他自主完成）**——问"重复 5 次 refresh 是'做不到'还是'只是麻烦'"，他**用昨天刚学的 refresh token rotation 推出杀手后果**："旧 token 失效 → 剩下 4 个被当成盗用 → 用户被强制登出"。**昨天学的机制、今天用来判新问题，且无提示。**这是继 08-07（结业测验新场景）、08-11（守卫判据跨用到错误兜底）之后第三次，**抽象与串联已可视为稳定资产，不再列为薄弱项**。
- 2026-08-12：**新增可迁移规律 ×3**（已入话术库）——① **「CSRF 的根源不是攻击者能发请求，而是浏览器自动替他鉴权」**，判据是"凭证由浏览器自动附加还是 JS 主动附加"；② **「Promise 本身就是队列」**，手写 `waiters` 回调数组其实是在手写 Promise；③ **「凡是'后发应覆盖先发'的场景都要做竞态控制」**（搜索联想/翻页/Tab 切换/筛选变更同类）。
- 2026-08-12：**「能用结构解决的，别靠条件判断去猜」第 3 次出现并已固化**——`_retry` 限次数 vs 猜 401 原因、裸实例发 refresh vs 判 URL 打补丁、`Symbol` vs 字符串前缀。**与他既有的"做不到 vs 只是麻烦"自查合并为同一条判据**：结构性防御 = "做不到"级，语义猜测 = "只是麻烦"级。
- 2026-08-17：**🔴 输入 URL 全流程开荒，渲染流水线从 0 到能扛追问**（模块 6 主体推进，同时给模块 7「性能优化」打下地基，该模块由 ❌ 升 ⚠️）。**七站骨架已成形，剩 TLS/TCP/DNS 三站待深挖。**
- 2026-08-17：**⭐ 教学策略新增「地图题的答法」**——「输入 URL」这类题考的是**版图有没有洞**，面试官会挑任意一站深挖。正确打法是**先给 1 分钟骨架、每站用词精准但点到为止，把停在哪一站的选择权交给面试官**（让他停在你最强的那站），而不是一头扎进 DNS 细节。**这个策略可复用到所有"讲讲 XX 流程"型问题。**
- 2026-08-17：**⚠️ 教练自查纪律第 3 次触发并生效**——用户连续对话、无日期提示，教练默认本次是"08-12 续接"，**写日志前跑 `Get-Date` 才发现实际是 08-17（隔 5 天）**，已单独建文件。**代价：因误判为同日，按"同天不复习"跳过了开场滚动复习，08-12 遗留的 3 题顺延。** → **规则升级：`Get-Date` 从"写日志前跑"提前到「每次会话开场前跑」。**
- 2026-08-12：**教练手法固化（移动端场景）**——他说"不方便写，你带我一步一步写"。改为**「每步只问一个能用嘴回答的问题，他答完我写出那一段代码」**：状态该存什么 → 守卫②不写会怎样 → 删掉 `finally` 置空会怎样 → 删掉更新 header 会怎样 → 验收时序表。**效果与闭卷手写接近，且不受设备限制**，作为"他不方便打字"时的标准做法。

## 学习特征观察（教练备忘）
- **优势**：① 理解力强，给框架就能推演；② **靠复述能把表述钉死**（computed 两天从"记反"到干净复述通过就是证据）；③ **学习主动性强**——本次 Signals/版本号机制/副作用三处都是他自己意识到"不懂"主动发问。
- **优势补充（07-24）**：**如实反馈、不硬撑**——主动说出"AI 这块只能吸收一点点、名词听不懂"，这是高质量的学习姿态，让教练能及时纠偏。
- **短板**：① **记忆精度**——易把概念记反（all/allSettled）；② **归因易错**——倾向于抓一个似是而非的解释就停止思考；③ 知识串联本在改善（methods 第 4 条已能联系到非响应式变量题）；④ **（07-24 新增）代码不验证运行时路径**——结构看着对就交卷，两次被明确要求"跑时序表"都未执行。
- **⚠️ 归因偏差已确认为稳定模式**（07-24 单次会话内复现 4 次）：TTFT 答"用户感觉快"、防抖第 2 版照抄提示未问不变量、RAG Q2 答"知识库没数据"、节流跳过时序验证。**不是偶发失误，是思维习惯。**
  - **07-29 明显收敛**：全场仅 1 次（登录 Q2"openid 容易被截获"），且一句话即可拉回。**更关键的是 setData 定位题他自发答了"先测不先改"——"先测量再下结论"的纪律首次自己出现。** 对策有效，继续。
- **07-29~31 新增优势：知识串联出现实质突破（长期短板）**——① 07-29 主动做跨框架类比"WXML 是不是编译成渲染函数拿到虚拟 DOM，类似 Vue？"；② 07-31 **自主推导出机制的存在**（分享链接空降分包"应该只下分包资源"→ 独立分包的设计动机，是他自己说出来的）。值得持续鼓励。
- **07-31：话术训练首次开展**（包体积题）——结构完整、**自发讲权衡**（长期训练目标达成）。这条能力已可用，后续重点转向"权衡要跟缓解"。
- **⚠️ 项目诚信边界（07-29 已明确约定）**：他项目里的长列表卡顿**当时并未优化**（误判为"小程序本身就卡"）。**严禁包装成"我优化了"**——面试官追两层必露馅，诚信问题直接出局。已产出诚实版表述（复盘型，落点在"没有量化就下结论"的方法论成长），并给出升级路径：**真回去优化并拿到前后数据**，才能说"我做了"。
- **对策**：多用「复述纠正」；**每个结论都追问一句"为什么"逼出正确归因**；**每接受一条修改建议，先问"这条建议保护的是什么不变量"**（07-24 新增，针对照抄提示改出新错的模式）；**写完代码必须自跑时序表**（硬要求）；持续鼓励主动提问的习惯。
- **教学节奏（07-24 起）**：单次只讲 1 个知识点 → 立刻复述 → 再追问，避免大信息量倾倒；新模块优先选他有经验挂钩的（小程序 > Vue > 浏览器八股 > AI 后端概念）。
