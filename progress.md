# 复习进度总览
> 最后更新：2026-08-05 ｜ 当前周次：第 4 周 ｜ 目标面试时间：未定（暂无邀约，碎片化备战）

## 模块掌握度
| 模块 | 状态 | 备注 |
|---|---|---|
| 1. JS 核心 | ⚠️ | Promise 四大静态方法 ✅；rAF 归属与自调度 ✅；副作用概念已讲；闭包/原型链/this/宏微任务 未测 |
| 2. Vue | ⚠️（**内容接近全覆盖，下阶段可冲第二个 ✅**） | 响应式主线：ref/.value + reactive 局限 ✅、computed（惰性+缓存+版本号计数+双向链表）✅、computed vs methods 四判据 ✅、props 解构（3.5+）✅；`watch` 的 `flush` 三档 + 为什么默认 `pre` + `sync` 的 glitch ✅、`nextTick` 原理（异步批量+调度队列+微任务+Vue2 四级降级 vs Vue3 纯 Promise）✅；**08-05 第 2 段新增**：`key` 与 diff（就地复用 / index 做 key 两层后果 / 同层比较 O(n³)→O(n)）✅、`keep-alive`（cache+keys / `onActivated` / `max` 用 LRU / 内存权衡）✅、组件通信分类框架 + **`defineModel()` 3.4 转正** + 为什么移除 EventBus ✅、`provide/inject` 深入（传容器不传值 / 同步调用 / `readonly`+修改方法 / `Symbol`+`InjectionKey`）✅、Vuex vs Pinia 三条设计动机 ✅、⭐ **统一规律「响应性属于容器不属于值」**（四处同根因收拢）✅；**待开**：Vue Router 路由守卫，之后可做结业测验 |
| 3. React | ❌ | 未开始 |
| 4. TypeScript | ❌ | 未开始 |
| 5. 小程序与跨端 | **✅ 掌握** 🎉 | **08-05 结业测验 5/5 全过，13 个模块中第一个 ✅**（07-29 开荒 → 08-05 结业，共 5 次会话）。覆盖：双线程架构、setData 成本模型与三方向、WXS、性能定位（真机/回调打点/stringify）、登录鉴权六步 + 三铁律 + OAuth 本质、手机号新方案、分包三规则 + `preloadRule` + 独立分包（含 `getApp()` 坑）、长列表（流量 vs 存量 / 数据路径追加 / 虚拟列表 / 前缀表+二分 / recycle-view）、跨端选型、移动端适配（rpx≡vw / 1px 根因 / 安全区 `wx.getWindowInfo`）。话术库已收录 7 条。**后续只做长期滚动抽查** |
| 6. CSS 与浏览器 | ❌ | 强/协商缓存不会 |
| 7. 性能优化 | ❌ | 虚拟列表、Web 指标均不会 |
| 8. 网络与通信 | ❌ | HTTPS、SSE 均不会 |
| 9. AI 应用前端 | ⚠️ | **SSE 全链路结业**：选型✅、fetch+ReadableStream+AbortController✅、六连接坑✅、断点续传(Last-Event-ID+服务器缓存)✅、指数退避✅、流式渲染(chunk边界/TextDecoder/Markdown截断/rAF批量/自动滚动)✅、DOMPurify防注入✅；TTFT⚠️(量法对、指标设计意义答浅)；**RAG ⚠️ 已下调为"前端最小可用版"**(链路+前端三角色+权限隔离动机，已入话术库；切块/Hybrid/评估仅"听过一遍"不作记忆要求)；**待开**：Function Calling、MCP、Agent loop、Prompt 工程 |
| 10. 工程化 | ❌ | 未开始 |
| 11. 场景题 | ❌ | 未开始 |
| 12. 手写与算法 | ⚠️ | 防抖 **接近可用**（08-05 闭卷三轮至正确，差最后一遍）、节流（时间戳版对，缺 trailing）；⭐ **「时序表」验证法 08-05 首次自主发挥作用**（bug 由他自己跑表抓到）——所有定时器/异步/状态机手写题的通用自查工具；其余 7 题未动 |
| 13. 非技术面 | ❌ | 未开始 |

## 薄弱点清单（待复查）
> 07-24 已清理：`props 解构归因`、`副作用`、`嵌套 computed 拉模型`、`AI 断点续传服务器端`、`代码块流式难点`、`SSE 实战细节`。
> **07-31 已清理**：`setData 性能定位的具体工具与打点`（连续两次未过，本次通过）、`"降低凭证价值"的安全设计通则`、`TTFT 的指标设计意义`、`写完代码必须自跑时序表`（已教方法并首次跑对 2.5/3）—— 全部移出。

**手写题（最高优先级）**
- [ ] **防抖 immediate 的执行时序** —— 08-05 闭卷三轮至正确，**差最后一次干净的一遍**。⭐ 错误性质已改变：前五版全是同一条不变量（冷却期未建立），08-05 第 2 版错的是**初值写法**（更表层、可一次根除）。**下次开场一遍闭卷，写干净即升「面试可用」**。只盯两点：① 设定时器**无条件执行**，不进 `if/else` 分支；② 初值与判断方式配套（首次暴露：07-24，状态：差最后一遍）
- [ ] **节流 trailing 分支 + 时间戳版/定时器版的行为差异** —— 只写了时间戳版且 `timer` 是死代码；未答"首次必执行、末次必丢失 vs 反之"（首次暴露：07-24，状态：待补完，**补写时必须附时序表**）

**AI 模块**
- [ ] **向量检索对专有名词/型号的天然局限** —— 07-24 Q2 答错（错归因为"知识库没数据"）。要点：embedding 编码语义，型号几乎无语义，`XR-2000` 与 `XR-3000` 向量几乎重合 → 解法 **Hybrid Search（BM25+向量+RRF）** + 元数据过滤（首次暴露：07-24，状态：待复查）
- [ ] **RAG 效果优化的第一步是建评估集** —— 07-24 有分层意识但缺"量化基线"概念，也漏了数据层（解析就错了）（首次暴露：07-24，状态：待复查）

**跨模块的稳定缺口（最高优先级）**
- [ ] **⚠️ 「照建议改，但改过头 / 改错位置」——已 3 次复发** ① 07-24 防抖第 2 版（在 `callNow` 判断前调 cancel）；② 07-31 防抖第 1 版（同一 bug 换变量名）；③ 07-31 教练说"`if (!timer)` 冗余、直接写赋值"，他把 `if` 和 `setTimeout` **一起删了**，冷却期消失。**根因：执行建议而不追问建议保护的是什么。对策（双向）——他：每接受一条建议先问"这条保护的是哪条不变量"、删任何一行前先问"这行在维持什么"；教练：给建议时必须说清边界（改哪行、保留什么）。**（首次暴露：07-24，状态：高频待观察）
- [ ] **讲权衡不跟缓解手段** —— 07-31 话术训练说"进二级页面会慢，这是权衡后的决定"，听起来像接受问题而非解决问题。**通用话术规则：讲权衡永远要跟一句"我怎么补的"**（此处即 `preloadRule` / 独立分包）（首次暴露：07-31，状态：待复查）
- [ ] **跑时序表：值必须从代码字面读，不能从"我以为"读** —— 07-31 填出自相矛盾的行未察觉；**08-05 升级版**：把 `let timer;`（`undefined`）的初值填成了 `null`，掩盖了 `timer === null` 恒假这个 bug。铁律：**从预期读值，表就只是想法的复印件，不是行为的显微镜**（首次暴露：07-31，08-05 复发升级，状态：待复查）
- [ ] **⚠️ 不会自己设计测试用例（08-05 定型）** —— 防抖和 LRU 两题，**bug 都是他跑教练给的用例才发现的**，自己交卷时从不自造用例。已给方法：**每个 `if` 分支至少一条用例 + 边界各一条**；LRU 的两个经典陷阱（"put 已存在的 key 不该淘汰"、"get 是否影响淘汰顺序"）他都漏了。**训练方式：下道手写题要求先列用例清单再写代码，教练只验收不代劳。**（首次暴露：08-05，状态：待训练）
- [ ] **选型题的理由要区分"做不到"和"不好做"** —— 08-05 表单组件库题答"props 层级不确定传不下去"（实为传得下去，只是 drilling；**真正的杀手理由是"中间层是用户写的组件，组件库无权要求它们透传"**）；答"Pinia 全局不适合"（**精确理由是 store 单例、多个 Form 实例互相污染**）。满分框架：**三者区别本质是作用域——props 一层 / provide 一棵子树 / Pinia 全局**（首次暴露：08-05，状态：待复查）
- [ ] **`undefined` vs `null` 与 `===` 的配套** —— `let timer;` 是 `undefined`，`undefined === null` 为 **false**。**初值写法必须和判断方式匹配**：用 `!timer` 就不必纠结初值；用 `=== null` 就必须 `let timer = null`（首次暴露：08-05，状态：待复查）

**Vue**
- [ ] **版本号计数 / 双向链表机制** —— 07-16 讲透但未复述，可让其口述"读 computed.value 的三步流程：globalVersion → 逐依赖版本对比 → 重算"（首次暴露：07-16，状态：待复查）
- [ ] ref 自动解包只在**顶层**生效（`{ count: ref(0) }` 在模板里不解包）—— 已讲未独立抽查（首次暴露：07-15，状态：待抽查）

**未学（0 分区，需排进日程而非"复查"）**
- [ ] 强缓存 vs 协商缓存的区别与响应头（首次暴露：07-15）
- [ ] HTTPS 握手：非对称+对称加密各用在哪步、为什么（首次暴露：07-15）
- [x] ~~不定高虚拟列表：前缀高度表 + 二分查找~~ —— **08-03 已讲透**（"为什么能二分"待复查，见上）
- [ ] Web 性能指标 LCP/INP/CLS 的定义与优化（首次暴露：07-15）

## 手写题清单
| 题目 | 状态 | 缺陷备注 |
|---|---|---|
| 防抖（含 immediate） | **接近可用（差最后一遍）** | 08-05 闭卷三轮至完全正确。⭐ **关键进展：bug 首次由他自己跑时序表抓到**（"执行了 3 次，正确是 1 次"），结构也是他据"不变量"概念自行改对的。残留两处需最后一遍验证：`if/else` 把"设定时器"耦合进分支；`let timer;`(undefined) 配 `=== null` 致 callNow 恒假 |
| 节流 | **有缺陷（待补完）** | 07-24：时间戳版核心公式 ✅、this/args ✅、复用同一 now ✅。缺陷：`timer` 声明后从未赋值（死代码）、**缺 trailing → 末次触发永久丢失**；未写定时器版、未答两版行为差异、未跑时序表 |
| 深拷贝 | 未写 | |
| Promise.all | 未写 | 原理已讲清（含非 Promise 值自动包装），可优先手写巩固 |
| LRU 缓存 | **基本可用（下次随手一写即过）** | 08-05 三轮至正确。⭐ **两个最难点自己想到**：选 `Map`（唯一同时具备"记插入顺序 + O(1) 增删查"）、`delete` 后 `set` 回去实现"移到末尾"。三个 bug：`this.map.keys[0]`（keys 是方法且返回迭代器 → 应为 `keys().next().value`，原写法致淘汰完全失效）、`get` 未命中没 `return -1`、**容量检查放在 `has→delete` 之前致"更新已存在 key"时误淘汰**。串联：`keep-alive` 的 `max` 内部就是 LRU |
| 超时重试 | 未写 | |
| 数组转树 | 未写 | |
| 并发数限制的异步调度器 | 未写 | |
| Promise 红绿灯 | 未写 | |

## 模拟面试记录
| 日期 | 轮次 | 总分 | 最大短板 |
|---|---|---|---|
| —— | 尚未开始 | —— | —— |

## 路线调整记录
- 2026-07-15：碎片化时间备战、暂无面试邀约 → 六周路线改为「**每次一个点讲透**」的弹性节奏；优先级：先补 JS/Vue 精度 → 尽早前置 AI 模块（差异化）。
- 2026-07-15：新增 `answers.md` 面试话术库；CLAUDE.md 第五节加第 9 条版本时效性原则、第六节改为多文件自主维护。
- 2026-07-16：会话日志改为 `sessions/` 目录、按日期一天一文件；取消汇总索引，全局状态只由 progress.md 负责。
- 2026-07-17：新增 README；约定每次会话结束自动 git commit+push 到 web-interview 仓库（已写入 CLAUDE.md 第六节）。〔07-29 依据 commit de9319a @ 07-17 18:00 更正原记录的 07-21〕
- 2026-07-22：SSE 全链路结业（对应 `sessions/2026-07-21.md`，commit 0852ce0 @ 07-22 11:35），进入第 2 周。
- 2026-07-24：**AI 模块深度下调**。RAG 讲超纲（BM25/RRF/HNSW/cross-encoder 属后端与算法词汇，前端岗不考此深度），学生反馈"吸收有限、名词听不懂"。根因判断：**AI 后端知识缺乏"挂钩点"**——Vue 学得快因为天天写、SSE 学得下去因为落在前端能摸到的地方（fetch/流/渲染），而向量/切块/召回全是悬空名词。→ RAG 降为"前端最小可用版"，Function Calling/MCP 后续同样按"前端视角"讲，不深入后端实现。
- 2026-07-24：**下阶段主线切到 🏠 小程序模块**（原计划的 AI 后续内容让位）。理由：他有真实项目经验（挂钩点最多、最省力）、是简历亮点却从未打磨、能反向带出通用原理（双线程≈进程隔离、setData≈跨线程通信成本）。
- 2026-07-24：**教学纪律调整**——改为"每次只讲 1 个点，讲完立刻让他复述"，不再一次性倾倒大信息量（他的强项是靠复述钉死表述，弱项是听完即滑走）。
- 2026-07-29：**"挂钩点"假设被数据证实**——同期对比：**RAG 复述通过率 1/3，小程序 8/9**。唯一差别是有没有真实经验做附着点。→ 确认模块优先级：**有经验挂钩的优先**（小程序 → Vue → 浏览器/网络八股 → 场景题），AI 只保留前端视角层，不碰后端实现细节。
- 2026-07-31：**提问纪律（他本人提议，采纳）**——① **一次只问 1 题**，答完追问、追问完再下一题；不再一次抛 3 题（一次一题才能形成追问链，也才贴近真实面试节奏）。② **同一天刚讲的内容不做滚动复习**（测的是短期记忆，无留存价值），滚动复习只针对**上次及更早**的内容；当天未答出的题按"欠账"留在薄弱点清单，下次开场补。
- 2026-07-29：**日志日期纪律**——发现 `sessions/2026-07-22.md` 实为 07-24 的会话（依 commit cae98e9 @ 07-24 11:54），已 `git mv` 更正为 `sessions/2026-07-24.md` 并同步文内日期；progress.md 中相关 07-22 引用一并改为 07-24，README 条目由 07-21 更正为 07-17。**今后写日志前必须先核对系统日期，不靠推测。**
- 2026-08-05：🎉 **模块 5「小程序与跨端」结业测验 5/5 全过，正式升 ✅——13 个模块中的第一个 ✅**（07-29 开荒 → 08-05 结业，5 次会话）。后续只做长期滚动抽查。**下阶段主线转 Vue**（他的主场、挂钩点最多、响应式主线已闭环，补完 watch/nextTick/diff/keep-alive 可冲第二个 ✅）。
- 2026-08-05（第 2 段）：**⭐ 知识串联出现最强一次表现**——Pinia 解构题他**没学过 `storeToRefs` 却自行推出"会有语法糖"**，归因为"解构切断响应链，类似 reactive 解构"。据此把四处同根因收拢成一条可朗读的规律：**「响应性属于容器，不属于值」**（reactive 解构 / watch 传值 / provide 传 `.value` / store 解构，解药统一为"包回容器"或"延迟取值"）。**这条已入话术库，是 Vue 模块的最高层抽象。**
- 2026-08-05：**教练手法调整（效果显著，固化）**——他说"××你帮我做"时，改为**"我做第一步，其余交还给他"**。本次防抖时序表用此法：教练只填 t=0 一行，t=50/t=80 交他填，**结果 bug 是他自己抓到的**（"执行了 3 次，正确是 1 次，我又写错了"）。比直接代劳有效得多，也比硬性拒绝更不伤积极性。
- 2026-08-03：**🏠 小程序模块内容全覆盖（5/5）**，用时 4 次会话。下次开场做 5 题结业测验，通过则成为 13 个模块中的**第一个 ✅**。之后建议顺序：Vue 继续（挂钩点最多）> 浏览器强/协商缓存（0 分区、20 分钟可拿一个 ✅）。
- 2026-08-03：**⭐ 他首次对教练的判断提出反驳且论证有价值**（uni-app 提前上车题：主张"迁移成本高，提前付值得"）。**这是好现象——面试中面试官会 challenge，能守住或调整立场是加分项。** 教练侧记录：**他反驳时先认真评估论证是否成立，不要一律当错处理**；本次他的判断本身正确，真正的缺口是"漏读题干里的『可能』二字，把不确定当确定"。
- 2026-07-31：**新增教学工具「时序表」**——他主动发问"时序表怎么写"（此前我要求过两次却从未讲过方法）。已给出方法论：状态变量占列 / 每行是一个时间点事件 / **定时器到期必须单独占一行** / 最后一列直接回答"我要验证什么" / **表要自洽，两列矛盾就是信号**。这是他第一个能自己验证代码的工具，后续节流、并发调度器、Promise 红绿灯全靠它。**教练教训：凡提出方法论要求，必须同时给方法，否则是要求不是教学。**
- 2026-07-31：**会话节奏（他本人要求）**——**下次起开场直接进主线，不做开场铺垫**；滚动复习与手写题穿插到主线告一段落之后，不占开场时间。
- 2026-07-31：**日志日期纪律（同型错误第二次）**——分包那次会话被误记为「07-29 第 2 次会话（同日续接）」，实际发生于 07-31（依 commit fa8b1ad @ 07-31 15:48），已拆分至 `sessions/2026-07-31.md`，progress.md 内相关 07-29 归因一并更正。**根因：用户说"继续"就默认同一天，但两次"收工→继续"之间实际隔了 2 天。硬性规则：每次写日志或追加会话前，先跑 `Get-Date`；"续接"≠ 同一天。**

## 学习特征观察（教练备忘）
- **优势**：① 理解力强，给框架就能推演；② **靠复述能把表述钉死**（computed 两天从"记反"到干净复述通过就是证据）；③ **学习主动性强**——本次 Signals/版本号机制/副作用三处都是他自己意识到"不懂"主动发问。
- **优势补充（07-24）**：**如实反馈、不硬撑**——主动说出"AI 这块只能吸收一点点、名词听不懂"，这是高质量的学习姿态，让教练能及时纠偏。
- **短板**：① **记忆精度**——易把概念记反（all/allSettled）；② **归因易错**——倾向于抓一个似是而非的解释就停止思考；③ 知识串联本在改善（methods 第 4 条已能联系到非响应式变量题）；④ **（07-24 新增）代码不验证运行时路径**——结构看着对就交卷，两次被明确要求"跑时序表"都未执行。
- **⚠️ 归因偏差已确认为稳定模式**（07-24 单次会话内复现 4 次）：TTFT 答"用户感觉快"、防抖第 2 版照抄提示未问不变量、RAG Q2 答"知识库没数据"、节流跳过时序验证。**不是偶发失误，是思维习惯。**
  - **07-29 明显收敛**：全场仅 1 次（登录 Q2"openid 容易被截获"），且一句话即可拉回。**更关键的是 setData 定位题他自发答了"先测不先改"——"先测量再下结论"的纪律首次自己出现。** 对策有效，继续。
- **07-29~31 新增优势：知识串联出现实质突破（长期短板）**——① 07-29 主动做跨框架类比"WXML 是不是编译成渲染函数拿到虚拟 DOM，类似 Vue？"；② 07-31 **自主推导出机制的存在**（分享链接空降分包"应该只下分包资源"→ 独立分包的设计动机，是他自己说出来的）。值得持续鼓励。
- **07-31：话术训练首次开展**（包体积题）——结构完整、**自发讲权衡**（长期训练目标达成）。这条能力已可用，后续重点转向"权衡要跟缓解"。
- **⚠️ 项目诚信边界（07-29 已明确约定）**：他项目里的长列表卡顿**当时并未优化**（误判为"小程序本身就卡"）。**严禁包装成"我优化了"**——面试官追两层必露馅，诚信问题直接出局。已产出诚实版表述（复盘型，落点在"没有量化就下结论"的方法论成长），并给出升级路径：**真回去优化并拿到前后数据**，才能说"我做了"。
- **对策**：多用「复述纠正」；**每个结论都追问一句"为什么"逼出正确归因**；**每接受一条修改建议，先问"这条建议保护的是什么不变量"**（07-24 新增，针对照抄提示改出新错的模式）；**写完代码必须自跑时序表**（硬要求）；持续鼓励主动提问的习惯。
- **教学节奏（07-24 起）**：单次只讲 1 个知识点 → 立刻复述 → 再追问，避免大信息量倾倒；新模块优先选他有经验挂钩的（小程序 > Vue > 浏览器八股 > AI 后端概念）。
