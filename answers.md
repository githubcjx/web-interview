# 面试话术库
> 用途：面试前朗读。最后更新：2026-07-21 ｜ 已收录 12 条（JS 核心 3 ｜ Vue 5 ｜ AI 应用前端 4）

---

## 1. JS 核心

### Q：`Promise.all`、`allSettled`、`race`、`any` 有什么区别？
- **一句话结论**：区别只在两件事——**对失败的态度**，和**看不看成败**。
- **面试口语版话术**：
  > 「这四个的区别，我习惯从两组对比来记。
  > 第一组是 **`all` 和 `allSettled`**，区别在**对失败的态度**：`all` 是一票否决，只要有一个 reject 它就立刻 reject，不等其他人——适合『必须全部成功』的场景，比如页面依赖三个接口，缺一个就没法渲染；而 `allSettled` 是等所有人都有结果，成败都算，它**永远不会 reject**，返回的是带 status 的结果数组——适合『允许部分失败』的场景，比如批量上报，成功的照常用、失败的单独处理。
  > 第二组是 **`race` 和 `any`**，区别在**看不看成败**：`race` 是第一个**结束**的说了算，哪怕它是失败结束的——所以常用来做超时控制；`any` 是第一个**成功**的说了算，失败的不算——适合多个备用源里取最快成功的那个。要补一句，`any` 在全部失败时抛的不是普通 error，是 **`AggregateError`**，里面的 `.errors` 装着所有失败原因。」
- **高频追问要点**：
  - **race 怎么做超时？** → `Promise.race([fetch(url), new Promise((_, reject) => setTimeout(() => reject(new Error('超时')), 3000))])`。注意超时哨兵要 **reject 而非 resolve**，这样外层 catch 能和成功区分开。
  - **race 超时后请求真的取消了吗？** → **没有**，只是你不等它了，请求还在后台跑。真正取消要配 **`AbortController`**（同一套 API 也用于中止 SSE 流式请求）。
  - **`all` 传入非 Promise 值（如数字 3）会怎样？** → 内部用 `Promise.resolve()` 包一层，自动变成已成功的 Promise，结果里就是 3。
- **翻车点**：把 `all` 和 `allSettled` 记反（`all` = 一票否决，`allSettled` = 无否决权）；漏答 `any` 的 `AggregateError`。

### Q：`requestAnimationFrame` 属于宏任务还是微任务？和 `setTimeout` 差在哪？
- **一句话结论**：都不属于，它是渲染流水线调度的**独立一类回调**。
- **面试口语版话术**：
  > 「rAF **既不是宏任务也不是微任务**，它不在这两个队列里，而是由**渲染流程**调度的独立回调——执行时机是在**每一帧重新渲染之前、微任务清空之后**。一帧的顺序大致是：执行一个宏任务 → 清空所有微任务 → 到了渲染时机就跑 rAF 回调 → 然后样式计算、布局、绘制。
  > 所以做动画要用 rAF 而不是 setTimeout：**setTimeout 属于宏任务，触发时机和渲染是解耦的**，一帧里可能跑好几次、也可能掉帧，动画会卡会抖；而 **rAF 和屏幕刷新率对齐**，保证每次重绘前正好执行一次，动画才丝滑。」
- **高频追问要点**：
  - **rAF 为什么要每帧手动约下一帧，不像 setInterval 设一次就循环？** → 三个好处：① 停止动画只要不再约下一帧，很干净；② **页面切到后台/不可见时浏览器自动暂停 rAF**，省电省 CPU，而 setInterval 在后台还傻跑；③ **不会任务堆积**——rAF 跟着渲染节奏走，setInterval 若一帧没处理完回调会积压，导致爆发式执行和跳帧。
- **翻车点**：说它是宏任务（最常见错误）；只答出"自由控制"而漏掉后台自动暂停和不堆积。

### Q：什么是"副作用（side effect）"？前端里指哪些操作？
- **一句话结论**：函数除了"根据入参算返回值"之外，对外部世界产生的任何影响，就是副作用。
- **面试口语版话术**：
  > 「副作用就是——一个函数除了『根据输入算出返回值』之外，还**对外部世界产生了影响**。纯函数像个计算器，相同输入永远相同输出、不碰外面任何东西；一旦它改了外部变量、发了请求、操作了 DOM、读写了 localStorage、设了定时器、打了 log，就是有副作用。
  > 判断口诀很简单：**这个函数跑完，除了吐一个返回值，有没有在外面留下痕迹、或依赖了外部会变的东西？有就是有副作用。**
  > 这也解释了**为什么 Vue 的 computed getter 必须纯**——computed 是惰性的、带缓存的、执行次数不可控，你在里面发请求，可能因为命中缓存永远不发、也可能发很多次。所以约定是：**computed 只做纯计算（what），副作用交给 watch/watchEffect/methods（do）。**」
- **高频追问要点**：
  - **哪些算副作用？** → 改外部状态、网络请求、操作 DOM、定时器、读写 localStorage/cookie、console/alert、路由跳转、`Math.random()`/`Date.now()`（不可预测）。
  - **和 React 的关系？** → React 的 `useEffect` 名字直接就叫 effect，管的就是"渲染之外的副作用"（请求、订阅、手动改 DOM），同一个概念。
- **翻车点**：把"副作用"狭义理解成只有"发请求"；忘了"修改传入的对象/参数"也是副作用。

---

## 2. Vue

### Q：`ref` 为什么要 `.value`？`reactive` 有什么局限？
- **一句话结论**：`.value` 是 **Proxy 代理不了原始值**这个语言限制的直接后果。
- **面试口语版话术**：
  > 「`.value` 的存在，本质是 **Proxy 无法代理原始值**这个语言限制的直接后果。你想，`let count = 0; count++`，对一个原始值变量的读写，**JS 语言层面根本没有可拦截的入口**——Proxy 拦的是『对象的属性操作』，而 count 压根不是对象、也没有属性。
  > 所以 Vue 只能用一个对象把值包起来、暴露成 `.value` 属性，这样『读』和『写』就变成了属性访问，才有 getter/setter 可以挂依赖收集和派发更新。所以**ref 内部不是 Proxy，是 class 加访问器**。
  > 而 reactive 接收的本来就是对象，Proxy 直接怼上去，属性访问天然可拦截，自然不需要 `.value`。但它有代价：**只能用于对象**、**解构会丢响应性**、**整体重新赋值也会丢**，得靠 `toRefs` 兜底。
  > 所以我的实践选择是**优先用 ref**——用一点 `.value` 的书写负担，换来引用稳定、不怕解构、不怕重新赋值，团队协作的心智成本更低。」
- **高频追问要点**：
  - **ref 是用 Proxy 实现的吗？** → **不是**。原始值的 ref 是 `class RefImpl` + `get value()/set value()` 访问器。
  - **`ref({a:1})` 传对象呢？** → 内部调 `reactive()` 把对象包成 Proxy 存进 `_value`，所以读 `.value` 拿到的是 **Proxy**。
  - **reactive 为什么不能解构？** → 解构把 Proxy 属性上的**原始值拷出来**了，从此和 Proxy 再无关系。解药是 `toRefs`，把每个属性包成持有源对象引用的 ref。
  - **为什么模板里不用写 `.value`？** → 叫**自动解包（unwrap）**，不叫解构。模板要经过**编译**，Vue 能在生成渲染函数时对 setup 上下文做 `unref()`；而你在 script 里写的任意 JS，Vue 拦不住普通变量的读写（还是同一个根因）。
  - **解包有什么边界？** → **只在顶层生效**。`const obj = { count: ref(0) }`，模板里 `{{ obj.count }}` **不会解包**，得写 `{{ obj.count.value }}`。
- **翻车点**：说"ref 是用 Proxy 实现的"；把"解包"说成"解构"。

### Q：Vue 里 `props` 能解构吗？为什么 `reactive` 不能而它能？（版本敏感 ⚠️）
- **一句话结论**：**Vue 3.5+ 可以解构**，靠的是**编译时改写**，不是运行时。
- **面试口语版话术**：
  > 「这个要分版本说。**3.4 及以下不能解构**，解构出来就是个永不改变的常量。但 **Vue 3.5 起，Reactive Props Destructure 已经稳定并默认开启**，可以放心解构。
  > 它能做到、而 reactive 做不到，关键区别在于：**props 靠的是编译时的魔法**——编译器会把你解构出来的变量，在**所有引用处自动改写回 `props.xxx`**，所以响应性从来没断过，你写的是 `count`，编译产物是 `props.count`。而 **reactive 的解构是纯运行时的普通 JS 解构**，编译器不介入，值一拷出来链路就断了。
  > 这也体现了 Vue 的一贯思路：**能在编译时解决的，就不留给运行时。**」
- **高频追问要点**：
  - **这个编译魔法的边界在哪？** → 注意别把解构出的 prop **当"值"传给 `watch`**：`watch(count, cb)` 编译后是 `watch(props.count, cb)`，`props.count` 在那一行**当场求值成一个死值（数字）**，监听不到；要写成 `watch(() => count, cb)`（编译后 `watch(() => props.count, cb)`），传一个 **getter 延后取值**才能追踪。
  - **注意：这不是"编译器改写不了函数内部"** —— 编译器**函数体内照样改写**（`function f(){ console.log(count) }` 里的 count 也会变 `props.count`）。真正原因是 **值 vs getter 的求值时机**，且这本就是 **watch 的通用规则：第一个参数只认 ref / reactive / getter，不能传普通值**（对普通 `reactive` 属性也一样：`watch(obj.count)` ❌ → `watch(() => obj.count)` ✅）。
- **翻车点**：① 只答"props 不能解构"——**3.4 旧答案**，2026 年会被认为版本停两年前；② 把 watch 失效归因为"改写不了回调内部"——错，是求值时机（值 vs getter）。

### Q：`computed` 的缓存和惰性求值是怎么实现的？（版本敏感 ⚠️）
- **一句话结论**：依赖变化时**不重算，只标记失效**；真正计算推迟到下次读 `.value`。
- **面试口语版话术**：
  > 「computed 的核心是**惰性求值加缓存**。最关键的一点是：**依赖变化时它不会立即重算，只是把自己标记为失效**，真正的计算推迟到下次有人读 `.value` 的那一刻。
  > 这样设计有两个理由：一是**可能根本没人读**——比如这个 computed 被 v-if 干掉了，立即算就是纯浪费；二是**能合并中间状态**——一次操作连改五个依赖，立即算要算五次，惰性只在最后读的时候算一次。所以惰性求值的本质价值，是把计算推迟到真正需要的那一刻，顺手合并掉中间所有无效计算。
  > 实现上 **Vue 3.4 重构过**：早期版本靠一个 `_dirty` 布尔标志位，3.4 之后换成了**版本号计数加双向链表**，借鉴了 Preact Signals。有个 **`globalVersion` 做快速通道**——全局没发生任何响应式变更就直接吃缓存，连依赖比对都省了；computed 还改成了**按需订阅**，拿到第一个订阅者才订阅依赖、失去订阅者自动退订，顺带解决了内存问题。3.4 另一个行为改进是：**依赖变了但算出的结果没变，就不触发下游更新**。3.5 又重构优化了一轮，内存降了 56%，并修复了 SSR 下 hanging computed 导致的 stale value。」
- **高频追问要点**：
  - **computed 的双重身份是什么？** → 它既是**消费者**（内部读响应式数据、收集依赖），又是**生产者**（别人读它、订阅它）。内部 = 一个 effect + 一个 dep。
  - **旧实现（3.3-）怎么做的？** → `_dirty` 布尔标志：初始 true → 读 `.value` 时若 dirty 就算、算完置 false → 依赖变化时 scheduler 把它**改回 true（只标脏，不重算）**。
  - **依赖变了会立即重算吗？** → **不会**，只标记失效。
  - **版本号计数具体怎么工作？** → 每个响应式数据带一个 `version`，每次变化 `version++`；computed 计算时记下各依赖当时的版本号，下次读 `.value` 时对比"依赖现在的 version 和我上次记的一样吗"——全一样直接吐缓存。比 `_dirty` 精准：布尔值只能表达"可能脏了"，版本号能精确判断依赖到底变没变。
  - **globalVersion 是什么？** → 全局版本号，**应用里任何响应式写操作都 `++`**。computed 读取第一步就看它：跟上次一样 → 全局啥都没变 → 连自己的依赖都不用逐个查，直接吐缓存（最便宜的提前退出）。
  - **双向链表解决什么？** → 依赖追踪的数据结构，替代旧的 Set。一个 **Link 节点同时挂在两条链上**（数据的"订阅者链" + effect 的"依赖链"），增删依赖 O(1)、节点可复用（effect 重跑不再清空重建）→ 内存更省。上面版本号对比用的"旧 version"就存在 Link 节点上。
  - **borrow 自哪里？** → Vue 3.4 的这套版本号计数 + 双向链表借鉴了 **Preact Signals** 的实现思路。Signals 和 Vue 的 ref/computed 本就是同源概念（可追踪的值 + 派生 + 副作用三件套），借的是"双向链表做依赖追踪"这个工程实现，不是新概念。
- **翻车点**：说"依赖变了就重新计算"（是标脏）；只会背 `_dirty`——**版本停在 3.3**，2026 年要能说出 3.4 的版本号计数重构。硬吹 Preact Signals 细节（被追问只答"同源概念 + 借了双向链表"即可，别聊它和组件渲染的绑定）。

### Q：一个 computed 依赖另一个 computed，Vue 怎么保证求值顺序正确、不会读到过期的中间值？
- **一句话结论**：**不需要保证**——顺序是被读取行为天然驱动出来的，这是拉模型的红利。
- **面试口语版话术**：
  > 「这个问题其实**不需要额外机制**——因为 computed 是**惰性的、拉取式的**。B 依赖 A，那 B 求值时会读 A 的 `.value`，自然就触发 A 的 getter；A 发现自己失效就先算自己、再把新值返回给 B。所以**求值顺序是被读取行为天然驱动的**，不是 Vue 特意安排的——B 想要 A 的值，就必须先把 A 算出来，这是代码执行的物理必然。
  > 这正是惰性求值的**隐藏红利**。如果是**推模型**——依赖一变就立刻推着下游重算——遇到菱形依赖就会出问题：A、B 都依赖 X，C 依赖 A 和 B，那 X 一变，C 可能被算两次，而且第一次读到的是『A 更新了但 B 还没更新』的中间态，业界管这叫 **glitch，毛刺**。而**拉模型**因为是用的时候才沿依赖链往上拉、一路拉到最新，**天然不可能读到过期的中间值**。
  > 所以在 Vue 里『嵌套 computed 的顺序』这个问题，**在设计层面就不存在**。」
- **高频追问要点**：
  - **pull 和 push 的本质区别？** → push 是"依赖变了推着下游算"（可能算了没人用、可能读到中间态）；pull 是"谁要用谁往上拉"（按需、顺序天然正确）。
  - **拉模型的代价是什么？** → 需要一套失效标记机制来判断"要不要重算"，也就是 3.4 的版本号计数；换来的是不做无用功。
- **翻车点**：编造"Vue 内部维护了一个拓扑排序/优先级队列来保证顺序"——不需要，惰性求值本身就保证了。

### Q：`computed` 和 `methods` 有什么区别？什么时候必须用 methods？
- **一句话结论**：computed 有缓存，methods 没有——因为 **Vue 无从追踪 method 的依赖，也就无从判断缓存何时失效**。
- **面试口语版话术**：
  > 「核心区别就一个：**computed 有缓存，methods 没有**。模板里写 `{{ getFoo() }}`，每次重新渲染都会老老实实再调一次；而 computed 只要依赖没变，读多少次都是吃缓存。
  > **为什么 methods 没缓存？** 不是设计者偷懒，而是**它没有缓存的资格**——methods 只是个普通函数，**Vue 根本无从知道它内部依赖了什么**，也就无从判断这个缓存什么时候该失效。没有依赖追踪，就不敢缓存。而 computed 的 getter 会跑在 effect 里，依赖被精确收集，Vue 才知道何时该让缓存失效。
  > 那是不是有缓存就该无脑用 computed？**不是**，有四种情况必须用 methods：**第一，需要传参**——computed 是属性不是函数，接不了参数；**第二，有副作用**——computed 的 getter 必须是纯函数，不能改状态、发请求、操作 DOM，所以事件处理一律用 methods；**第三，每次都要新结果**——像 `Math.random()`、`Date.now()`，用 computed 会被缓存死，永远返回第一次的值；**第四，依赖的是非响应式数据**——收集不到依赖，缓存就永远不会失效，页面也就永远不更新。
  > 所以我的判断标准很简单：**『由响应式数据推导出来的、纯的、要复用的值』用 computed；『要传参的、有副作用的、每次都得重新求的』用 methods。**」
- **高频追问要点**：
  - **computed 的 getter 里读了一个非响应式的普通变量（`let n = 1`），n 变了 computed 会更新吗？** → **不会**。这个变量没被收集进依赖，改变时也不会派发更新，computed 永远停在缓存值上。**这正是"必须用 methods"的第四种情况。**
  - **要传参又想要缓存怎么办？** → 用返回函数的 computed（`computed(() => (id) => ...)`）能传参但**没有缓存意义**；真要缓存就自己按 key 做一层 Map 缓存，或把参数提升成响应式状态。
- **翻车点**：说"methods 不需要实时更新所以不缓存"——**循环论证**，说的是现象不是原因；说"computed 更快所以都该用 computed"。

---

## 9. AI 应用前端

### Q：AI 聊天的流式输出，为什么常用 SSE 而不是 WebSocket？
- **一句话结论**：选型要匹配场景——AI 对话本质是服务器单向吐字，SSE 刚好够用、还自带重连、又能复用整个 HTTP 生态；WebSocket 的全双工是过剩能力。
- **面试口语版话术**：
  > 「先说本质：**SSE 就是一个不结束的 HTTP 响应**，服务器保持连接持续往里写数据，单向；WebSocket 是通过 HTTP 握手升级出来的**独立协议 ws://**，全双工双向。
  > AI 聊天的场景是：用户发一次问题，然后服务器**源源不断单向推 token**——这就是个单向流，**SSE 的能力刚好完全匹配，WebSocket 的双向在这里是浪费**。而且 SSE 有几个实打实的好处：一是**浏览器原生自带断线重连**，WebSocket 得自己写；二是它**就是 HTTP**，天然穿代理、防火墙、CDN，还能复用鉴权、HTTP/2 多路复用这些现成基建；三是实现简单，不用维护连接状态、心跳这一整套。
  > 所以我的判断是：**能用简单方案就别上重的**。只有像协同编辑、实时对战、双向音视频信令那种真需要双向低延迟的场景，才值得上 WebSocket。」
- **高频追问要点**：
  - **单向的 SSE，用户怎么发问、怎么点停止生成？** → 发问走**普通 POST**；停止用 **AbortController**（`fetch(url,{signal})` → `controller.abort()`，后端能感知连接断开、停止生成省 token）。
  - **生产里用原生 EventSource 吗？** → 一般**不用**。原生 EventSource **只能 GET、不能带 body/自定义 header**，而 AI 要 POST 对话历史 + 带鉴权头。所以生产用 **fetch + ReadableStream 手动读流**；代价是自动重连、`Last-Event-ID` 断点续传要自己实现。
  - **只能传文本是缺点吗？** → 不是。**大模型输出本身就是文本 token**，SSE 传文本天然对齐、无需转换；二进制在纯文本对话里没用武之地。
  - **SSE 有什么坑？** → HTTP/1.1 下浏览器**每域名并发连接约 6 个**，每条 SSE 长连接占一个，开多了会阻塞同域名其他请求。解法：上 **HTTP/2** 靠多路复用，或控制连接数。
- **翻车点**：说"SSE 不能自动重连"（**恰恰相反，原生自带**）；说"SSE 是 WebSocket 的简化版"（是不同协议，SSE=HTTP、WS=独立协议）；只会背"WS 全双工"却答不出"这个场景为什么不需要全双工"。

### Q：SSE 流式对话断线了怎么办？怎么做重连和断点续传？
- **一句话结论**：客户端记 `Last-Event-ID` 报进度、指数退避重连；能不能续取决于**服务器有没有缓存已生成的输出**（AI 生成不可重放）。
- **面试口语版话术**：
  > 「因为我们用 fetch 读流、放弃了原生 EventSource 的自动重连，所以要自己实现。机制是：**客户端持续记住收到的最后一个 event id**，断线重连时通过 `Last-Event-ID` 请求头带给服务器，服务器从下一个 id 接着发，做到不重复不丢字。重连间隔用**指数退避加随机抖动**——1s、2s、4s 逐次翻倍、设上限、加 jitter，避免把抖动中的服务器打垮、避免所有客户端同时重连造成风暴。
  > 但**关键的一半在服务器**：它必须把已经生成的内容**缓存下来**（比如存 Redis），否则续不了。这在 AI 场景尤其重要——**大模型是现生成的，你没法让它从第 1026 个 token 重新接着生成**，生成过程不可重放，所以只能靠服务器缓存已吐出的 token 来补发。
  > 另外要区分**主动中止和意外断线**：用 AbortController 主动 abort 会抛 AbortError，这种不重连；只有网络错误才走重连。UI 上重连失败要**保留已收到的半截回答**、给个手动'重新生成'按钮兜底。」
- **高频追问要点**：
  - **服务器凭什么能续？** → 它必须**缓存/持久化已生成的输出**（以会话/消息 id 为 key）；AI 生成不可重放，这是硬要求。
  - **为什么用指数退避？** → 立刻死命重连会在服务器抖动时雪上加霜；退避 + jitter 避免重连风暴。
  - **abort 和断线怎么区分？** → 自己设 `isManualAbort` 标志，abort 前置 true，catch 里看到就直接 return 不重连。
- **翻车点**：只答"客户端带 Last-Event-ID"、漏掉"服务器要缓存输出"（这是这题考点）；主动中止也去重连。

### Q：AI 流式对话，前端重点监控什么性能指标？
- **一句话结论**：TTFT 首字延迟最关键，因为流式渲染本质就是用"尽快出第一个字"掩盖"整体生成慢"。
- **面试口语版话术**：
  > 「AI 场景不看普通页面的 LCP，看它专属的两个指标：**TTFT，首字延迟**——从发请求到收到第一个字的时间；和 **token 生成速度**——首字之后每秒吐多少字。其中 **TTFT 最关键**，因为流式渲染的本质，就是用'尽快让用户看到第一个字'来掩盖'整段生成很慢'这件事——哪怕总共要 10 秒，只要 0.5 秒就开始逐字蹦，用户焦虑感就大幅下降，这也是所有大模型产品都做流式的原因。前端测法很简单：发请求打时间戳 t0，ReadableStream 读到第一个 chunk 打 t1，差值就是 TTFT，上报监控。」
- **高频追问要点**：
  - **为什么流式能提升体验？** → 它不缩短总时长，而是把"感知等待"从整段砍到首字，用户体感完全不同。
  - **生成速度慢怎么办？** → 前端可加"匀速输出"缓冲，把涌入的 token 排队匀速吐，稳定打字机节奏。
- **翻车点**：拿 LCP/FCP 套 AI 场景；只会说"越快越好"答不出 TTFT 为什么是核心。

### Q：流式输出 Markdown，逐字渲染会遇到什么问题？怎么解决？（AI 岗必考 ⭐）
- **一句话结论**：大模型逐字吐，Markdown 却要完整结构才能正确解析——四层坑：chunk 边界、语法截断、性能、安全。
- **面试口语版话术**：
  > 「核心矛盾是：模型一个字一个字吐，但 Markdown 要看到完整结构才能正确解析，拿残缺输入实时渲染必然出问题。我按四层处理：
  > **第一，chunk 边界**——ReadableStream 的 chunk 是按网络切的，一条 SSE 消息可能被切断、多字节中文可能被截半。所以要用 buffer 累积、按 `\n\n` 切完整消息，并用 **TextDecoder 的 stream:true** 处理截断的多字节字符防乱码。
  > **第二，Markdown 语法截断**——比如 ` ```co ` 代码块没闭合、`**` 粗体没配对，直接解析会乱渲染、会闪烁。解法是**缓冲住结尾不完整的语法单元、等闭合再渲染**，或用**容忍残缺的流式解析器做自动补全**。代码块最难，因为它作用域大、未闭合会吞掉后面全部，而且块内 Markdown 不转义、同一个字符含义未定。
  > **第三，性能**——别每来一个字就全量重解析，那是 O(n²) 会卡死主线程。用 **requestAnimationFrame 批量渲染**、只增量解析新增尾部。
  > **第四，安全**——转成 HTML 用 v-html 前**必须过 DOMPurify 消毒**，因为内容来自大模型，可能被提示词注入诱导输出恶意脚本，不消毒就是 XSS 漏洞。
  > 最后体验上做智能自动滚动：贴底就跟随，用户上滚就停。」
- **高频追问要点**：
  - **代码块为什么最难？** → 作用域大（未闭合吞后文）+ 块内 Markdown 不转义（`**` 在不在代码块里含义相反，流式时状态未定）。
  - **批量渲染的副作用？** → 打字机顺滑感丢失、文字一坨坨蹦；平衡点是每 16~33ms（30~60fps）渲一次，或加匀速输出缓冲。
  - **v-html 的风险？** → XSS。Markdown→HTML 若含 `<script>`/`<img onerror>` 会执行；必须先 DOMPurify 白名单过滤。AI 场景是刚需（提示词注入）。
- **翻车点**：以为 chunk = 完整消息；不消毒直接 v-html；说批量渲染"变慢"（实为不连贯）。
