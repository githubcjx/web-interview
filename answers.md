# 面试话术库
> 用途：面试前朗读。最后更新：2026-08-06 ｜ 已收录 32 条（JS 核心 3 ｜ Vue 15 ｜ 小程序 7 ｜ AI 应用前端 5 ｜ 手写题 2）

---

## 1. JS 核心

### Q：`Promise.all`、`allSettled`、`race`、`any` 有什么区别？
- **一句话结论**：区别只在两件事——**对失败的态度**，和**看不看成败**。
- **面试口语版话术**：
  > 「这四个的区别，我习惯从两组对比来记。
  > 第一组是 **`all` 和 `allSettled`**，区别在**对失败的态度**：`all` 是一票否决，只要有一个 reject 它就立刻 reject，不等其他人——适合『必须全部成功』的场景，比如页面依赖三个接口，缺一个就没法渲染；而 `allSettled` 是等所有人都有结果，成败都算，它**永远不会 reject**，返回的是带 status 的结果数组——适合『允许部分失败』的场景，比如批量上报，成功的照常用、失败的单独处理。
  > 第二组是 **`race` 和 `any`**，区别在**看不看成败**：`race` 是第一个**结束**的说了算，哪怕它是失败结束的——所以常用来做超时控制；`any` 是第一个**成功**的说了算，失败的不算——适合多个备用源里取最快成功的那个。要补一句，`any` 在全部失败时抛的不是普通 error，是 **`AggregateError`**，里面的 `.errors` 装着所有失败原因。」
- **高频追问要点**：
  - **race 怎么做超时？** → `Promise.race([fetch(url), new Promise((_, reject) => setTimeout(() => reject(new Error('超时')), 3000))])`。注意超时哨兵要 **reject 而非 resolve**，这样外层 catch 能和成功区分开。
  - **race 超时后请求真的取消了吗？** → **没有**，只是你不等它了，请求还在后台跑。真正取消要配 **`AbortController`**（同一套 API 也用于中止 SSE 流式请求）。
  - **`all` 传入非 Promise 值（如数字 3）会怎样？** → 内部用 `Promise.resolve()` 包一层，自动变成已成功的 Promise，结果里就是 3。
- **翻车点**：把 `all` 和 `allSettled` 记反（`all` = 一票否决，`allSettled` = 无否决权）；漏答 `any` 的 `AggregateError`。

### Q：`requestAnimationFrame` 属于宏任务还是微任务？和 `setTimeout` 差在哪？
- **一句话结论**：都不属于，它是渲染流水线调度的**独立一类回调**。
- **面试口语版话术**：
  > 「rAF **既不是宏任务也不是微任务**，它不在这两个队列里，而是由**渲染流程**调度的独立回调——执行时机是在**每一帧重新渲染之前、微任务清空之后**。一帧的顺序大致是：执行一个宏任务 → 清空所有微任务 → 到了渲染时机就跑 rAF 回调 → 然后样式计算、布局、绘制。
  > 所以做动画要用 rAF 而不是 setTimeout：**setTimeout 属于宏任务，触发时机和渲染是解耦的**，一帧里可能跑好几次、也可能掉帧，动画会卡会抖；而 **rAF 和屏幕刷新率对齐**，保证每次重绘前正好执行一次，动画才丝滑。」
- **高频追问要点**：
  - **rAF 为什么要每帧手动约下一帧，不像 setInterval 设一次就循环？** → 三个好处：① 停止动画只要不再约下一帧，很干净；② **页面切到后台/不可见时浏览器自动暂停 rAF**，省电省 CPU，而 setInterval 在后台还傻跑；③ **不会任务堆积**——rAF 跟着渲染节奏走，setInterval 若一帧没处理完回调会积压，导致爆发式执行和跳帧。
- **翻车点**：说它是宏任务（最常见错误）；只答出"自由控制"而漏掉后台自动暂停和不堆积。

### Q：什么是"副作用（side effect）"？前端里指哪些操作？
- **一句话结论**：函数除了"根据入参算返回值"之外，对外部世界产生的任何影响，就是副作用。
- **面试口语版话术**：
  > 「副作用就是——一个函数除了『根据输入算出返回值』之外，还**对外部世界产生了影响**。纯函数像个计算器，相同输入永远相同输出、不碰外面任何东西；一旦它改了外部变量、发了请求、操作了 DOM、读写了 localStorage、设了定时器、打了 log，就是有副作用。
  > 判断口诀很简单：**这个函数跑完，除了吐一个返回值，有没有在外面留下痕迹、或依赖了外部会变的东西？有就是有副作用。**
  > 这也解释了**为什么 Vue 的 computed getter 必须纯**——computed 是惰性的、带缓存的、执行次数不可控，你在里面发请求，可能因为命中缓存永远不发、也可能发很多次。所以约定是：**computed 只做纯计算（what），副作用交给 watch/watchEffect/methods（do）。**」
- **高频追问要点**：
  - **哪些算副作用？** → 改外部状态、网络请求、操作 DOM、定时器、读写 localStorage/cookie、console/alert、路由跳转、`Math.random()`/`Date.now()`（不可预测）。
  - **和 React 的关系？** → React 的 `useEffect` 名字直接就叫 effect，管的就是"渲染之外的副作用"（请求、订阅、手动改 DOM），同一个概念。
- **翻车点**：把"副作用"狭义理解成只有"发请求"；忘了"修改传入的对象/参数"也是副作用。

---

## 2. Vue

### Q：`ref` 为什么要 `.value`？`reactive` 有什么局限？
- **一句话结论**：`.value` 是 **Proxy 代理不了原始值**这个语言限制的直接后果。
- **面试口语版话术**：
  > 「`.value` 的存在，本质是 **Proxy 无法代理原始值**这个语言限制的直接后果。你想，`let count = 0; count++`，对一个原始值变量的读写，**JS 语言层面根本没有可拦截的入口**——Proxy 拦的是『对象的属性操作』，而 count 压根不是对象、也没有属性。
  > 所以 Vue 只能用一个对象把值包起来、暴露成 `.value` 属性，这样『读』和『写』就变成了属性访问，才有 getter/setter 可以挂依赖收集和派发更新。所以**ref 内部不是 Proxy，是 class 加访问器**。
  > 而 reactive 接收的本来就是对象，Proxy 直接怼上去，属性访问天然可拦截，自然不需要 `.value`。但它有代价：**只能用于对象**、**解构会丢响应性**、**整体重新赋值也会丢**，得靠 `toRefs` 兜底。
  > 所以我的实践选择是**优先用 ref**——用一点 `.value` 的书写负担，换来引用稳定、不怕解构、不怕重新赋值，团队协作的心智成本更低。」
- **高频追问要点**：
  - **ref 是用 Proxy 实现的吗？** → **不是**。原始值的 ref 是 `class RefImpl` + `get value()/set value()` 访问器。
  - **`ref({a:1})` 传对象呢？** → 内部调 `reactive()` 把对象包成 Proxy 存进 `_value`，所以读 `.value` 拿到的是 **Proxy**。
  - **reactive 为什么不能解构？** → 解构把 Proxy 属性上的**原始值拷出来**了，从此和 Proxy 再无关系。解药是 `toRefs`，把每个属性包成持有源对象引用的 ref。
  - **为什么模板里不用写 `.value`？** → 叫**自动解包（unwrap）**，不叫解构。模板要经过**编译**，Vue 能在生成渲染函数时对 setup 上下文做 `unref()`；而你在 script 里写的任意 JS，Vue 拦不住普通变量的读写（还是同一个根因）。
  - **解包有什么边界？** → **只在顶层生效**。`const obj = { count: ref(0) }`，模板里 `{{ obj.count }}` **不会解包**，得写 `{{ obj.count.value }}`。
- **翻车点**：说"ref 是用 Proxy 实现的"；把"解包"说成"解构"。

### Q：Vue 里 `props` 能解构吗？为什么 `reactive` 不能而它能？（版本敏感 ⚠️）
- **一句话结论**：**Vue 3.5+ 可以解构**，靠的是**编译时改写**，不是运行时。
- **面试口语版话术**：
  > 「这个要分版本说。**3.4 及以下不能解构**，解构出来就是个永不改变的常量。但 **Vue 3.5 起，Reactive Props Destructure 已经稳定并默认开启**，可以放心解构。
  > 它能做到、而 reactive 做不到，关键区别在于：**props 靠的是编译时的魔法**——编译器会把你解构出来的变量，在**所有引用处自动改写回 `props.xxx`**，所以响应性从来没断过，你写的是 `count`，编译产物是 `props.count`。而 **reactive 的解构是纯运行时的普通 JS 解构**，编译器不介入，值一拷出来链路就断了。
  > 这也体现了 Vue 的一贯思路：**能在编译时解决的，就不留给运行时。**」
- **高频追问要点**：
  - **这个编译魔法的边界在哪？** → 注意别把解构出的 prop **当"值"传给 `watch`**：`watch(count, cb)` 编译后是 `watch(props.count, cb)`，`props.count` 在那一行**当场求值成一个死值（数字）**，监听不到；要写成 `watch(() => count, cb)`（编译后 `watch(() => props.count, cb)`），传一个 **getter 延后取值**才能追踪。
  - **注意：这不是"编译器改写不了函数内部"** —— 编译器**函数体内照样改写**（`function f(){ console.log(count) }` 里的 count 也会变 `props.count`）。真正原因是 **值 vs getter 的求值时机**，且这本就是 **watch 的通用规则：第一个参数只认 ref / reactive / getter，不能传普通值**（对普通 `reactive` 属性也一样：`watch(obj.count)` ❌ → `watch(() => obj.count)` ✅）。
- **翻车点**：① 只答"props 不能解构"——**3.4 旧答案**，2026 年会被认为版本停两年前；② 把 watch 失效归因为"改写不了回调内部"——错，是求值时机（值 vs getter）。

### Q：`computed` 的缓存和惰性求值是怎么实现的？（版本敏感 ⚠️）
- **一句话结论**：依赖变化时**不重算，只标记失效**；真正计算推迟到下次读 `.value`。
- **面试口语版话术**：
  > 「computed 的核心是**惰性求值加缓存**。最关键的一点是：**依赖变化时它不会立即重算，只是把自己标记为失效**，真正的计算推迟到下次有人读 `.value` 的那一刻。
  > 这样设计有两个理由：一是**可能根本没人读**——比如这个 computed 被 v-if 干掉了，立即算就是纯浪费；二是**能合并中间状态**——一次操作连改五个依赖，立即算要算五次，惰性只在最后读的时候算一次。所以惰性求值的本质价值，是把计算推迟到真正需要的那一刻，顺手合并掉中间所有无效计算。
  > 实现上 **Vue 3.4 重构过**：早期版本靠一个 `_dirty` 布尔标志位，3.4 之后换成了**版本号计数加双向链表**，借鉴了 Preact Signals。有个 **`globalVersion` 做快速通道**——全局没发生任何响应式变更就直接吃缓存，连依赖比对都省了；computed 还改成了**按需订阅**，拿到第一个订阅者才订阅依赖、失去订阅者自动退订，顺带解决了内存问题。3.4 另一个行为改进是：**依赖变了但算出的结果没变，就不触发下游更新**。3.5 又重构优化了一轮，内存降了 56%，并修复了 SSR 下 hanging computed 导致的 stale value。」
- **高频追问要点**：
  - **computed 的双重身份是什么？** → 它既是**消费者**（内部读响应式数据、收集依赖），又是**生产者**（别人读它、订阅它）。内部 = 一个 effect + 一个 dep。
  - **旧实现（3.3-）怎么做的？** → `_dirty` 布尔标志：初始 true → 读 `.value` 时若 dirty 就算、算完置 false → 依赖变化时 scheduler 把它**改回 true（只标脏，不重算）**。
  - **依赖变了会立即重算吗？** → **不会**，只标记失效。
  - **版本号计数具体怎么工作？** → 每个响应式数据带一个 `version`，每次变化 `version++`；computed 计算时记下各依赖当时的版本号，下次读 `.value` 时对比"依赖现在的 version 和我上次记的一样吗"——全一样直接吐缓存。比 `_dirty` 精准：布尔值只能表达"可能脏了"，版本号能精确判断依赖到底变没变。
  - **globalVersion 是什么？** → 全局版本号，**应用里任何响应式写操作都 `++`**。computed 读取第一步就看它：跟上次一样 → 全局啥都没变 → 连自己的依赖都不用逐个查，直接吐缓存（最便宜的提前退出）。
  - **双向链表解决什么？** → 依赖追踪的数据结构，替代旧的 Set。一个 **Link 节点同时挂在两条链上**（数据的"订阅者链" + effect 的"依赖链"），增删依赖 O(1)、节点可复用（effect 重跑不再清空重建）→ 内存更省。上面版本号对比用的"旧 version"就存在 Link 节点上。
  - **borrow 自哪里？** → Vue 3.4 的这套版本号计数 + 双向链表借鉴了 **Preact Signals** 的实现思路。Signals 和 Vue 的 ref/computed 本就是同源概念（可追踪的值 + 派生 + 副作用三件套），借的是"双向链表做依赖追踪"这个工程实现，不是新概念。
- **翻车点**：说"依赖变了就重新计算"（是标脏）；只会背 `_dirty`——**版本停在 3.3**，2026 年要能说出 3.4 的版本号计数重构。硬吹 Preact Signals 细节（被追问只答"同源概念 + 借了双向链表"即可，别聊它和组件渲染的绑定）。

### Q：一个 computed 依赖另一个 computed，Vue 怎么保证求值顺序正确、不会读到过期的中间值？
- **一句话结论**：**不需要保证**——顺序是被读取行为天然驱动出来的，这是拉模型的红利。
- **面试口语版话术**：
  > 「这个问题其实**不需要额外机制**——因为 computed 是**惰性的、拉取式的**。B 依赖 A，那 B 求值时会读 A 的 `.value`，自然就触发 A 的 getter；A 发现自己失效就先算自己、再把新值返回给 B。所以**求值顺序是被读取行为天然驱动的**，不是 Vue 特意安排的——B 想要 A 的值，就必须先把 A 算出来，这是代码执行的物理必然。
  > 这正是惰性求值的**隐藏红利**。如果是**推模型**——依赖一变就立刻推着下游重算——遇到菱形依赖就会出问题：A、B 都依赖 X，C 依赖 A 和 B，那 X 一变，C 可能被算两次，而且第一次读到的是『A 更新了但 B 还没更新』的中间态，业界管这叫 **glitch，毛刺**。而**拉模型**因为是用的时候才沿依赖链往上拉、一路拉到最新，**天然不可能读到过期的中间值**。
  > 所以在 Vue 里『嵌套 computed 的顺序』这个问题，**在设计层面就不存在**。」
- **高频追问要点**：
  - **pull 和 push 的本质区别？** → push 是"依赖变了推着下游算"（可能算了没人用、可能读到中间态）；pull 是"谁要用谁往上拉"（按需、顺序天然正确）。
  - **拉模型的代价是什么？** → 需要一套失效标记机制来判断"要不要重算"，也就是 3.4 的版本号计数；换来的是不做无用功。
- **翻车点**：编造"Vue 内部维护了一个拓扑排序/优先级队列来保证顺序"——不需要，惰性求值本身就保证了。

### Q：`computed` 和 `methods` 有什么区别？什么时候必须用 methods？
- **一句话结论**：computed 有缓存，methods 没有——因为 **Vue 无从追踪 method 的依赖，也就无从判断缓存何时失效**。
- **面试口语版话术**：
  > 「核心区别就一个：**computed 有缓存，methods 没有**。模板里写 `{{ getFoo() }}`，每次重新渲染都会老老实实再调一次；而 computed 只要依赖没变，读多少次都是吃缓存。
  > **为什么 methods 没缓存？** 不是设计者偷懒，而是**它没有缓存的资格**——methods 只是个普通函数，**Vue 根本无从知道它内部依赖了什么**，也就无从判断这个缓存什么时候该失效。没有依赖追踪，就不敢缓存。而 computed 的 getter 会跑在 effect 里，依赖被精确收集，Vue 才知道何时该让缓存失效。
  > 那是不是有缓存就该无脑用 computed？**不是**，有四种情况必须用 methods：**第一，需要传参**——computed 是属性不是函数，接不了参数；**第二，有副作用**——computed 的 getter 必须是纯函数，不能改状态、发请求、操作 DOM，所以事件处理一律用 methods；**第三，每次都要新结果**——像 `Math.random()`、`Date.now()`，用 computed 会被缓存死，永远返回第一次的值；**第四，依赖的是非响应式数据**——收集不到依赖，缓存就永远不会失效，页面也就永远不更新。
  > 所以我的判断标准很简单：**『由响应式数据推导出来的、纯的、要复用的值』用 computed；『要传参的、有副作用的、每次都得重新求的』用 methods。**」
- **高频追问要点**：
  - **computed 的 getter 里读了一个非响应式的普通变量（`let n = 1`），n 变了 computed 会更新吗？** → **不会**。这个变量没被收集进依赖，改变时也不会派发更新，computed 永远停在缓存值上。**这正是"必须用 methods"的第四种情况。**
  - **要传参又想要缓存怎么办？** → 用返回函数的 computed（`computed(() => (id) => ...)`）能传参但**没有缓存意义**；真要缓存就自己按 key 做一层 Map 缓存，或把参数提升成响应式状态。
- **翻车点**：说"methods 不需要实时更新所以不缓存"——**循环论证**，说的是现象不是原因；说"computed 更快所以都该用 computed"。

### Q：`watch` 的 `flush` 有哪几种？为什么默认是 `pre`？
- **一句话结论**：`pre`/`post`/`sync` 决定回调**相对于组件更新**的时机；默认 `pre` 是为了**合并中间态、且能并进同一次渲染**。
- **面试口语版话术**：
  > 「`flush` 决定 watch 回调在**组件更新的哪个时机**执行：**`pre` 是更新前（默认）**，**`post` 是更新后**（此时能读到最新 DOM），**`sync` 是同步立即执行**。
  > **为什么默认是 `pre` 而不是 `sync`？** 因为一次操作往往会改动多个依赖——`sync` 会**同步触发多次回调**，纯浪费；而 `pre` 是把回调推进一个**会去重的队列**，同一个 watcher 在一个 tick 内触发五次也只保留一份，最后统一 flush 只跑一次。
  > 更关键的是，**`sync` 还会让你读到中间态**——改了 A 还没改 B，回调就跑了，看到的是不一致的状态。这个问题有个名字叫 **glitch，毛刺**，和 computed 那里讲的『推模型会读到中间值』是同一个坑，`flush:'sync'` 相当于把 watch 变成了推模型。
  > **那为什么是 `pre` 而不是 `post`？** 因为**大多数 watch 回调做的是改状态、发请求，根本不需要 DOM**。放在更新之前执行，如果回调里又改了状态，**可以并进同一次渲染**；放在 `post` 就得多渲染一轮。所以 `post` 是给『确实要读新 DOM』的场景准备的，不该当默认。」
- **高频追问要点**：
  - **watch 回调里要读更新后的 DOM，有哪两种做法？区别？** → ① `flush: 'post'`——**声明式，作用于整个 watcher**；② `nextTick()`——**命令式，只影响调用的那一处**。选择标准：**这个 watcher 是不是每次都要新 DOM**，是就用 `post`，只有个别分支需要就用 `nextTick`。
  - **`sync` 什么时候才该用？** → 极少。只有需要"立刻拿到变更、不能等一个 tick"的特殊场景（如某些测试、或与外部非响应式系统同步），且要明确接受多次触发和中间态的代价。
- **翻车点**：说 `pre` 是"延迟执行"（是**队列去重后统一 flush**，不是单纯延迟）；只说 `sync` "执行多次"而**答不出更严重的"读到中间态/glitch"**；把 `post` 当默认。

### Q：`nextTick` 是怎么实现的？为什么 Vue 的 DOM 更新是异步的？
- **一句话结论**：更新是**异步批量**的，`nextTick` 只是**把你的回调挂到同一条微任务链上、排在更新任务后面**。
- **面试口语版话术**：
  > 「先说为什么需要它：**Vue 的 DOM 更新是异步批量的**。你连改三次数据，Vue 不会渲染三次——它把更新任务丢进一个**会去重的调度队列**，等当前这段同步代码跑完，**统一渲染一次**。所以你在同步代码里立刻读 DOM，读到的必然是旧值。这么设计纯粹是为了性能：一次用户操作可能改 100 次数据，同步更新就要渲染 100 次。
  > 而 **`nextTick` 的实现比想象中简单得多**——Vue 用**微任务**来 flush 那个队列，`nextTick` 做的事就是**把你的回调 `.then` 到同一条 Promise 链的后面**。更新任务排在前面、你的回调排在后面，所以轮到你时 DOM 必然已经更新完了。**它不是『等一帧』也不是『延迟一下』，而是『排在更新任务后面』。**
  > **为什么用微任务而不是 `setTimeout`？** 因为一轮事件循环是『执行一个宏任务 → 清空所有微任务 →（可能）渲染』——**微任务赶在这一帧渲染之前执行完**，数据变更和 DOM 更新在同一帧内完成，用户感知不到延迟；而 `setTimeout` 是下一个宏任务，至少要等本帧渲染结束，更新就晚了一整帧，用户可能先看到旧内容闪一下。
  > 版本上还有个演进：**Vue 2 有一套四级降级**——Promise → MutationObserver → setImmediate → setTimeout，为了兼容老浏览器；**Vue 3 直接只用 Promise**，因为它放弃了 IE，兼容包袱一卸实现就剩几行了。」
- **高频追问要点**：
  - **`nextTick` 的核心实现长什么样？** → 
    ```js
    const resolvedPromise = Promise.resolve()
    let currentFlushPromise = null
    function nextTick(fn) {
      const p = currentFlushPromise || resolvedPromise
      return fn ? p.then(fn) : p
    }
    ```
    就是挂到同一条 Promise 链上。
  - **为什么 Vue 要异步更新？** → 合并中间态、去重，一次操作只渲染一次。**这和 computed 惰性求值合并中间状态、watch 的 `pre` 队列去重是同一个设计哲学：合并中间态，只对最终结果响应一次。**
  - **`nextTick` 和 `flush:'post'` 底层是一回事吗？** → 同一套调度队列的不同用法：`post` 是把 watcher 回调排进"更新后"那一批；`nextTick` 是把任意回调排到当前 flush 之后。
- **翻车点**：说 `nextTick` 是 `setTimeout` 包了一层（**是微任务，不是宏任务**）；答不出"为什么必须是微任务"（赶在本帧渲染前）；只会背 Vue 2 的四级降级而**不知道 Vue 3 已简化为纯 Promise**（版本停在几年前）。

### Q：`key` 有什么作用？为什么不能用 `index` 做 key？（🔴 高频）
- **一句话结论**：`key` 是节点的**身份证**；用 `index` 等于没有 key，代价不只是性能，**更是状态错乱**。
- **面试口语版话术**：
  > 「`key` 是节点的**身份证**——它告诉 diff 算法『新列表里这个节点，和旧列表里那个，是不是同一个东西』。
  > **不写 key 时 Vue 采用『就地复用』**：按位置对应，第 0 个对第 0 个，只更新内容不移动节点。纯静态展示很高效，但列表一旦会增删排序就出问题。
  > **而用 `index` 做 key，效果基本等同于没有 key**——因为 index 是按位置分配的，节点一动 index 就重新洗牌，身份证等于作废。
  > 后果有两层。**第一层是性能**：在列表头部插入一项，后面所有 index 全部 +1，Vue 一比对认为**所有节点都变了**，于是全部重新 patch，而实际上只需要插入一个。
  > **第二层才是致命的——状态错乱**。列表项里的勾选框、输入框内容、组件内部的 ref，**这些状态是跟着 DOM 节点走的，不是跟着数据走的**。比如一个待办列表用 index 做 key，第一项被勾选了，这时候删掉第一项——**第二项会顶上 index 0，直接继承那个勾选状态**，用户看到的是"我删了 A，B 自己被勾上了"。这种 bug 极难排查。
  > 所以我的原则是：**key 要用业务上唯一且稳定的 id**。只有在列表纯静态、不会增删排序、且列表项没有任何内部状态时，index 才是无害的——但既然要求是"唯一且稳定"，直接用 id 更省心。」
- **高频追问要点**：
  - **Vue 的 diff 为什么只做同层比较？** → 因为**完整的树 diff 是 O(n³)**，1000 个节点就是十亿量级操作，**根本不可用**；同层比较压到 **O(n)**。代价是：真发生跨层移动时，节点被**销毁重建**——不只多花性能，**内部状态也一起丢**。这是典型的"用极小概率场景的低效，换绝大多数场景的高效"，React 的 diff 做了完全相同的假设。
  - **类型变了会怎样？** → `type` 不同直接卸载重建，不再 diff 子树。
  - **Vue 3 的列表 diff 比 Vue 2 强在哪？** → Vue 2 是双端比较；**Vue 3 在处理中间乱序部分时用了「最长递增子序列」**，求出不需要移动的最长子序列，**让 DOM 移动次数最少**——因为移动 DOM 是昂贵操作。
- **翻车点**：只会说"index 做 key 性能差"，**答不出状态错乱**（这才是考点）；说"不写 key 会报错"（只是警告/降级为就地复用）；说 diff 是"完整树对比"。

### Q：`keep-alive` 的原理是什么？`max` 是干什么的？
- **一句话结论**：它**阻止组件销毁**、把实例缓存起来；`max` 用 **LRU** 淘汰，因为缓存要吃内存。
- **面试口语版话术**：
  > 「典型场景是**列表页 → 详情页 → 返回列表页**：默认组件切走就销毁，返回时重建，于是滚动位置回顶部、筛选条件重置、数据重新请求一遍。`keep-alive` 把组件实例**缓存起来而不是销毁**，切回来直接复用，状态原封不动。
  > 原理三句话：**它是个抽象组件**，自己不渲染任何 DOM，只在渲染函数里做拦截；内部维护一个 **`cache`（key → vnode）和一个 `keys` 数组（记使用顺序）**，渲染时先查缓存，命中就复用实例；组件被切走时**不走卸载流程**，而是把 DOM 移到一个内存中的隐藏容器存着，切回来再移回文档——**实例从未销毁，所以状态全在**。
  > 因为实例不销毁，`mounted`/`unmounted` 就不再反复触发了，取而代之是 **`onActivated` / `onDeactivated`** 两个专属钩子。**所以『每次进入页面都要刷新数据』的逻辑必须写在 `onActivated` 里**——写在 `onMounted` 只会执行一次，这是最经典的 keep-alive 踩坑。
  > 至于 `max`，它是缓存上限，**淘汰策略是 LRU，最近最少使用**。为什么要有上限？因为**缓存的组件实例和它整棵 DOM 都占着内存**，无限缓存等于内存只增不减，用户来回切几十个详情页就爆了。所以 `max` 是在**『状态保留』和『内存占用』之间划一条线**——`keep-alive` 换来的体验提升，代价就是内存。」
- **高频追问要点**：
  - **`keep-alive` 内部的 LRU 怎么实现？** → `cache` 存内容，`keys` 数组记顺序：**命中缓存就把该 key 从数组删掉、重新 push 到末尾**（标记为刚用过）；**超过 max 就删 `keys[0]`**（头部即最久未用）并销毁其实例。**"命中就移到末尾，淘汰就砍头部"——这两句就是 LRU 的全部。**
  - **和 `key` 是什么关系？** → 是同一根轴的两端：**`key` 变化 = 强制销毁重建；`keep-alive` = 阻止销毁。** 管的都是组件实例的生死。
  - **`include`/`exclude` 匹配什么？** → 匹配组件的 `name`（`<script setup>` 下可用 `defineOptions({ name })` 指定）。
- **翻车点**：说 `keep-alive` 会渲染一个包裹元素（**它是抽象组件，不渲染 DOM**）；把刷新逻辑写在 `onMounted` 里；答不出 `max` 的淘汰策略名字（LRU）。

### Q：Vue 组件通信有哪些方式？怎么选？
- **一句话结论**：不要背 API 清单，**按"组件之间是什么关系"分类**再展开。
- **面试口语版话术**：
  > 「我习惯按**组件关系**来分，而不是背 API：
  > **父传子用 `props`，子传父用 `emit`**，这是最基本也最该优先用的——因为关系明确、可静态追踪。
  > **父子双向用 `v-model`**。要强调一点：**`v-model` 本质就是 `props + emit` 的语法糖**。**Vue 3.4 起 `defineModel()` 已经转正**，一行 `const model = defineModel()` 就能当 ref 读写、内部自动 emit，比以前手写 `modelValue` + `update:modelValue` 干净得多。
  > **父要调用子的方法**，用 `ref` 拿实例配 `defineExpose` 暴露。
  > **跨多层、基础设施型的东西用 `provide/inject`**——主题、国际化、表单上下文这类。
  > **任意关系的共享状态用 Pinia。**
  > 最后，**Vue 3 已经移除了 `$on`/`$off`，也就是没有内置 EventBus 了**。这个移除是有道理的——**EventBus 是隐式通信，谁发的谁收的在代码里静态分析不出来**，事件满天飞，出 bug 无从追溯，组件卸载忘了解绑还会内存泄漏。Vue 3 的态度是：**通信应该显式、可追踪**——父子关系明确的走 props/emit，跨组件的走状态管理，有唯一数据源也有 devtools 可查。真需要临时事件总线就用 `mitt`，几百字节，但解绑要自己管。」
- **高频追问要点**：
  - **`v-model` 在组件上是怎么工作的？（⚠️ 版本敏感）** → 本质是 `props: modelValue` + `emit: update:modelValue`。**3.4 起用 `defineModel()`**（3.3 是实验性），还支持多个 v-model 和修饰符。**还在讲手写 `update:modelValue` 那套 = 版本停在 3.3。**
  - **`provide/inject` 和 Pinia 怎么选？** → 看**作用域**：`provide` 的作用域是**一棵子树**（且后代自动找最近的祖先），Pinia 是**全局单例**。组件库内部的父子协作必须用 `provide`——因为**一个页面可能有多个组件实例，Pinia 单例会互相污染**。
  - **组件库为什么不能用 props 逐层传？** → 关键不是"麻烦"，是**"做不到"**——`<Form>` 和 `<FormItem>` 之间隔着什么是**使用者决定**的（div/Row/Col/用户自封装的组件），**组件库无权要求用户的组件帮忙透传**。
- **翻车点**：一上来罗列 8 个 API 不给判断标准；说 Vue 3 还能用 EventBus；答 `v-model` 只会说"双向绑定"说不出它是 props+emit 语法糖。

### Q：`provide` / `inject` 有哪些坑？
- **一句话结论**：**传容器不传值**、必须在 setup 同步调用、修改权要留在提供方。
- **面试口语版话术**：
  > 「最核心的一条是**传容器，不传值**——`provide('count', count.value)` 是错的，传的是当前那个数字的快照，链路当场就断了；要 `provide('count', count)` 把 `ref` 本身传下去，后代拿到的才是同一个容器。
  > 第二，**`inject` 必须在 `setup` 里同步调用**，不能放在 `onMounted` 回调、`setTimeout` 或事件处理函数里。原因是它依赖『当前正在初始化的组件实例』这个上下文，一进异步回调上下文就没了——**和生命周期钩子必须同步调用是同一个机制**。
  > 第三是**修改权**。后代直接改注入的数据，会让数据流向彻底不可追踪——出了 bug 不知道是哪个后代改的。官方推荐用 **`readonly()` 包一层只读地 provide 出去，同时把修改方法一起 provide 下去**，要改就调方法。**状态的修改权，留在 provide 它的那一方**——和 Pinia 要求"通过 action 改状态"是同一个思路。
  > 第四，写组件库的话，**key 要用 `Symbol` 而不是字符串**，避免重名冲突；配合 TS 的 **`InjectionKey<T>`**，让 key 本身携带类型，`inject` 出来就能自动推导，不然是 `unknown`。
  > 最后提醒一点：`provide/inject` **本身也是隐式依赖**——后代不声明就能拿到，看代码不知道数据从哪来，脱离这个祖先就跑不起来。所以它**适合稳定的、基础设施型的东西**（主题、i18n、表单上下文），**不适合业务数据**，那是 Pinia 的活。」
- **高频追问要点**：
  - **provide 一个 `reactive` 对象，后来整体重新赋值会怎样？** → **后代还指着旧对象**（`reactive` 的老毛病）。所以更推荐 provide 一个 `ref`——整体换值改 `.value` 即可，引用稳定。
  - **`inject` 的第三个参数是干什么的？** → `inject(key, () => new Map(), true)`——**把默认值当工厂函数调用**。因为默认值本身是函数时，Vue 无法判断你想返回它还是调用它；`true` 表示调用，这样**每个组件拿到独立实例**而不是共享同一个对象。
  - **`app.provide()` 呢？** → 全应用可注入，适合插件和全局配置。但**和全局变量一样容易被滥用**，非必要还是就近 provide。
- **翻车点**：`provide('c', count.value)` 传值不传容器；在异步回调里 `inject`；让后代随意修改注入的状态；组件库里用字符串 key。

### Q：Vuex 和 Pinia 有什么区别？为什么 Vue 3 推荐 Pinia？
- **一句话结论**：**取消 mutation、取消 modules 嵌套、TS 零配置**——三条都是在还 Vuex 欠下的技术债。
- **面试口语版话术**：
  > 「先给结论：**Pinia 是现在的官方推荐，Vuex 已经进入维护模式**，新项目一律 Pinia。但更值得说的是**为什么演进**，有三条。
  > **第一，取消了 mutation。** 要理解这一步，得先想清楚 Vuex 里 mutation 存在的唯一理由——它要求所有状态变更走一个**同步、可记录**的入口，**这样 devtools 才能追踪谁在什么时候改了什么**，才能做时间旅行。但代价很重：改一个状态要写 mutation 加 action 两层，而 90% 的 action 内容就是调一下对应的 mutation，纯样板代码。**Pinia 靠 Vue 3 的响应式系统 + `$subscribe` 直接订阅变更**，追踪能力由框架底层提供，不再需要靠开发者遵守约定来换。**一句话：mutation 是为可追溯性交的税，Vue 3 之后这个税不用交了。**
  > **第二，取消了 modules 嵌套。** Vuex 是一个大 store 内部再分模块，于是有了 namespaced、有了 `dispatch('user/login')` 这种字符串路径——**TS 推不出来，重构改错名字要到运行时才报错**。Pinia 是**多个平行的 store**，import 进来直接用。本质变化是**从"一棵树"变成"一组扁平模块"**，因为 Composition API 已经让"组合"变得很廉价，不需要靠嵌套结构来组织。
  > **第三，TypeScript 才是决定性因素。** Vuex 4 的 `this.$store.state.user.name` 无法自动推导，想要类型安全得手写一大堆泛型声明，实践中大多数团队干脆放弃了。而 **Pinia 的 store 本质就是一个函数返回的对象，类型天然流动、零配置**。在 TS 已是标配的今天，这一条最实际。」
- **高频追问要点**：
  - **Pinia 的两种写法？** → **Option Store**（形状像 Vuex，迁移友好）和 **Setup Store**（像组件的 setup，更推荐）。Setup Store 的优势是**可以用任意 Composition API**——`watch`、`watchEffect` 甚至生命周期，心智负担为零。
  - **`const { count } = useStore()` 还响应式吗？** → **不是**，解构把值从容器里取出来就断了。要用 **`storeToRefs(store)`**——它把每个 state/getter 重新包成 ref。**但 action 可以直接解构**，因为函数不需要响应性，`storeToRefs` 也正是这么设计的（自动跳过 action）。**它本质就是 `toRefs` 的 store 版本。**
  - **还有什么场景该用 Vuex？** → 只有老项目维护、或团队已深度绑定时。新项目没有理由选它。
- **翻车点**：只会背对比表说不出**为什么取消 mutation**；解构 store 后奇怪为什么不更新；说 Pinia "更快"（**不是性能问题，是开发体验和类型安全**）。

### Q：为什么 Vue 里"解构就丢响应性"？（⭐ 统一规律，一条打四题）
- **一句话结论**：**响应性属于「容器」，不属于「值」。**
- **面试口语版话术**：
  > 「这其实是同一条规律在不同地方的表现。**Vue 的响应性是绑在『容器』上的——`ref` 也好、`reactive` 的 Proxy 也好，拦截的都是对这个容器的属性读写。所以任何把值从容器里取出来的动作，都会在那一刻切断链路。**
  > 我遇到过四个场景，全是同一个根因：
  > **一是 `reactive` 解构**——`const { count } = state`，把 Proxy 属性上的原始值拷出来了，从此和 Proxy 再无关系；
  > **二是 `watch` 传参**——`watch(props.count, cb)` 在那一行就把值求出来了，监听的是个死值；
  > **三是 `provide`**——`provide('c', count.value)` 传的是快照不是容器；
  > **四是 Pinia 解构** ——`const { count } = store`，同样是把值拷了出来。
  > **而解药也统一成两类：要么重新包回容器**——`toRefs`、`storeToRefs`（后者本质就是前者的 store 版）；**要么延迟取值**——传 getter，比如 `watch(() => count, cb)`，等真正需要时才去读。
  > 所以我理解这件事的方式是：**不要问『为什么这里丢了响应性』，要问『我是不是把值从容器里拿出来了』**。想清楚这一层，这一类问题就不用一个个记了。」
- **高频追问要点**：
  - **那模板里为什么不用写 `.value`？** → 那叫**自动解包**，是**编译时**做的（Vue 在生成渲染函数时对 setup 上下文做 `unref`），而且**只在顶层生效**——`{{ obj.count }}` 里的嵌套 ref 不会解包。
  - **`props` 解构为什么反而可以？（3.5+）** → 因为它也是**编译时改写**——编译器把解构出的变量在所有引用处改回 `props.xxx`，**链路从来没断过**。**能在编译时解决的就不留给运行时，这是 Vue 的一贯思路。**
- **翻车点**：把这四件事当四个孤立的知识点背；把"自动解包"说成"解构"。

### Q：Vue Router 的导航守卫有哪几类？为什么要分三层，只留一个 `beforeEach` 不行吗？

- **一句话结论**：三层的区别不是"代码写在哪"，而是**"手上能拿到什么"**——全局守卫看不见任何组件实例。
- **面试口语版话术**：
  > 「守卫分三层，分类依据只有一个——**作用域**：全局的 `beforeEach`/`beforeResolve`/`afterEach` 管所有跳转；路由独享的 `beforeEnter` 只管配置里的那一条；组件内的 `beforeRouteEnter`/`Update`/`Leave` 只管某个组件。
  > 为什么不能只留全局的？代码组织是次要理由，**关键理由是能力差异**：全局守卫的签名只有 `to` 和 `from` 两个路由对象，它站在路由这一层，**拿不到组件实例，也就拿不到组件内部状态**。所以像『表单填了一半，离开前提示未保存』这种需求，全局守卫根本做不到——那份 dirty 状态只存在于组件实例里，得用 `onBeforeRouteLeave` 写在组件里，闭包里就是它自己的状态。
  > 所以我的判断顺序是：**先问这件事的作用域有多大，再选层级**。这跟组件通信里 props 一层、provide 一棵子树、Pinia 全局是同一条轴。」
- **高频追问要点**：
  - **"把 dirty 存进 Pinia，全局守卫不就拿到了？"（必问）** → 能拿到，但错在层级。① **Pinia store 是单例**：后台系统开着多页签 + `keep-alive` 同时缓存两个编辑页时，两个实例共用一份 dirty，互相覆盖会丢数据；② **生命周期对不上**：组件销毁后 store 里的状态还在，得手动清，忘了下次进来就是脏的（**和 EventBus 忘解绑同型**）；③ 每加一个页面就要改一次全局守卫的分支。而 `onBeforeRouteLeave` 三个问题都不存在——**状态天然按实例隔离，组件销毁时守卫自动解绑**。判据一句话：**状态的作用域应该和它的生命周期对齐。**
  - **"那产品就要一个全局的『你有 3 个页面未保存』呢？"** → 那需求确实全局了，可以用 store，但结构得是 `Map<实例 id, dirty>`，进出各自登记注销——**等于在单例里手工造一层实例作用域，反过来正好说明原方案错在哪**。
  - **"用户直接刷新/关标签页呢？"（加分）** → **路由守卫管不到**，它只拦 SPA 内部跳转。要另配 `beforeunload`，且现代浏览器只允许弹系统默认文案，不能自定义。完整方案是两层：守卫管站内，`beforeunload` 管离站。
  - **执行顺序？** → 记三段：**先问能不能走 → 再问能不能进 → 最后收尾**。`beforeRouteLeave` → 全局 `beforeEach` → `beforeRouteUpdate` → `beforeEnter` → 解析异步组件 → `beforeRouteEnter` → 全局 `beforeResolve` → 导航确认 → `afterEach` → DOM 更新。中间那段就是**作用域由大到小**。
  - **`beforeResolve` 和 `afterEach` 干什么用？** → `beforeResolve` 是"所有守卫和异步组件都过完、页面马上要出来了"，适合"进去前必须先拿到的东西"（摄像头权限、预取数据）；`afterEach` 导航已确认、**没有拦截能力**，只做副作用：关进度条、埋点、改 title。
- **翻车点**：只答"方便维护/代码清晰"（属于"不好做"，挡不住"我在 beforeEach 里按 to.name 分发不也一样"的反问，**要答"做不到"级别的理由**）；用 `next()` 风格作答（那是 Vue Router 3，**v4 推荐用返回值**：无返回/`true` 放行、`false` 取消、返回地址即重定向）。

### Q：讲讲你怎么用路由守卫做登录鉴权和权限控制？（🔴 高频，社招必问）

- **一句话结论**：登录拦截的坑是**死循环**，权限设计的底线是**前端权限只是体验，不是安全边界**。
- **面试口语版话术**：
  > 「登录拦截我会在全局 `beforeEach` 里做，但有个坑必须先绕开——**如果只写『没 token 就重定向到 /login』，会死循环**：因为 `/login` 页面本身也没有 token，重定向过去又被拦，Vue Router 检测到会直接报 infinite redirection 并中断，**现象就是白屏**。
  > 我判断这类死循环有个通用判据：**重定向之后，触发重定向的那个条件还成不成立**——还成立就必然循环。解法只有两条：**要么改条件，要么豁免目标页**。登录这种情况条件改不了（登录前不可能凭空有 token），所以走豁免——但我不用一个全局白名单数组，而是写在路由的 `meta` 上，比如 `requiresAuth`，**信息跟着它描述的对象走**，新增页面不会忘，而且 `meta` 会沿嵌套路由继承，整个 `/admin` 子树写一次就行。另外重定向时我会把 `to.fullPath` 存进 `query.redirect`，登录后跳回用户原本想去的页面。
  > 权限这块，正确做法是**路由表拆成静态 + 动态**：登录后拿到后端返回的角色，筛出有权访问的路由用 `router.addRoute()` 动态注册，菜单也由这份路由表生成，不用维护两套。**但我很清楚这不是安全方案**——组件代码仍然在打包产物里，攻击者读一读 chunk 就能拿到接口路径和参数结构，直接绕过前端打后端。**所以前端权限控制是体验，真正的权限边界只能在后端，每个接口都要校验。前端做的只是不让用户看见他点了会失败的东西。**」
- **高频追问要点**：
  - **`redirect` 参数有什么风险？（加分）** → **开放重定向（Open Redirect）**：`redirect` 来自 URL 可被篡改成 `https://evil.com`，用户在你的正规域名登录后被弹到钓鱼站。跳转前必须校验它是**站内相对路径**（以 `/` 开头且不以 `//` 开头，`//evil.com` 会被当协议相对 URL）。
  - **`addRoute` 之后为什么要 `return to.fullPath`？** → 本次导航的**匹配发生在守卫执行之前**，刷新页面直接访问 `/admin/users` 时路由还没加，匹配到的是 404；加完路由必须重新触发一次导航，让它拿新路由表重跑。
  - **那这不会死循环吗？** → 不会，正好印证上面的判据：触发重定向的条件是"权限路由还没添加"，而**我在重定向之前已经亲手把这个条件消灭了**，第二轮进不去那个分支——**它是自愈的，所以不需要白名单**。
  - **守卫里发异步请求（拿用户信息）要注意什么？** → 守卫可以是 `async`，Router 会等它，但**等待期间用户停在旧页面、点了没反应**，体验像卡死。① 必须配顶部进度条给反馈；② 必须 `try/catch`，接口挂了要 `return false` 或跳错误页，**否则这次导航永远 pending，页面彻底卡住**。
- **翻车点**：说"不做动态路由的话，用户改 URL 就能进无权限页面"（**不成立**，守卫照样拦得住，真正的危害是**打包产物泄露接口结构**）；把前端权限当成安全措施；死循环归因成"跳到了同一个地址"（那是现象，根因是**重定向目标本身也被拦**）。

---

## 5. 小程序与跨端 🏠

### Q：小程序为什么是双线程架构？这么设计有什么代价？
- **一句话结论**：核心动机是**管控**——让第三方 JS 物理上碰不到界面；代价是把"直接调用"变成了"跨线程通信"。
- **面试口语版话术**：
  > 「小程序把**逻辑层和渲染层拆到两个线程**：你的 JS 跑在 JsCore 沙箱里，界面跑在 WebView 里，**两层不能直接通信，必须经原生层转发**。所以小程序里没有 `document`、没有 `window`——**不是不让你用，是逻辑层那个线程里压根没有这些东西**，想改界面只能调 `setData`。
  > 这么设计主要是**为了管控**。如果小程序的 JS 能像 H5 一样直接碰 DOM、拿到 window，那它就能动态执行任意脚本、绕过微信的管控。**微信作为平台方不可能允许第三方代码为所欲为**，所以用一道物理隔离，把界面这块牢牢握在自己手里，`setData` 是唯一受控的通道。次要好处是**性能隔离**——H5 单线程下 JS 一重算界面就冻住，双线程下逻辑再重也不直接阻塞渲染。
  > 但它**不是免费的**：H5 改界面是一次函数调用，纳秒级；小程序改界面要**序列化数据、跨线程传两跳、再反序列化重渲染**。所以**小程序几乎所有性能问题，根子都在这条通信链路上**——这也是为什么 `setData` 优化是小程序性能的核心命题。」
- **高频追问要点**：
  - **为什么拿不到 `document`？** → 逻辑层运行在无 DOM 的 JS 引擎沙箱里，这是**故意的隔离**，不是 API 没做。
  - **WXML 是不是类似 Vue 的模板编译？** → 是同一个思路：WXML 编译成渲染层的渲染函数、维护一份虚拟节点树，`setData` 传来新数据后做 diff 再更新真实 DOM。**区别在于 Vue 的 diff 和数据在同一线程，小程序的数据要跨线程送过来**——这就是额外成本的来源。
  - **双线程怎么影响开发？** → 拿不到 DOM 尺寸只能用 `createSelectorQuery` 异步查询；纯视图逻辑要用 WXS 才能留在渲染层。
- **翻车点**：说"小程序就是个 WebView"（忽略了逻辑层）；只说性能不说**管控**（管控才是主因）；只讲好处不讲代价。

### Q：`setData` 为什么慢？怎么优化？怎么定位？（🏠 项目主场必考）
- **一句话结论**：**成本 ≈ 单次数据量 × 调用频次**，所以优化只有三条路——少传、少调、不传。
- **面试口语版话术**：
  > 「`setData` 是逻辑层到渲染层**唯一的跨线程通道**，它的成本可以用一个公式概括：**单次数据量 乘以 调用频次**。所有优化手段都是从这个公式里推出来的，就三条路：
  > **第一，少传。** 最典型的反例是列表里改一项就把整个数组 `setData` 回去——几百条数据全量序列化跨线程。正确做法是用**数据路径**，`this.setData({'list[2].done': true})`，只传变化的那个字段，数据量能差几百倍。另外 **`data` 里只放渲染需要的数据**，接口返回的 id、createTime 这些不参与渲染的挂在 `this` 上就行，或者用 `pureDataPattern` 声明成纯数据字段。单次 `setData` 官方上限是 1024KB。
  > **第二，少调。** 经典翻车点是在 `onPageScroll` 里 `setData`，一秒几十次通信必卡。一是加**节流**；二是更省的一招——**先判断状态是否真的变了再 setData**，滚动 100 次里可能只有 2 次是状态翻转，一行 if 就能消灭 98% 的无效通信。
  > **第三，不传。** 像导航栏透明度跟随滚动这种**纯视图逻辑**，根本不需要逻辑层参与，用 **WXS 写在渲染层**就地算完就地改样式，**整个过程零跨线程**，自然丝滑。
  > 不过我想强调的是**定位比优化重要**：我会先在**真机**上量——模拟器不是真双线程环境，测不出跨线程成本；用 **`setData` 的渲染完成回调**量端到端耗时，因为这个回调是渲染层真渲染完才触发的，只在调用前后打点量不到真实链路；再用 `JSON.stringify` 看单次到底传了多少字节。**先分清是"传太多"还是"调太频"，再对症下药**，否则就是瞎改。」
- **高频追问要点**：
  - **为什么要用 `setData` 的回调打点？** → 回调在**渲染层渲染完成后**触发，量到的才是完整链路（序列化+跨线程+diff+渲染）。调用前后打点只量到"发起动作"。**和 AI 场景测 TTFT 是同一个方法论：打错点，数就是假的。**
  - **为什么必须真机？** → 开发者工具模拟器**不是真正的双线程环境**，跨线程成本被抹平，模拟器流畅真机照样卡。
  - **长列表怎么办？** → 分页加载 + 只 `setData` 增量数据（别每次全量替换数组）；再进一步用官方 `recycle-view` 或自己实现虚拟列表，把渲染节点数控制住。
- **翻车点**：只会背"少 setData"却说不出成本模型；在模拟器上测性能；把不参与渲染的数据全塞进 `data`；**把没做过的优化说成做过（诚信问题，追两层必露馅）**。

### Q：讲讲小程序的登录鉴权流程。为什么要绕一道 `code`？
- **一句话结论**：本质是 **OAuth 2.0 授权码模式**——用一次性短时效凭证 + 服务端密钥换身份，让敏感信息永不经过客户端。
- **面试口语版话术**：
  > 「流程是六步：**第一**，小程序调 `wx.login()` 拿到一个临时 `code`，5 分钟过期、只能用一次；**第二**，把 code 发给我自己的后端；**第三**，后端拿 code 加上 AppID、AppSecret 去调微信的 `code2Session` 接口；**第四**，微信返回 `openid` 和 `session_key`；**第五**，后端按 openid 查用户、没有就建，然后**签发自己的 token**；**第六**，前端存下来，后续请求带上。
  > 这里要强调一点：**微信只负责告诉你'这是谁'，登录态是我自己维护的**——token 的有效期、刷新、注销都是我的事，微信不管。
  > **为什么要绕一道 code、不直接返回 openid？** 因为 code 是**一次性、5 分钟过期，而且必须配合只有后端才有的 AppSecret 才能兑换**。关键不在于 code 更难被截获——它和 openid 走的是同一条网络通道，**真正的区别是被截获之后的后果**：code 偷了也换不到东西，很快就废；而 **openid 是永久不变的，如果拿它当登录凭证，泄露一次就等于被永久冒充，既不能过期也没法撤销**。这其实就是安全设计的通则——**假设凭证一定会泄露，然后让泄露不产生后果**。
  > 说到底这就是 **OAuth 2.0 的授权码模式**：code 对应授权码，AppSecret 对应 client secret，敏感信息全程只在'微信服务器到我的后端'这条服务器间通道上传输，从不经过客户端。微信只是把它包装成了 `wx.login`。」
- **高频追问要点**：
  - **三条安全铁律？** → ① **AppSecret 只能在后端**（写进前端等于被反编译后冒充你的小程序）；② **`session_key` 绝不下发给前端**（它是解密用户敏感数据、校验签名的钥匙，下发等于公开钥匙——这是最常见的真实事故）；③ **不能拿 `openid` 当登录态**（`openid` 回答"你是谁"，token 回答"你现在是否已登录"，两件事不能合并）。
  - **`openid` 和 `unionid` 区别？** → `openid` 是用户在**单个**小程序/公众号内的标识，同一个人在你的小程序和公众号里是**不同的**；`unionid` 是同一**微信开放平台账号**下所有应用共享的统一标识，**要打通多端账号必须用它**。
  - **`session_key` 会过期吗？** → 会。用 `wx.checkSession()` 检查——注意它查的是 session_key 是否有效，**和你自己的登录态是两回事**，别混。
  - **怎么获取手机号？（⚠️ 版本敏感）** → 用 `<button open-type="getPhoneNumber">`，在 `bindgetphonenumber` 回调里拿到一个 **`code`（动态令牌）**，后端调 **`phonenumber.getPhoneNumber`** 换取手机号。三个坑：**这个 code 和 `wx.login` 的 code 是两回事不能混用**；同样 5 分钟一次性；**基础库 2.21.2 起不需要提前调 `wx.login`**。旧的 `encryptedData` + `iv` + `session_key` 解密方案已被取代。
- **翻车点**：把 `session_key` 或 `openid` 直接返回给前端当登录态；说 AppSecret 可以放在前端做加密；答手机号还在讲 `encryptedData` 解密（**版本停在几年前**）；说不出"为什么绕 code"（只会背流程）。

### Q：小程序包体积超了 2MB 怎么办？讲讲分包。（🏠 项目主场必考）
- **一句话结论**：三步走——**先定位、再拆分、最后补体验**；本质不是被动瘦身，是按"用户什么时候需要什么"重组加载时机。
- **面试口语版话术**：
  > 「我的思路是三步：**先定位、再拆分、最后补体验**。
  > **第一步定位。** 我不会凭感觉猜哪里大，会用开发者工具的**代码依赖分析**看每个文件的体积占比，先找到真正的大头。通常最大的一块不是代码，是**本地图片和字体**——这些一律上 CDN，往往这一步就能砍掉一大截。同时清掉没用到的组件和第三方库。
  > **第二步拆分。** 判断标准就一条：**这个功能是不是人人一进来就要用？** tabBar 页面、公共组件、工具函数必须留主包；订单详情、设置、活动页这类低频二级页面全部进分包。
  > **第三步补体验。** 分包不是免费的——它**把等待从"启动时"转移到了"首次进入分包时"**，而且这个等待更刺痛，因为用户已经点击了、有明确预期。所以我会配 **`preloadRule` 分包预下载**，利用用户停留在首页的空闲时间把大概率会去的分包提前下好，而且默认只在 WiFi 下预下载，不占用户流量。如果是分享落地页那种用户空降进来的场景，还可以上**独立分包**，让它跳过主包直接启动——代价是不能复用主包代码，要权衡冗余。
  > 所以我的理解是：**2MB 看起来是限制，其实是微信在强制你保证启动性能。** 我的做法不是被动瘦身，而是**按'用户什么时候需要什么'重新组织代码的加载时机**。」
- **高频追问要点**：
  - **分包的三条规则？** → ① **tabBar 页面必须在主包**（启动即展示，不能等下载）；② 分包**可以**引用主包资源，**不能**引用其他分包；③ 主包**不能**引用分包。记法：**主包是地基，分包是楼层——楼层能用地基，楼层之间不通，地基不能依赖还没盖的楼。**
  - **`preloadRule` 的粒度和细节？** → 粒度是**页面级**（"进入首页时预下载活动分包"），不是主包加载完就全拖；`network` **默认只在 WiFi 下**预下载——因为预下载本质是"赌用户会用"，**赌错就是白耗用户流量**。它和浏览器 `<link rel="prefetch">`、路由预取是同一个套路：**在用户产生意图之前，用闲置资源把可能需要的东西备好。**
  - **独立分包解决什么？代价是什么？** → 解决"用户从分享链接**空降**进分包页，却被迫先下载用不到的主包"。代价两条：① **不能引用主包和其他分包**，公共代码要复制一份进去，**拿代码冗余换启动速度**；② **`getApp()` 拿不到实例**（主包未下载、`app.js` 未执行），要用 `getApp({ allowDefault: true })`——**这条很多人是线上报错才发现**。
  - **骨架屏能解决分包首次进入慢吗？** → **不能**。骨架屏解决的是"**数据**还没到"，分包问题是"**代码**还没到"——页面代码都没下载，连渲染骨架屏的代码都没有。
  - **体积上限具体是多少？** → 单个包（主包或任一分包）**2MB**；所有分包总和的上限微信调整过（早期 20MB，后来上调），**面试前以官方文档当前值为准**。
- **翻车点**：答"先分析"却说不出**用什么分析**（开发者工具代码依赖分析）；**只讲权衡不讲缓解**——说"进二级页面会慢，这是权衡"听起来像接受问题，必须跟一句"我用 `preloadRule` / 独立分包补的"；把 tabBar 页面放进分包。

### Q：小程序里几千条的长列表滑动很卡，你怎么优化？（🏠 项目主场必考）
- **一句话结论**：卡顿只有两个来源——**`setData` 的传输量（流量）和渲染节点数（存量）**，要分开定位、分开治。
- **面试口语版话术**：
  > 「先说结论：小程序长列表卡顿基本就两个来源——**一个是 `setData` 的传输量，一个是渲染节点的存量**。这两个要分开定位、分开治。
  > **第一步先量，确认卡在哪一头。** 在**真机**上——模拟器不是真双线程环境，测不准——用 `setData` 的**渲染完成回调**量端到端耗时，用 `JSON.stringify` 看单次传了多少字节。**如果传输量随列表变长而增长，是流量问题；如果传输量恒定但滑动照样掉帧，那就是节点存量问题。**
  > **第二步治流量。** 最常见的坑是触底加载写成 `setData({ list: [...old, ...new] })`——这是全量传输，第 10 页为了追加 20 条要传 200 条，累计是 **O(n²)**，而且**卡顿随使用时长恶化**，最难被测出来。改成数据路径追加，`list[20]`、`list[21]`，永远只传新增那 20 条，传输量和已有长度无关。
  > **第三步治存量。** 流量控住了节点还在涨——几千条数据可能是上万个节点，内存、布局、合成全在恶化，**这跟你传多少数据完全无关**。唯一解法是虚拟列表：只渲染可视区加缓冲，让节点数不随数据量增长。生产上我直接用官方的 `recycle-view`，不自己造轮子。
  > **最后说代价。** 虚拟列表在小程序里不是免费的——**逻辑层拿不到 DOM，量高度只能靠 `createSelectorQuery` 异步往返**。所以不定高方案得靠"预估高度先渲染、实测后回填前缀高度表"，中间有一帧位置未校正的抖动，修正量大了还要反向补偿 `scrollTop`，否则画面会蹿。
  > **所以我的做法是优先用产品约束规避算法复杂度**——能把列表项设计成定高、或者只有两三种固定高度的就尽量这么做。**定高是纯计算、零测量、零抖动，连前缀表和二分都不需要，一次除法就能算出起始索引。**不定高留给确实无法约束的场景，而不是默认选择。」
- **高频追问要点**：
  - **为什么小程序长列表比 H5 难？** → H5 虚拟列表依赖三个前提：同步测高、同步监听滚动、同步改 DOM。**双线程把这三条全打破了**——测高只能 `createSelectorQuery` 异步往返、滚动事件从渲染层跨线程送来天然滞后、更新还要 `setData` 再跨一次。**三重延迟叠加，等于隔着一层玻璃做手术。**
  - **定高和不定高分别怎么算起始索引？（⚠️ 别混为一谈）** → **定高**：`Math.floor(scrollTop / 项高)` 一次除法，**不需要前缀表**；**不定高**：**前缀高度表 + 二分查找**。两者是互斥的两条路，能定高就别上前缀表。
  - **为什么前缀高度表能用二分，原始高度数组不能？** → **二分查找的唯一前提是有序。** 原始高度 `[50,80,60,100]` 完全无序，二分到中间那个值也不知道该往左还是往右；**前缀和必然单调递增**（高度不为负），比一下就知道方向。**所以前缀和真正的价值是"把无序数组变成有序数组"从而解锁二分**，省去重复累加只是顺带的。复杂度从 O(n) 降到 O(log n)，10000 条只要 14 次比较。
  - **不知道高度怎么建前缀表？** → **预估 + 实测回填**：先给经验预估值（如 80px）建表并渲染 → 渲染后 `createSelectorQuery` 异步测真实高度 → 回填并更新前缀和 → 量过的用真值、没量过的继续用预估。**表是边滚边变准的。**
- **翻车点**：**把"定高"和"前缀高度表"并列说**（面试官会判定你背了招式没分清适用条件）；答"先分析"却说不出量化手段；只讲方案不讲代价（**讲权衡必须跟一句"我怎么补的"**）；以为增量 `setData` 能解决节点数问题（**流量 ≠ 存量**）。

### Q：你们为什么用原生小程序，不用 uni-app / Taro？（技术选型题 ⭐）
- **一句话结论**：**跨端框架的全部价值来自"跨端"两个字**——只投一端时，多一层抽象就是纯负担。
- **面试口语版话术**：
  > 「因为我们只投微信一端。**跨端框架的价值 100% 来自"跨端"这两个字**——一旦只做一端，多出来的那层抽象就是纯负担：有性能损耗、新 API 要等框架适配、而且**出问题时报错在编译产物里不在你写的代码里**，排查链路长一截。这些代价我们全付了，却什么都没换回来。
  > **反过来，如果确定要投微信、支付宝、抖音、App 四端，那这些代价换回的是四份代码变一份，怎么算都划算**——那种情况我会毫不犹豫选 uni-app 或 Taro。
  > 不过我们也没有把路堵死：**架构上我们把 `wx.*` 的调用统一封装在一层适配层里，业务逻辑不直接依赖平台 API**。这样当下拿到的是原生的性能，未来真要多端时，要改的是适配层而不是整个业务代码。**我的原则是——面对不确定的未来，不提前下注，而是降低将来改主意的成本。**」
- **高频追问要点**：
  - **"我们以后可能要多端，所以现在先上 uni-app 为未来做准备"——这个决策有问题吗？** → 问题全在"**可能**"两个字。**成本是从第一天起天天付、100% 确定的；收益是未来某个不确定时点、"可能"才兑现的。** 用确定的持续成本买不确定的未来收益，是典型的过度设计。**资深工程师的第一反应应该是澄清确定性**——"以后是多久？有排期吗？谁承诺的？"：有明确路线图 → 提前上跨端是对的；只是"老板说可能" → 先原生 + 适配层隔离。
  - **选型该怎么起手？** → 先定三个约束再看方案：**① 要投几端（决定性）② 性能敏感吗 ③ 团队是 Vue 还是 React**。约束定了方案基本唯一。别一上来就比框架参数。
  - **uni-app 和 Taro 怎么选？** → **uni-app**：Vue 为主，DCloud 生态，**国内小程序端覆盖最全**、插件市场和国内 SDK 适配好、上手门槛低；**Taro**：React/Vue 都支持，**更适合复杂系统架构与工程化**，若想和 Web 端（Next.js 等）最大化复用业务逻辑和 hooks，Taro + React 更顺。
  - **H5 内嵌 WebView 呢？** → 复用性最好、上手最低，但**性能最差、原生能力受限**，适合营销页/活动页这类"看完就走"的长尾功能，不适合核心高频模块。
- **翻车点**：答"公司定的""一直这么用"（**这轮直接废**）；只会列框架参数不谈约束；把"可能要多端"当成"确定要多端"；说不出跨端框架的**具体**代价（性能/调试看编译产物/新 API 滞后）。

### Q：小程序怎么做移动端适配？rpx 是什么？
- **一句话结论**：**`rpx` 就是微信内置的 `vw`——`750rpx ≡ 100vw`**，把换算从开发者手里拿走了。
- **面试口语版话术**：
  > 「核心问题是：设计稿是固定宽度（一般 750px 的二倍图），但设备宽度从 320 到 430 都有，要让同一套代码在所有屏上看起来一样。
  > 这套方案是演进来的：最早写死 `px` 完全不适配；后来用 **`rem`**，但它**必须靠 JS 动态设置根元素 font-size**，依赖 JS、有闪烁风险；再后来 H5 转向 **`vw`**，纯 CSS 不需要 JS，缺点是要自己换算。
  > 而小程序直接给了 **`rpx`**——**微信规定屏幕宽度恒为 750rpx**，所以它本质上就是内置的 `vw`，`750rpx` 恒等于 `100vw`。它的全部价值在于：设计稿是 750px 宽时，**标注多少你就写多少，零换算零心智负担**。
  > 但要注意 **`rpx` 做的是等比缩放，不是响应式布局**——它隐含假设"所有设备只是同一份设计的不同尺寸"。在手机区间成立，但到了 iPad 这种大屏，750rpx 依然拉满整屏，所有元素被等比放大两三倍，字巨大、留白巨多。**而大屏用户要的是"看到更多内容"，不是"更大的同样内容"**，这是布局问题，等比缩放解决不了。所以大屏要给关键容器加 `max-width`（用 px 不用 rpx），或者单独做布局。」
- **高频追问要点**：
  - **1px 边框为什么会变粗？怎么解？** → 根因是**两种像素错位**：设计稿的 1px 是**物理像素**，CSS 的 1px 是**逻辑像素**，在 DPR=2/3 的屏上会被渲染成 2~3 个物理像素。H5 常用 `transform: scale(0.5)` 配伪元素、`border-image`、或 `0.5px`（iOS 支持、部分安卓不认）。**小程序里 `rpx` 顺手解决了一半**——375pt 宽、DPR=2 的设备上 `1rpx = 0.5 逻辑像素 = 正好 1 个物理像素`。
  - **安全区怎么适配？（⚠️ 版本敏感）** → 优先用 CSS 的 `env(safe-area-inset-bottom)`；需要动态计算时用 **`wx.getWindowInfo()`** 取 `safeArea`。⚠️ **`wx.getSystemInfo` / `wx.getSystemInfoSync` 已废弃**，拆成了 `getWindowInfo` / `getDeviceInfo` / `getAppBaseInfo` / `getSystemSetting`。典型翻车：**底部固定操作栏被 Home Indicator 盖住，按钮点不到。**
  - **iOS 和安卓有什么差异？** → 小程序抹平了大部分，但**原生组件仍有分歧**：`input`/`textarea`/`picker` 的表现、键盘弹起时的视口行为（iOS 顶起页面、安卓可能压缩视口）、安卓上 `position: fixed` 配键盘的错位。**必须真机双端各测一遍。**
- **翻车点**：说 `rpx` 是"响应式"（它是**等比缩放**）；安全区还在答 `wx.getSystemInfo`（**版本停在几年前**）；答 1px 边框只会背 `scale(0.5)` 说不出物理像素/逻辑像素的根因。

---

## 9. AI 应用前端

### Q：AI 聊天的流式输出，为什么常用 SSE 而不是 WebSocket？
- **一句话结论**：选型要匹配场景——AI 对话本质是服务器单向吐字，SSE 刚好够用、还自带重连、又能复用整个 HTTP 生态；WebSocket 的全双工是过剩能力。
- **面试口语版话术**：
  > 「先说本质：**SSE 就是一个不结束的 HTTP 响应**，服务器保持连接持续往里写数据，单向；WebSocket 是通过 HTTP 握手升级出来的**独立协议 ws://**，全双工双向。
  > AI 聊天的场景是：用户发一次问题，然后服务器**源源不断单向推 token**——这就是个单向流，**SSE 的能力刚好完全匹配，WebSocket 的双向在这里是浪费**。而且 SSE 有几个实打实的好处：一是**浏览器原生自带断线重连**，WebSocket 得自己写；二是它**就是 HTTP**，天然穿代理、防火墙、CDN，还能复用鉴权、HTTP/2 多路复用这些现成基建；三是实现简单，不用维护连接状态、心跳这一整套。
  > 所以我的判断是：**能用简单方案就别上重的**。只有像协同编辑、实时对战、双向音视频信令那种真需要双向低延迟的场景，才值得上 WebSocket。」
- **高频追问要点**：
  - **单向的 SSE，用户怎么发问、怎么点停止生成？** → 发问走**普通 POST**；停止用 **AbortController**（`fetch(url,{signal})` → `controller.abort()`，后端能感知连接断开、停止生成省 token）。
  - **生产里用原生 EventSource 吗？** → 一般**不用**。原生 EventSource **只能 GET、不能带 body/自定义 header**，而 AI 要 POST 对话历史 + 带鉴权头。所以生产用 **fetch + ReadableStream 手动读流**；代价是自动重连、`Last-Event-ID` 断点续传要自己实现。
  - **只能传文本是缺点吗？** → 不是。**大模型输出本身就是文本 token**，SSE 传文本天然对齐、无需转换；二进制在纯文本对话里没用武之地。
  - **SSE 有什么坑？** → HTTP/1.1 下浏览器**每域名并发连接约 6 个**，每条 SSE 长连接占一个，开多了会阻塞同域名其他请求。解法：上 **HTTP/2** 靠多路复用，或控制连接数。
- **翻车点**：说"SSE 不能自动重连"（**恰恰相反，原生自带**）；说"SSE 是 WebSocket 的简化版"（是不同协议，SSE=HTTP、WS=独立协议）；只会背"WS 全双工"却答不出"这个场景为什么不需要全双工"。

### Q：SSE 流式对话断线了怎么办？怎么做重连和断点续传？
- **一句话结论**：客户端记 `Last-Event-ID` 报进度、指数退避重连；能不能续取决于**服务器有没有缓存已生成的输出**（AI 生成不可重放）。
- **面试口语版话术**：
  > 「因为我们用 fetch 读流、放弃了原生 EventSource 的自动重连，所以要自己实现。机制是：**客户端持续记住收到的最后一个 event id**，断线重连时通过 `Last-Event-ID` 请求头带给服务器，服务器从下一个 id 接着发，做到不重复不丢字。重连间隔用**指数退避加随机抖动**——1s、2s、4s 逐次翻倍、设上限、加 jitter，避免把抖动中的服务器打垮、避免所有客户端同时重连造成风暴。
  > 但**关键的一半在服务器**：它必须把已经生成的内容**缓存下来**（比如存 Redis），否则续不了。这在 AI 场景尤其重要——**大模型是现生成的，你没法让它从第 1026 个 token 重新接着生成**，生成过程不可重放，所以只能靠服务器缓存已吐出的 token 来补发。
  > 另外要区分**主动中止和意外断线**：用 AbortController 主动 abort 会抛 AbortError，这种不重连；只有网络错误才走重连。UI 上重连失败要**保留已收到的半截回答**、给个手动'重新生成'按钮兜底。」
- **高频追问要点**：
  - **服务器凭什么能续？** → 它必须**缓存/持久化已生成的输出**（以会话/消息 id 为 key）；AI 生成不可重放，这是硬要求。
  - **为什么用指数退避？** → 立刻死命重连会在服务器抖动时雪上加霜；退避 + jitter 避免重连风暴。
  - **abort 和断线怎么区分？** → 自己设 `isManualAbort` 标志，abort 前置 true，catch 里看到就直接 return 不重连。
- **翻车点**：只答"客户端带 Last-Event-ID"、漏掉"服务器要缓存输出"（这是这题考点）；主动中止也去重连。

### Q：AI 流式对话，前端重点监控什么性能指标？
- **一句话结论**：**总耗时那段时间用户是在「读」不是在「等」**，所以要拆成 TTFT 衡量响应性、TPS 衡量流畅度。
- **面试口语版话术**：
  > 「AI 场景不看普通页面的 LCP，看两个专属指标：**TTFT 首字延迟**——从发请求到吐出第一个字；和 **TPS**——首字之后每秒吐多少字。
  > 为什么不看总耗时？因为**总耗时在 AI 场景根本不是一个有意义的指标**——它主要由输出长度决定，回答越长总耗时越长，但**那段时间用户是在「读」，不是在「等」**。整条链路里唯一的真空白等待期，就是首字之前那一段。所以 **TTFT 直接等价于用户感知到的卡顿**，哪怕总共要 10 秒，只要 0.5 秒就开始逐字蹦，焦虑感就大幅下降。
  > 更关键的是这两个指标必须**正交拆开**，因为**优化手段完全不同**：TTFT 慢就去查检索、Prompt 拼装、模型排队；TPS 慢就去调模型和渲染批量。**混成一个数，出了问题根本没法归因。**
  > 前端测法：发请求前打 t0，**读到第一个非空 content 增量**时打 t1，差值就是 TTFT。」
- **高频追问要点**：
  - **t1 为什么不能打在"第一个 chunk"上？** → 首包常常是空的（OpenAI 兼容协议首包只有 `delta:{role:"assistant"}`，还有 `: keep-alive` 心跳注释包）。**必须打在第一个非空 content 增量上**，否则测出来系统性偏小，监控白做。
  - **TTFT 突然从 800ms 涨到 3s，怎么定位是网络还是模型？** → 用 `PerformanceObserver` 把 TTFT 再拆成**网络握手** + **服务端首字**（含 RAG 检索、Prompt 拼装、模型排队）两段。
  - **打时间戳用 `Date.now()` 吗？** → 不。**凡是测"时间差"一律用 `performance.now()`（单调时钟）**，`Date.now()` 是墙上时钟，会被改系统时间/NTP 校正影响，理论上可能倒退导致负数耗时。`Date.now()` 只用于记"发生在什么时刻"。
  - **生成速度慢怎么办？** → 前端可加"匀速输出"缓冲，把涌入的 token 排队匀速吐，稳定打字机节奏。
- **翻车点**：拿 LCP/FCP 套 AI 场景；只会说"越快越好、用户感觉快"（**表层归因，答不出"用户在读不在等"**）；把 TTFT 和 TPS 混成一个指标。

### Q：流式输出 Markdown，逐字渲染会遇到什么问题？怎么解决？（AI 岗必考 ⭐）
- **一句话结论**：大模型逐字吐，Markdown 却要完整结构才能正确解析——四层坑：chunk 边界、语法截断、性能、安全。
- **面试口语版话术**：
  > 「核心矛盾是：模型一个字一个字吐，但 Markdown 要看到完整结构才能正确解析，拿残缺输入实时渲染必然出问题。我按四层处理：
  > **第一，chunk 边界**——ReadableStream 的 chunk 是按网络切的，一条 SSE 消息可能被切断、多字节中文可能被截半。所以要用 buffer 累积、按 `\n\n` 切完整消息，并用 **TextDecoder 的 stream:true** 处理截断的多字节字符防乱码。
  > **第二，Markdown 语法截断**——比如 ` ```co ` 代码块没闭合、`**` 粗体没配对，直接解析会乱渲染、会闪烁。解法是**缓冲住结尾不完整的语法单元、等闭合再渲染**，或用**容忍残缺的流式解析器做自动补全**。代码块最难，因为它作用域大、未闭合会吞掉后面全部，而且块内 Markdown 不转义、同一个字符含义未定。
  > **第三，性能**——别每来一个字就全量重解析，那是 O(n²) 会卡死主线程。用 **requestAnimationFrame 批量渲染**、只增量解析新增尾部。
  > **第四，安全**——转成 HTML 用 v-html 前**必须过 DOMPurify 消毒**，因为内容来自大模型，可能被提示词注入诱导输出恶意脚本，不消毒就是 XSS 漏洞。
  > 最后体验上做智能自动滚动：贴底就跟随，用户上滚就停。」
- **高频追问要点**：
  - **代码块为什么最难？** → 作用域大（未闭合吞后文）+ 块内 Markdown 不转义（`**` 在不在代码块里含义相反，流式时状态未定）。
  - **批量渲染的副作用？** → 打字机顺滑感丢失、文字一坨坨蹦；平衡点是每 16~33ms（30~60fps）渲一次，或加匀速输出缓冲。
  - **v-html 的风险？** → XSS。Markdown→HTML 若含 `<script>`/`<img onerror>` 会执行；必须先 DOMPurify 白名单过滤。AI 场景是刚需（提示词注入）。
- **翻车点**：以为 chunk = 完整消息；不消毒直接 v-html；说批量渲染"变慢"（实为不连贯）。

### Q：讲讲 RAG 是什么？前端在 RAG 链路里做什么？（前端岗最小可用版 ⭐）
- **一句话结论**：RAG 就是给大模型开卷考试——先检索出参考材料，再让它照着答。
- **面试口语版话术**：
  > 「**RAG 就是给大模型开卷考试**。纯大模型是闭卷，只能靠参数里背过的东西，记不清就开始编，这就是幻觉；RAG 是在提问之前，先从知识库里**检索出相关片段，拼进 Prompt 当参考材料**，让它照着答。而微调相当于'重新上一遍学'，成本高、周期长，学完还是闭卷。
  > 好处有四点：**知识能实时更新**，不受模型训练截止日期限制；**能支持企业私域文档**；**能给出引用出处**，用户可以核对，幻觉大幅下降；还有很关键的一点——**知识存在检索层而不是模型参数里，所以可以带着用户身份做权限过滤**，A 部门检索不到 B 部门的文档。这是微调根本做不到的，微调进参数的知识没法按用户隔离。
  > **前端在这条链路上主要做三件事**：第一是**文档上传和解析进度反馈**——建索引是分钟级的慢操作，得设计任务状态推送，不能让用户干等；第二是**引用溯源 UI**，把答案里的角标做成可点击跳原文并高亮，这是用户敢信 AI 答案的前提，是 RAG 产品的信任核心；第三是**分阶段状态反馈**——RAG 因为多了检索和重排，**首字延迟比纯对话长 1 到 2 秒**，前端必须用『正在检索知识库…』『已找到 5 篇相关文档』这类中间态把等待感填掉。**这一条本质是用产品手段掩盖物理延迟，是纯粹的前端价值。**」
- **高频追问要点**：
  - **RAG 和微调怎么选？** → 知识频繁更新、需要溯源、需要权限隔离 → RAG；要改变模型的**风格/语气/输出格式**或领域表达习惯 → 微调。**RAG 补知识，微调补能力**，不是二选一。
  - **完整链路是什么？** → 分两条：**离线索引**（文档解析 → 切块 → 向量化 → 存向量库）；**在线检索**（query 改写 → 向量化 → 检索 top-k → 重排 → 拼 Prompt → 流式生成 → 返回答案+引用）。
  - **切块 chunk 多大合适？** → 权衡：**太小语义不完整**、缺上下文；**太大则一个向量要代表多个主题，语义被平均掉，检索精度反而崩**。工程上用**父子块**化解——小块做检索保证精准，命中后返回父块给模型保证上下文完整。
  - **只用向量检索够吗？** → 不够。**向量检索对型号/编号/专有名词天生弱**（`XR-2000` 几乎没有语义，和 `XR-3000` 在向量空间里几乎重合），生产标配是 **Hybrid Search**：BM25 关键词精确匹配 + 向量语义检索，用 RRF 融合排名。
  - **RAG 效果不好怎么优化？** → 第一步不是改代码，是**建评估集拿到量化基线**（"答不准"是主观描述，不可优化）。然后**把召回的片段打印出来看**做二分定位：片段里没答案 → 检索层问题；片段里有答案却答错 → 生成层问题；再加一层容易被忽略的**数据层**——PDF 表格/双栏排版解析就错了，垃圾在源头。
- **翻车点**：说 RAG 是微调的一种；说 embedding 和生成用同一个模型（是两个独立模型）；**忘了 query 和文档必须用同一个 embedding 模型**（不然是两套坐标系，相似度纯噪音）；答"前端就是调接口显示结果"（这轮直接废）。

---

## 12. 手写题

### Q：手写防抖 debounce（含 immediate、cancel）
- **一句话结论**：三个考点——**判断早于清理**、**timer 一身二职**、**前沿执行不能提前 return**。
- **面试满分版代码**：
  ```js
  function debounce(fn, wait, immediate = false) {
    let timer = null
    let result

    const debounced = function (...args) {
      // ① 必须在 clear 之前判断——此刻 timer 才真实反映"是否在冷却期"
      const callNow = immediate && timer === null

      if (timer) clearTimeout(timer)

      // ② 每次触发都重设定时器：它既是"后沿执行器"，也是"冷却期结束信号"
      timer = setTimeout(() => {
        timer = null                                   // 冷却期结束，状态复位
        if (!immediate) result = fn.apply(this, args)  // 箭头函数继承 this
      }, wait)

      // ③ 前沿执行放在设完定时器之后，保证冷却期一定被建立
      if (callNow) result = fn.apply(this, args)

      return result
    }

    debounced.cancel = function () {
      clearTimeout(timer)
      timer = null
    }

    return debounced
  }
  ```
- **讲解话术（写完主动说，加分）**：
  > 「这里有三个容易翻车的点。**第一，`callNow` 必须在 `clearTimeout` 之前算**——因为 `timer` 在 immediate 模式下身兼二职，既是定时器句柄，又是『是否处于冷却期』的标志位，先清掉判断依据就没了。**第二，标志位的复位时机在定时器回调里**，不是在清理时，因为冷却期是到点才结束的。**第三，前沿执行要放在设完定时器之后**，否则提前 return 会导致冷却期根本没被建立，防抖直接失效。」
- **高频追问要点**：
  - **`cancel` 为什么挂在函数上而不是返回值里？** → 真实用法是 `onUnmounted(() => debounced.cancel())`，组件卸载时可能压根没触发过；挂返回值等于必须先触发一次才能拿到 cancel，而且会占掉 `fn` 返回值的位置。
  - **`return result` 有什么问题？** → **签名在撒谎**。非 immediate 模式下 `fn` 是延迟执行的，调用那一刻 `result` 装的是**上一次**的结果（首次是 undefined）。同步返回值和延迟执行在类型层面就矛盾——诚实的做法是**返回 Promise**。
  - **返回 Promise 后，被"抖掉"的调用怎么办？** → 三种设计：① **resolve 成最终那次的结果**（业务默认选它，搜索场景调用方本来只关心最新）；② **永远 pending**（❌ 内存泄漏——`await` 它的 async 函数永不恢复，闭包和组件实例都回收不掉，而且这是最容易"不小心"写出来的）；③ **reject 一个 `CancelledError`**（语义最诚实，但每个被抖掉的调用都产生 rejected promise，不 catch 就满屏 unhandledrejection）。
  - **③ 眼熟吗？** → 这就是 `AbortController` 的 `AbortError` 模式。**"正常的取消"被塞进了异常通道**，因为 Promise 只有 resolve/reject 两个出口，没有第三个"作废"出口——这是语言设计约束，不是 API 设计失误。
  - **防抖 vs 节流一句话？** → 防抖是**等你停下来才做**（搜索联想、表单校验）；节流是**固定频率做**（滚动、resize、拖拽）。
- **翻车点**：`setTimeout` 里用普通 function 导致 `this` 丢失（必须用箭头函数）；`clearTimeout` 后不置 `null`（clearTimeout 不会改变量的值）；immediate 分支忘记 `return`/`else` 导致 fn 执行两次；**只在脑子里过一遍就说"写完了"——必须自己跑一遍时序表（t=0 / t=50 / t=wait）验证**。

### Q：手写 LRU 缓存（`get` / `put` 都要 O(1)）
- **一句话结论**：**用 `Map`**——它是 JS 里唯一同时具备"记住插入顺序 + O(1) 增删查"的结构；**`delete` 后 `set` 回去就是"移到末尾"**。
- **面试满分版代码**：
  ```js
  class LRUCache {
    constructor(capacity) {
      this.map = new Map()
      this.max = capacity
    }
    get(key) {
      if (!this.map.has(key)) return -1
      const value = this.map.get(key)
      this.map.delete(key)          // 删了再塞回去 = 移到末尾（标记为刚用过）
      this.map.set(key, value)
      return value
    }
    put(key, value) {
      if (this.map.has(key)) this.map.delete(key)   // 已存在先删，保证 set 后落到末尾
      this.map.set(key, value)                      // 先放进去
      if (this.map.size > this.max) {               // 再看超没超（注意是 >）
        this.map.delete(this.map.keys().next().value)   // 迭代器第一个 = 最久未使用
      }
    }
  }
  ```
- **讲解话术（写完主动说，加分）**：
  > 「关键是**选对数据结构**：普通对象不保证键顺序、数组删中间是 O(n)，**只有 `Map` 同时满足『记住插入顺序』和『O(1) 增删查』**。
  > 第二个点是——**Map 没有『把某个 key 移到末尾』的 API，所以用 `delete` 再 `set` 来实现移动**，这是这题最核心的技巧。
  > 第三个点是**容量检查放在 `set` 之后**。如果放在之前，你还不知道这次 put 到底是新增还是更新——是更新的话容量根本没变，却会误淘汰一个无辜的 key。**先做再看结果，比先预测再做少一层推理。**」
- **高频追问要点**：
  - **为什么是 `keys().next().value`？** → `keys` 是**方法不是属性**，且返回的是**迭代器不是数组**，不能 `[0]` 取值。迭代器的第一个就是插入最早、也就是最久未使用的 key。
  - **不用 Map 怎么做？** → 经典解法是 **哈希表 + 双向链表**：哈希表管 O(1) 查找，双向链表管顺序和 O(1) 移动/删除。**`Map` 本质上就是内置了这套。** 能说出这句，说明你知道 Map 为什么能 O(1)。
  - **`get` 也要更新顺序吗？** → **要**。LRU 是 Least *Recently Used*，"用过"包括读——读了就算最近使用过。**这是最常被漏掉的一条。**
  - **哪里还见过 LRU？** → **Vue 的 `keep-alive` 的 `max`** 就是 LRU 淘汰；浏览器缓存、CDN、数据库缓冲池也都用它。
- **必测用例清单（自己写完先跑一遍）**：
  | 用例 | 测什么 |
  |---|---|
  | `get` 不存在的 key | 返回 -1 |
  | `get` 存在的 key | 返回值 **+ 顺序被更新** |
  | `put` 新 key 未超容量 | 正常插入 |
  | `put` 新 key 超容量 | 淘汰最旧 |
  | **`put` 已存在的 key** | **不该淘汰任何人** ← 最常漏 |
  | **`get` 后再 `put` 触发淘汰** | **验证 get 真的影响了淘汰顺序** ← 也常漏 |
- **翻车点**：`this.map.keys[0]`（keys 是方法、返回迭代器，写错会导致**淘汰完全失效、缓存无限增长**）；`get` 未命中忘了 `return -1`；**`get` 时不更新顺序**；**容量检查放在 `set` 之前**导致更新已有 key 时误淘汰。
