# 2026-07-21 会话记录
> 当日共 1 次会话 ｜ 累计约 60 分钟 ｜ 第 2 周

---

## 第 1 次会话 ｜ 时长约 60 分钟
- **本次内容**：
  - 开场滚动复习 3 题（fetch vs EventSource / 六连接坑 / 嵌套 computed 拉模型）。
  - **SSE 实战链路（一）可靠性**：断线重连 + 断点续传（`Last-Event-ID`）、指数退避 + jitter、主动 abort vs 意外断线、TTFT 首字延迟监控。
  - **SSE 实战链路（二）流式渲染**：chunk 边界（buffer + `\n\n` 切分 + TextDecoder stream:true）、Markdown 标签截断（缓冲未闭合 / 容忍性解析器自动补全）、性能（rAF 批量渲染避免 O(n²)）、自动滚动、DOMPurify 防 XSS。
  - **SSE 大模块结业**。
- **表现评价**：
  - **滚动复习 3/3 全过**：Q3"拉模型"这次说出来了（上次差这个词）。Q2 六连接坑把严重性说轻了（"响应慢"→ 已纠正为"长连接占满连接池、其他请求被阻塞卡死"）。
  - **可靠性追问 2/3**：Q2（主动 abort vs 断线要区别对待）✅；Q3（UI 兜底）思路对，补充"保留半截内容 + 手动重试"；**Q1 只答客户端、漏了服务器端关键**——没意识到"服务器必须缓存已生成输出才能续传，且 AI 生成不可重放"。**归因不完整的老毛病复现（只答了看得见的一半）**。
  - **流式渲染追问 1/3**：Q3（v-html 的 XSS 风险）✅ 直接命中，补 DOMPurify + 提示词注入；Q1（代码块为何最难）答"不会"，已讲透（作用域大 + 块内不转义）；Q2（批量渲染副作用）沾边但不准——说成"慢一点"，实为"打字机顺滑感丢失、一坨坨蹦"。
  - **整体**：从摸底 Q9/Q10 全"不会"到能复述完整 SSE 链路，AI 差异化模块实质成型。
- **新增薄弱点**：AI 断点续传的服务器端要求；代码块流式渲染为何最难（均已同步 progress.md）。
- **修复的薄弱点**：SSE 可靠性链路 + 流式渲染（✅ 结业）；嵌套 computed 拉模型（✅ 复查通过）。

---

## 📌 下次计划（当日最终版）
1. **开场滚动复习**：① AI 断点续传服务器端要什么（缓存已生成输出、生成不可重放）；② 代码块为何最难流式渲染（作用域大 + 块内不转义）；③ 快问 TTFT 是什么、怎么测。
2. **续 AI 模块，二选一**：
   - **A｜RAG 全流程**（多模态那题已埋钩子：文档抽取→切块→向量化→检索；前端在链路中的角色）——性价比高，AI 岗高频。
   - **B｜Function Calling / MCP 原理 + Agent loop**——把"前端+AI"讲成体系。
3. 备选换脑：回 Vue（watch/nextTick 原理）或开 JS 手写题（防抖/节流，手写清单目前 0 题，需尽早开工）。
4. 提醒：手写题清单 9 题全"未写"，第 2 周需穿插开始，别只堆概念。
