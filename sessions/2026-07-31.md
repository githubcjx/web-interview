# 2026-07-31 会话记录
> 当日共 2 次会话 ｜ 累计约 145 分钟 ｜ 第 3 周
> 〔本次会话原被误记入 `sessions/2026-07-29.md` 作「第 2 次会话（同日续接）」，2026-07-31 依据 commit fa8b1ad @ 07-31 15:48 核实后移出。**教练同型错误第二次：写日志/追加会话前必须先跑 `Get-Date`，"续接"不等于同一天。**〕

---

## 第 1 次会话 ｜ 时长约 40 分钟
- **本次内容**：
  - **教学纪律调整（他本人提议，已采纳并写入 progress.md）**：① 一次只问 1 题，答完追问、追问完再下一题；② 同一天刚讲的内容不做滚动复习（测短期记忆，无留存价值），只复习上次及更早的内容。
  - **小程序分包加载**（主线，全程一次一题）：
    - 基础：2MB 单包硬限制的真实动机（**微信在强制你保证启动性能**，不是小气）；分包 = 把"启动全量下载"改成"按需下载"；三条规则（tabBar 必须在主包 / 分包可用主包不可用其他分包 / 主包不能引用分包）；拆分判据"**是不是人人一进来就要用**"；图片上 CDN。
    - **分包预下载 `preloadRule`**：触发粒度是**页面级**（进入 A 页面时预下载 B 分包），`network` 默认仅 WiFi（预下载是"赌用户会用"，赌错就是白耗流量）；归类到前端预加载通用套路（prefetch / 路由预取 / 图片预加载）。
    - **独立分包 `independent: true`**：动机（分享落地页用户空降，不该被迫下载用不到的主包）；两个代价——**不能引用主包/其他分包**（要用就得复制，拿代码冗余换启动速度）、**`getApp()` 拿不到实例**（主包未下载、app.js 未执行，需 `getApp({allowDefault:true})`，线上易踩）。
    - **话术训练首次开展**：模拟"包体积超了 2MB 怎么处理"，让他自组织 1-2 分钟面试口语版，逐条打磨。
  - 查证：单包 2MB 上限已确认；**所有分包总上限未能从官方文档确认**（微信调整过，早期 20MB 后上调），已如实告知不给未验证的数字，让他面试前自行核对。
- **表现评价（本段状态很好）**：
  - **Q1（分包引入什么新问题）半对**：答出"首次进入分包会慢"✅，但答不出缓解机制。已升级表述为"**分包没有消除等待，只是把等待从启动时转移到首次进入时**，且更刺痛——用户已表达意图却得不到响应"。
  - **Q1 追问走偏一次**：提示"手机闲着怎么利用"后答"先渲染一部分再慢慢填充"（骨架屏）。已纠正一个有价值的分界：**骨架屏解决"数据还没到"，分包问题是"代码还没到"——连渲染骨架屏的代码都没有，无米之炊**。给"预"字提示后即答出**预加载** ✅。
  - **Q2（分享链接空降进分包页）✅ 完全正确，且自己推出了设计动机**——"要下主包+分包，不合理，应该只下分包资源"。**这是他第二次自主推导出机制的存在**（上次是主动做 WXML/Vue 类比），知识串联的突破在延续。
  - **话术训练 7/10**：结构完整（定位→手段→权衡）✅、**主动讲权衡** ✅（这是长期训练目标，自发做到了）、图片上 CDN ✅。三个缺口：
    - ① **"先分析"说不出分析手段**（应答"开发者工具的代码依赖分析"）——**今天第二次同型缺口**（setData 定位也是"有先定位的直觉、说不出工具"），已明确警告别有第三次。
    - ② **最关键：讲了权衡却没讲缓解**。"进二级页面会慢，这是权衡后的决定"在面试里减分，听起来像接受问题而非解决问题——**而 preloadRule / 独立分包正是刚学的还账手段**。已立通用话术规则：**讲权衡永远要跟一句"我怎么补的"**。
    - ③ 缺"哪些必须留主包"这类体现真配过的细节。
  - 已产出打磨版话术（在他的骨架上改，结构未动），升华落点：**"2MB 看似限制，实为强制保证启动性能；我不是被动瘦身，而是按'用户什么时候需要什么'重新组织代码的加载时机"**。
- **新增薄弱点**：「有定位直觉但说不出定位工具」已升级为**跨模块的稳定缺口**（setData + 包体积两次）；讲权衡不跟缓解手段。（已同步 progress.md）
- **修复的薄弱点**：无（本段为新模块推进）。

---

## 第 2 次会话（同日续接）｜ 时长约 105 分钟
- **本次内容**：
  - **归档纠错（教练侧同型错误第二次）**：核实系统日期后发现，「分包加载」那次会话被误记为 `sessions/2026-07-29.md` 的第 2 次会话，实际发生于 07-31（依 commit fa8b1ad @ 07-31 15:48）。已拆分为独立的 `sessions/2026-07-31.md`，并回填 07-29 的原版「下次计划」；progress.md 内 6 处日期归因一并更正。**已立硬性规则：写日志/追加会话前先跑 `Get-Date`，"续接"≠ 同一天。**
  - **滚动复习 3 题（一次一题，全部为欠账题）**：① setData 卡顿的具体定位手段；② 用"假设凭证一定泄露"框架重答为什么绕 code；③ TTFT vs 总耗时。
  - **⭐ 新教学工具：时序表（timing table）**。他主动问"时序表怎么写"（我要求过两次却从未讲解——教练疏漏）。已补讲方法论：**每个状态变量占一列 / 每行是一个时间点事件（外部调用 + 定时器到期两类）/ 定时器到期必须单独占一行 / 最后一列直接回答"我要验证什么"**；先用一段与防抖无关的批量收集器代码做示范，再落到他自己的代码上。
  - **防抖闭卷默写**（四轮迭代至正确）。
- **表现评价**：
  - **滚动复习 3/3 全过**，且三条全部从薄弱点清单移出：
    - Q1（setData 定位）**①② 一次答对**（真机 / setData 前 + 渲染完成回调），这是连续两次栽的地方，今天补上；③ 起初答"还要量渲染耗时"（错，渲染已含在①②区间内），经两问引导后**自己从成本公式反推出"数据量和调用次数"** ✅。已钉话术：**耗时是症状，数据量和频次才是病因**。
    - Q2（为什么绕 code）先只答了 code 那半边，提示"对比"后补出"**openid 永久不变，泄露就能一直冒充**" ✅。
    - Q3（TTFT）**自己说出关键句"用户读的也没那么快"** ✅；教练补齐 TTFT/TPS 正交拆分那一层。
  - **防抖：四轮才对，且暴露了最严重的一次同型复发**：
    - 第 1 版（本次）：非 immediate 分支 ✅ **首次写干净**；immediate 分支**仍在 `if (!timer)` 之前调 `cancel()`** → 判断依据被抹掉 → fn 每次都执行。**与 07-24 第 2 版是同一个 bug 换了变量名（第 3 次）。**
    - 第 2 版：按提示把 `callNow` 提到 `cancel()` 之前 → **代码正确** ✅。
    - 第 3 版：教练说"`if (!timer)` 是冗余判断，直接写赋值即可"，他**把 `if` 和 `setTimeout` 一起删了** → immediate 模式再次失效。**「照建议改但改过头」第 3 次复发。** 已再次强调对策并具体化为：**删任何一行前先问"这行在维持什么"**（那行 `setTimeout` 维持的是"冷却期"这个概念本身）。
    - 第 4 版：**自己改对并跑表验证** ✅。最终版已确认正确、无冗余。
  - **时序表 2.5/3**：t=50 / t=80 两行完全正确，**t=80 写对了 T2**（上一轮误写 null），口径已统一；t=0 那行填了"callNow=false 但 fn 执行"——**两列自相矛盾而未察觉**。已补最后一条心法：**表要自洽，矛盾本身就是信号**（这次是填错，下次可能就是代码有 bug 被抓到）。
- **教练自省**：① 日期同型错误第二次，已立硬规则；② **"跑时序表"要求了两次却从未教过怎么跑**——这是要求而非教学，他今天主动发问才补上。**今后凡提出方法论要求，必须同时给方法。**
- **新增薄弱点**：跑时序表的自洽性检查（首次暴露 07-31）。
- **修复的薄弱点**：① **setData 性能定位的具体工具与打点** ✅（连续两次未过，本次通过，移出"跨模块稳定缺口"）；② **"降低凭证价值"的安全设计通则** ✅；③ **TTFT 的指标设计意义** ✅；④ 防抖 immediate 时序 → 代码已正确（但过程用了提示，**未升"面试可用"，待真闭卷**）。

---

## 📌 下次计划（当日最终版）
> **他明确要求：下次直接开始主线，不要开场铺垫。** 滚动复习压缩为主线讲完后的穿插抽查，不占开场。

1. **开场直接进主线：🏠 小程序长列表**（接他项目里的真实卡顿场景，与 setData 优化闭环）：分页加载 / 只 `setData` 增量、`recycle-view`、**虚拟列表在小程序里的特殊性**（拿不到 DOM、只能 `createSelectorQuery` 异步测量、不定高更难）。之后可接跨端选型 / 移动端适配。
2. **穿插（主线告一段落后再做，不占开场）**：
   - **防抖真闭卷**（本次是提示下改对的，需独立复现才升「面试可用」）
   - **节流补完**：trailing 分支 + 定时器版 + 两版行为差异 + **必须附时序表**（正好练自洽性检查）
3. 备选：浏览器强缓存 vs 协商缓存（0 分区，边界清晰，20 分钟可拿一个 ✅）。
4. **给他的作业（可选，价值最高）**：回到那个卡顿的小程序长列表，真机量一次 `setData` 耗时和数据量，改完再量一次——**把"我会做"变成"我做了"**，直接进简历。
5. **教练自查**：① 写日志前先跑 `Get-Date`；② 提方法论要求时必须同时给方法；③ 给修改建议时说清**边界**（改哪行、保留什么），他有"改过头"的稳定倾向。
