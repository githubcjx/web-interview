# 2026-07-15 会话记录
> 当日共 2 次会话（同日续接）｜ 累计约 60 分钟 ｜ 第 1 周

---

## 第 1 次会话 ｜ 时长约 20 分钟
- **本次内容**：开场 + 10 道摸底题定位水位；深讲「Promise 四大静态方法」；带讲「事件循环中 rAF 的归属」。
- **表现评价**：
  - 摸底暴露真实水位：JS/Vue 有基础但不精确，网络/性能/AI 五题全「不会」。
  - Q1 Promise 出现硬伤——`all` 与 `allSettled` 定义记反；`any` 全失败抛 AggregateError 漏答。
  - 讲解后追问 3 题（超时用 race / 部分失败用 allSettled / all 传非 Promise）**全对**，说明"给框架能正确应用"，问题在记忆精度而非理解。
  - rAF 归属：结论听懂，但"每帧自调度"的好处只答出"自由控制"，漏了「后台自动暂停」「不堆积跳帧」两大关键点。
- **新增薄弱点**：rAF 自调度好处答不全；ref/.value 与 reactive 局限、强/协商缓存、HTTPS、虚拟列表、Web 指标、SSE、流式 Markdown（均已同步至 progress.md）。
- **修复的薄弱点**：Promise 四大静态方法（由"记反"→ 追问全对，升级为 ✅ 掌握）。

---

## 第 2 次会话（同日续接）｜ 时长约 40 分钟
- **本次内容**：
  - 复查 rAF 自调度好处（通过 ✅）。
  - 主场深挖 **Vue `ref` 为什么要 `.value` / `reactive` 三大局限**，追问 3 题全对；补充 Vue 3.5 Reactive Props Destructure、自动解包只在顶层。
  - 深讲 **`computed` 缓存与惰性求值**（讲前已查证官方文档与尤雨溪 PR #10397，纠正版本），追问 4 题。
  - 机制建设：新增 `answers.md` 话术库并回填 7 条；CLAUDE.md 第五节加第 9 条版本时效性原则、第六节改为三文件自主维护。
- **表现评价**：
  - **rAF 复查全对**（后台自动暂停 + 不堆积跳帧）→ 升 ✅ 出清单。
  - **ref/reactive 追问 3/3 全对**，第 2 题（`ref({a:1})` 内部走 reactive 存 Proxy）答得干净。术语瑕疵：把"自动解包 unwrap"说成"解构"。
  - props 解构答的是 **3.4 旧版答案**（"不能解构"），已按新规则纠正为 3.5+ 编译时改写。
  - **computed 追问 1/4**：① computed vs methods **踩了刚讲完的翻车点**——说成"依赖变化时会重新执行"（正确：只标记失效），且"methods 为何无缓存"是循环论证（真因：Vue 无从追踪 method 的依赖，也就无从判断缓存何时失效）；② 何时必须用 methods 只答出事件处理，漏传参/副作用/缓存陷阱/非响应式依赖；③ 嵌套 computed 求值顺序"不会"（已补讲 pull vs push 与 glitch）；④ **非响应式变量题完全正确**。
  - **关键观察**：第 4 题答对却没意识到它就是第 2 题的实践推论——**知识点之间不主动串联**，这是 5 年经验档要补的表达能力。
- **新增薄弱点**：computed 表述内化不足；computed vs methods 的四条硬判据；嵌套 computed 拉模型；props 解构的编译边界（待抽查）；ref 顶层解包边界（待抽查）。（均已同步 progress.md）
- **修复的薄弱点**：rAF 自调度好处（✅）；ref 为什么要 `.value` + reactive 局限（✅，由"不会"直接升掌握）。

---

## 📌 下次计划（当日最终版）
1. **开场必做**：让他**用自己的话完整复述 computed**（惰性 + 缓存 + 标脏不重算 + 3.4 版本号计数），当场纠正表述——这是他的主要短板，不是理解问题，是嘴上功夫。
2. 复述过关后抽查：computed vs methods 四条判据 + Vue 3.5 props 解构的编译边界。
3. 之后二选一：**A** 继续 Vue（watch 实现原理 / nextTick）；**B** 开 AI 模块 SSE vs WebSocket（0 分模块，差异化性价比最高）。
