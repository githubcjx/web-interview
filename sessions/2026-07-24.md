# 2026-07-24 会话记录
> 当日共 1 次会话 ｜ 累计约 90 分钟 ｜ 第 2 周
> ⚠️ 本文件原误命名为 `2026-07-22.md`，2026-07-29 依据 commit 时间戳（cae98e9 @ 07-24 11:54）更正为 07-24。文内日期字样同步修正。

---

## 第 1 次会话 ｜ 时长约 90 分钟
- **本次内容**：
  - 开场滚动复习 3 题（断点续传服务器端 / 代码块为何最难流式渲染 / TTFT）。
  - 应他要求输出**学习总进度盘点**（4 天 6 次会话约 3.5h；13 模块 0✅/3⚠️/10❌；手写题 0/9；模拟面试 0 场）。
  - **手写题开工**：防抖（含 immediate、this/args 透传、cancel、返回值）两轮 review + 面试满分版；节流（时间戳版）一轮 review。
  - 延伸设计题：防抖返回值为何是"半残"设计 → 返回 Promise → **被抖掉的调用怎么处理（resolve 最终值 / 永远 pending 内存泄漏 / reject CancelledError）** → 串联 `AbortError` 与"取消被塞进异常通道"的语言约束。
  - 补充：`performance.now()` 单调时钟 vs `Date.now()` 墙上时钟（凡测时间差一律单调时钟，直接串联 TTFT 测量）。
  - **RAG 全流程**：定义与开卷考试类比、五条设计动机（含 ⭐ 权限隔离）、离线索引 + 在线检索两条链路、⭐ 前端在链路中的五个角色；追问 3 题；补讲切块策略全谱（含父子块 small-to-big）、Hybrid Search + RRF、评估集与二分法定位、Lost in the Middle、Agentic RAG。
  - 他主动反馈"AI 这块吸收有限、名词听不懂" → **教练下调 RAG 目标至"前端最小可用版"**，并诊断根因。
- **表现评价**：
  - **滚动复习 2.5/3**：Q1 断点续传 ✅ 完整（服务器缓存 + LLM 不可重放，上次的漏项补上了）；Q2 代码块 ✅（作用域大 + `**` 含义未定）；**Q3 TTFT ⚠️ 答浅**——量法基本对但 t0/t1 打点不精确（首个 chunk ≠ 首个非空 content），"为什么比总耗时重要"只答"用户感觉快"，未答出"总耗时由输出长度决定、那段时间用户在**读**不在**等**；TTFT 与 TPS 应正交拆分"。
  - **手写防抖 5/10 → 第二版更坏**：
    - 第 1 版：`this`/args 透传 ✅（唯一亮点）；immediate 分支无 return 导致 fn **执行两次** + 悬空定时器；`clearTimeout` 后未置 null；`cancel` 挂在调用返回值上（应挂函数本身）。
    - 第 2 版：`cancel` 挂载 ✅、分支互斥 ✅，但**新引入两个叠加 bug** —— ① 在 `callNow = !timeout` **之前**就调 `cancel()` 把判断依据抹掉 → `callNow` 恒 true；② `if (callNow) return fn.apply()` 直接 return，**冷却定时器根本没机会创建**。结果 immediate 模式退化为"每次都立即执行"，完全没有防抖。
    - **关键诊断**：第 2 版是"照抄教练提示但没理解提示保护的是什么不变量"。已给出对策——**每接受一条修改建议，先问"这条保护的是哪条不变量"**（`timeout` 在 immediate 版身兼二职：定时器句柄 + 冷却期标志，两条不变量不能由同一变量承担）。
  - **手写节流 6.5/10**：时间戳版核心公式 ✅、`this`/args ✅、复用同一个 `now` ✅、`return result` 位置这次是对的（节流 leading 同步执行，返回值有意义）。但 **`timer` 是死代码从未赋值**（说明脑子里有 trailing 骨架、手上没落下去）、**缺 trailing → 末次触发永久丢失**（滚动停止后最后一段位移不计算）。**明确要求的"自己跑一遍时序表"没有执行**——而只要跑第一行就能自己抓到 timer 未赋值。
  - **RAG 追问 1/3**：
    - Q1 chunk 太小/太大 ✅（补充：太大真正的问题是"一个向量代表多个主题→语义被平均掉→检索精度崩"，不只是浪费 token）。
    - **Q2 ❌ 答错**——问"型号 XR-2000 检索不到为什么"，他答"知识库没这份文档，要补资料"。① 审题失误（题干已设定文档在库）；② **归因偏差再现**：假设一个简单外部原因，而不分析技术机制的天然局限。正确答案：**向量检索对专有名词/型号/编号天生弱**（embedding 编码语义，型号几乎没有语义，XR-2000/XR-3000 在向量空间几乎重合）→ 解法 **Hybrid Search（BM25 + 向量 + RRF 融合）**、元数据过滤。
    - Q3 ⚠️ 半对——列出各环节说明**有分层定位意识** ✅，但"看老板写的提示词"跑偏，且**完全缺失"先建评估集量化基线"**这一第一步，也漏了数据层（文档解析就错了）。已补二分法：**把召回片段打印出来看**——没答案→检索层；有答案没答对→生成层。
  - **⚠️ 本次最重要的观察：归因偏差今天复现 4 次**（TTFT"用户感觉快" / 防抖第 2 版照抄提示 / RAG Q2"知识库没数据" / 节流跳过时序验证）。这已不是偶发，是稳定模式：**抓住一个听起来合理的解释就停止思考**。面试官只要追一层就会露馅。
  - **值得肯定**：主动说出"AI 这块只能吸收一点点、很多名词听不懂"——**不硬撑、如实反馈**，这是高质量的学习姿态。
- **教练自省与路线调整**：
  - **本次 RAG 讲超纲了**。BM25/RRF/HNSW/cross-encoder/Lost in the Middle 是后端与算法工程师的词汇，**前端岗面试不考到这个深度**。诊断根因：**AI 后端知识在他脑中没有任何"挂钩点"**——Vue 学得快是因为天天写，SSE 学得下去是因为落在前端能摸到的地方（fetch/流/渲染），而向量、切块、召回全是悬空名词，听完即滑走。**不是记忆力问题，是知识缺乏附着点。**
  - 决定：**RAG 深度下调至"前端最小可用版"**（链路 + 前端三角色 + 权限隔离动机），已写入 answers.md；其余内容标记为"听过一遍"，不作记忆要求。
  - 决定：**下阶段切到小程序模块**——他有真实项目经验（挂钩点最多）、是简历亮点却从未打磨、且能反向带出通用原理（双线程≈进程隔离、setData≈跨线程通信成本）。
- **新增薄弱点**：TTFT 的指标设计意义（TTFT/TPS 正交拆分）；防抖 immediate 时序（两版皆错，需闭卷默写）；节流 trailing 分支 + 时间戳版/定时器版差异；向量检索对专有名词的天然局限与 Hybrid Search。（均已同步 progress.md）
- **修复的薄弱点**：AI 断点续传服务器端要求 ✅；代码块流式渲染难点 ✅；SSE 实战细节复述 ✅（连续两次通过，移出清单）。另清理了清单与会话记录不一致的两条（props 解构归因、副作用——07-16 实际已复查通过但清单未同步）。

---

## 📌 下次计划（当日最终版）
1. **开场滚动复习**（3 题，都是今天的漏项）：① TTFT 为什么比总耗时重要（"用户在读不在等" + TTFT/TPS 正交）；② 型号检索不到为什么，怎么解（向量对专有名词弱 → Hybrid Search）；③ 快问：`performance.now()` 和 `Date.now()` 什么时候用哪个。
2. **主线：🏠 小程序模块开荒**（本次已定方向，从双线程架构起步）：
   - 双线程架构（逻辑层 / 渲染层分离的原因与代价）
   - setData 的通信成本与优化手段
   - 之后接：登录鉴权链路（code → session）、分包加载、长列表处理
   - **教学纪律**：每次只讲 1 个点，讲完立刻让他复述（他的强项是"靠复述钉死表述"），不再一次性倾倒。
3. **手写题回归**（延后不等于跳过）：状态好时开专场，一天两题清库存。优先级 ① 防抖闭卷默写（今天两版都没写对）② 节流补 trailing + 定时器版 + 时序表。
4. 备选换脑：浏览器强缓存 vs 协商缓存（边界清晰的纯八股，20 分钟能吃透一个 ✅，适合恢复信心）。
5. **教练提醒（对他的核心对策）**：每个结论都追问一句"为什么"逼出正确归因；每接受一条建议先问"它保护的是什么不变量"；写完代码必须自己跑时序表。
