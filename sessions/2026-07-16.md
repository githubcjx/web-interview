# 2026-07-16 会话记录
> 当日共 2 次会话 ｜ 累计约 90 分钟 ｜ 第 1 周

---

## 第 1 次会话 ｜ 时长约 50 分钟
- **本次内容**：
  - 开场复查 **computed 复述**（让他用自己的话完整讲一遍）。
  - 深挖 **Preact Signals**（他主动提问"这是什么"）。
  - 讲透 **版本号计数 + globalVersion + 双向链表**的完整机制与流程（他主动要求"想真懂"）。
  - 复查 **computed vs methods 四判据**。
  - 补讲 **"副作用"概念**（他主动说这块模糊）。
  - 攻 A：**props 解构的编译边界**（`watch(count)` vs `watch(() => count)`）。
- **表现评价**：
  - **computed 复述——干净通过 ✅**。上次翻车的那句（"依赖变化会重新执行"）这次自己绕过去了，正确说成"标记失效、下次读 .value 才算"。表述已内化。缺的只是两个加分句（3.4"结果没变不触发下游"、借鉴 Preact Signals），非硬伤。
  - **computed vs methods 四判据——4/4 全中 ✅**（上次仅 1/4）。且能把第 4 条"依赖非响应式数据"和昨天独立答对的"非响应式变量题"联系起来——**知识串联的短板本次有改善**。
  - **props 解构边界——结论对（场景一挂/场景二通），但机制解释错**：误以为"编译器改写不了 watch 回调内部"。已纠正：编译器函数体内照样改写，真正原因是**值 vs getter 的求值时机**（`watch(count)`→`watch(props.count)`当场求值成死值；`watch(()=>count)`传 getter 延后取值），且这本是 watch 的通用传参规则。
  - **主动提问质量高**：Signals、版本号机制、副作用三处都是他自己意识到"不懂/模糊"主动发问——学习主动性强，值得肯定。
- **新增薄弱点**：props 解构边界的**正确归因**（值 vs getter，而非改写范围）需下次复述验证。
- **修复的薄弱点**：computed 表述内化（✅ 升掌握）；computed vs methods 四判据（✅ 4/4）。
- **本次涉及但未独立测试（下次可抽查）**：版本号计数/双向链表机制（讲透但未复述）；副作用概念（讲解为主）；ref 顶层解包边界（仍待抽查）；嵌套 computed 拉模型（仍待抽查）。

---

## 第 2 次会话（同日续接）｜ 时长约 40 分钟
- **本次内容**：
  - 开场滚动复习 3 题（props 边界归因 / 嵌套 computed 模型 / 副作用三类）。
  - 正式开 **9. AI 应用前端 —— SSE vs WebSocket**：两者本质、核心对比表、"为什么 AI 聊天流式常用 SSE"四条理由。
  - 追问 3 题（发问/中止的实现、只传文本是否缺点、HTTP/1.1 六连接限制）。
  - 补充实战加分点：原生 EventSource 不能 POST/带 header → 生产用 **fetch + ReadableStream** 手动读流 + AbortController 中止。
  - 他主动提问：**多模态平台如何识别图片/文档/表格/音频**（认知拓展，非八股）。
- **表现评价**：
  - **滚动复习 2.5/3**：props 归因 ✅（已纠正为"值 vs getter"）；副作用 ✅（网络/DOM/定时器）；嵌套 computed ⚠️ 半对——答出"惰性"根因，但没说出模型名词"**拉模型 pull**"（已补）。
  - **SSE 追问表现好**：Q1 主动调出 AbortController（呼应前两天 race 超时留的钩子，**知识串联主动完成**）；Q2 方向对（文本够用）；Q3 **猜对方向**（HTTP 连接数限制），补全为"每域名约 6 连接、SSE 各占一条、HTTP/2 多路复用解决"。
  - **提问质量高**：多模态问题问到了"传输 vs 理解"的分界，学习姿态好。
- **新增薄弱点**：无硬新增（嵌套 computed 拉模型仍在复查中）。
- **修复的薄弱点**：props 解构边界正确归因（✅）；副作用（✅）；SSE vs WebSocket 核心对比 + 选型理由（✅，由 0 分建成框架）。
- **本次涉及但未独立复述（下次可抽查）**：SSE 为何用 fetch 而非原生 EventSource；HTTP/1.1 六连接限制；SSE 实战链路（重连/断点续传/首字延迟）尚未开讲。

---

## 📌 下次计划（当日最终版）
1. **开场滚动复习**：① 快问：为什么生产 AI 应用用 fetch+ReadableStream 而不用原生 EventSource（因不能 POST/带 header）；② 快问：SSE 在 HTTP/1.1 下的六连接坑 + 解法；③ 补抽：嵌套 computed 的"拉模型"一词。
2. **继续 SSE 实战链路**（把"懂协议"升到"做过产品"）：断线重连 + 断点续传（`Last-Event-ID`）、**首字延迟 TTFT / token 生成速度监控**、流式渲染抖动处理。
3. 之后可衔接：**流式 Markdown 解析（标签截断问题）** → 再进 **RAG**（多模态那题已埋钩子：文档抽取→切块→向量化）。
4. 备选换脑：Vue 的 watch/nextTick 原理。
