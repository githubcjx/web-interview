# 2026-08-06 会话记录
> 当日共 1 次会话 ｜ 累计约 90 分钟 ｜ 第 4 周

---

## 第 1 次会话 ｜ 时长约 90 分钟

- **本次内容**：
  1. **开场两道闭卷手写题**：防抖（含 immediate + cancel）、LRU 缓存 —— **两题代码均一次成型、完全正确，双双升「面试可用」**。
  2. **新训练项首次执行：自己设计测试用例**（交卷前先列用例清单）。
  3. **Vue Router 导航守卫（Vue 模块最后一块，至此内容全覆盖）**：三层守卫的作用域框架 → 为什么必须分层（"能拿到什么" vs "写在哪"）→ Pinia 单例反问的完整应对 → v4 返回值风格 → 登录拦截死循环 → `meta` vs 白名单 → `redirect` 开放重定向 → 动态权限 `addRoute` → **死循环通用判据** → 执行顺序 / `beforeResolve` / `afterEach` / 异步守卫的坑。

- **表现评价**：
  - **⭐ 手写防抖：闭卷一次写对，六版长跑收官**。上次残留的两个点全部守住——`setTimeout` 无条件执行（不进 `if/else`）、`!timer` 与 `let timer`(undefined) 配套；`callNow` 在 `clearTimeout` **之前**读；还主动加了 `cancel` 且在里面把 `timer = null`（cancel 后 immediate 能立刻再响应，加分项）。
  - **⭐ 手写 LRU：闭卷一次写对**，上次三个 bug 全部未复发（`keys().next().value`、`get` 未命中 `return -1`、**容量检查放在 `has→delete→set` 之后**）。
  - **用例设计（本次新训练项）—— 有进步，但仍是主要缺口**：
    - 防抖：只列了"连续触发只执行首次"的三行，**缺定时器到期行、缺冷却期结束后再调用的行** → 意味着**把 `timer = null` 删掉，他的用例全绿**。经点破后自己补出 t=150 到期、t=160 再调用两行 ✅。
    - LRU：4 个分支只覆盖 2 个（`get` 命中 ✅、`size > max` ✅；**缺 `get` 未命中、缺 `put` 已存在的 key**）。**但值得记一笔：`get` 影响淘汰顺序这条经典陷阱他这次自己想到了**（上次漏的两条之一）。点破后补的两条用例质量很高——用例 3（put a/put b/put a/put c）**能区分"删掉 delete 两行"和"没删"**，正是要的效果。
    - 教练加的那道题（"挑出最'删了也不报错'的一句"）**第一次被他跳过**，第二次问才答对（`get` 里的 delete/set → 用例 1 会红）。
  - **⭐ 产出一条通用自查规则**：**对每句"状态重置"问——删掉它，哪条用例会红？答不上来就是没被覆盖。**（变异测试的直觉版，可直接用于"你怎么保证测试质量"这类面试题。）
  - **Vue Router 部分，三处归因偏差集中复发**：
    - ① 问"为什么要三层守卫"，答"代码好维护、架构清晰" —— **属于"不好做"级理由，挡不住"我在 beforeEach 里按 to.name 分发不也一样"的反问**。给了具体场景（表单未保存）后，一句话答对"全局守卫只有 to/from，拿不到组件里的表单状态" ✅。
    - ② 答动态路由硬伤时说"用户可以改 URL 跳到没权限的页面" —— **不成立**（守卫照样拦得住）。对的那半是"代码都被打包了"，但没推到危害层（真正的危害是**接口路径与参数结构泄露 → 直接绕过前端打后端**）。
    - ③ 死循环解法答 `from.path === to.path` —— **归因抓现象**。让他跑"/home → /order 未登录"这个反例后自己发现条件不生效；点出根因"重定向目标本身也被守卫拦"后，一次答对"判断 `to.path` 在不在白名单" ✅。
  - **⚠️ 本次最硬的一道坎：抽象规律（`addRoute` 后为什么不死循环）**。第一次答"我不会"，第二次答"检查角色权限和白名单" —— **把两个具体实例当成规律**（换成"资料没填完"的新场景立刻失效）。改为直接讲判据 + 三场景对照表后，**复述 ✅ 干净，并用它独立推对了新场景（没绑手机号 → 会死循环 → 豁免目标页）** ✅。
  - **教练侧失误（他当场纠正，正确）**：我用"同时开两个浏览器标签会导致 Pinia 状态污染"举例，**该例不成立**（不同 tab 是不同 JS 运行时、不同 store 实例）。已当场更正为**后台系统页内多页签 + `keep-alive` 缓存多实例**（真实且常见）。他明确要求：**举例前必须自查例子是否成立，不能编。**

- **新增薄弱点**：
  - "做不到 vs 不好做"的理由分级 **第 2 次复发**（08-05 表单组件库选型 → 08-06 三层守卫）。
  - **抽象规律的提取能力**（把实例当规律）——本次首次以"完全答不出"的形式暴露。
  - 用例设计仍缺一半分支（能自造陷阱用例，但不做分支盘点）。

- **修复的薄弱点**：
  - ✅ **防抖 immediate 的执行时序** —— 闭卷一次成型，移出清单，升「面试可用」。
  - ✅ **LRU（原为"基本可用"）** —— 闭卷一次成型，升「面试可用」。
  - ✅ **`undefined` vs `null` 与 `===` 的配套** —— 本次主动改用 `!timer`，移出清单。
  - ✅ **跑时序表从代码字面读值** —— 本次未再出现从预期读值的情况（防抖表全对）。

---

## 📌 下次计划（当日最终版）
> 沿用约定：**开场直接进主线，一次一题，同天内容不复习**。

1. **主线：Vue 模块结业测验（5 题闭卷）** —— **Vue 内容已全覆盖**，过了就是 13 个模块中的**第二个 ✅**。出题范围建议偏向"讲过但没独立测过"的：
   - `key` / diff（状态错乱那条已答过，可换角度）
   - `provide/inject` 的坑（传容器不传值 / 必须同步调用）
   - **响应性属于容器不属于值**（⭐ 统一规律，要求他自己举出 4 个实例中的 2 个）
   - **路由守卫死循环判据**（换一个全新场景，测抽象规律是否真的内化）
   - `nextTick` / `watch flush` 二选一
   - 版本号计数 / 双向链表（薄弱点清单里挂了很久，可作为附加题）
2. **穿插：节流补完** —— trailing + 定时器版 + 两版行为差异，**必须自附时序表 + 自己先列用例清单**（手写题清单欠账最久的一题）。
3. **备选主线（Vue 结业后）**：浏览器**强缓存 vs 协商缓存**（0 分区、边界清晰、20 分钟可拿一个 ✅），或 TypeScript 开荒。
4. **⭐ 持续训练项**：
   - 交卷前**先列用例清单**，并且**按分支盘点**（每个 `if` 一条 + 边界各一条），不能只凭直觉造两条。
   - 每个理由问一句：**这是"做不到"还是"不好做"？** —— 已连续两次栽在这里，下次主动设陷阱测。
   - 讲完一个机制，要求**用它去判断一个没讲过的新场景**（这是抽象能力的唯一检验方式，本次证明"复述通过"不代表"能迁移"）。
5. **教练自查**：① 写日志前先跑 `Get-Date`；② 提方法论要求必须同时给方法；③ 给修改建议时说清边界（**本次做到了**：明确说"是这个 `if` 冗余，不是这一段可以删"）；④ "你帮我做"时改为"我做第一步、其余交还给他"；⑤ **举例前先自查例子是否真实成立**（本次因"浏览器多标签"被他当场纠正，新增此项）。
6. **给他的作业（可选，价值最高，已挂两次）**：回到那个卡顿的小程序长列表，真机量一次 `setData` 耗时和数据量，改完再量 —— 把"我会做"变成"我做了"。
